diff --git a/skills/agent-benchmarking/SKILL.md b/skills/agent-benchmarking/SKILL.md
index c39073e..7ca5c72 100644
--- a/skills/agent-benchmarking/SKILL.md
+++ b/skills/agent-benchmarking/SKILL.md
@@ -1,7 +1,7 @@
 ---
 name: agent-benchmarking
 description: Design, execute, audit, or analyze controlled benchmarks of agents, prompts, skills, models, tools, and workflows with sealed conditions, certified graders, resumable Fabric-native execution, exact telemetry, and task-paired inference.
-compatibility: Verified for Pi 0.84.4 and Pi Fabric 0.77.0. Requires a runtime-capability receipt proving the effective 100-or-lower call ceiling, staged runner bindings, and recursive requests with custom cwd omitted. Other versions or missing capabilities return unsupported before mutation.
+compatibility: Requires Pi >=0.84.4 and Pi Fabric >=0.77.0. Higher versions must pass the runtime-capability checks proving the effective 100-or-lower call ceiling, staged runner bindings, and recursive requests with custom cwd omitted. Older or malformed versions and missing capabilities return unsupported before mutation.
 ---
 
 # Agent Benchmarking
@@ -44,7 +44,7 @@ Every stage has frozen input digests, exact planned IDs, call reservations inclu
 
 Run gates in this order:
 
-1. **Capability** - exact versions and requested runner behavior are canaried. Failure: `unsupported`.
+1. **Capability** - Pi >=0.84.4 and Pi Fabric >=0.77.0; record the exact installed versions and canary requested runner behavior. Failure: `unsupported`.
 2. **Protected state** - declare protected roots, capture the baseline, and prove task-relevant workspace/home/config/cache/browser/account/tool-service isolation. Existing mismatch or no safe isolation: `blocked`.
 3. **Design** - freeze population, estimand, practical thresholds, vetoes, revisions, randomization, graders, stage IDs, and global call/token/cost budgets.
 4. **Condition and mechanism** - prove exact-path loading and total mechanism evidence for actor and non-actor terminals.
diff --git a/skills/agent-benchmarking/references/architecture.md b/skills/agent-benchmarking/references/architecture.md
index 2ea5e67..822bf35 100644
--- a/skills/agent-benchmarking/references/architecture.md
+++ b/skills/agent-benchmarking/references/architecture.md
@@ -4,7 +4,9 @@ This reference maps the public benchmark workflow to the installed Pi and Pi Fab
 
 ## Version scope
 
-This map is verified only for:
+The runner requires **Pi >=0.84.4** and **Pi Fabric >=0.77.0**, not exact version pins. Versions are compared by SemVer precedence: build metadata is ignored, and a prerelease of the minimum release is below that minimum. Malformed or older versions return `unsupported`. Higher versions pass the version gate but must still satisfy every capability check and exact-path canary. Record and seal the exact versions used by each run; a runtime upgrade does not authorize changing an existing sealed condition.
+
+The historical declaration map below was verified for:
 
 - Pi coding agent 0.84.4;
 - Pi Fabric 0.77.0;
diff --git a/skills/agent-benchmarking/references/validation.md b/skills/agent-benchmarking/references/validation.md
index 8d314e5..be7bee2 100644
--- a/skills/agent-benchmarking/references/validation.md
+++ b/skills/agent-benchmarking/references/validation.md
@@ -46,11 +46,11 @@ python -B /home/balauru/.pi-profiles/fabric/skills/ultra-skill-creator/scripts/v
   > "$RUN_ROOT/skill-validator.txt"
 ```
 
-The deterministic suite must exercise every cataloged known-good, known-bad, isolated-defect, boundary, malformed, and adversarial case. Static checks do not replace generated Fabric guest declarations.
+The deterministic suite must exercise every cataloged known-good, known-bad, isolated-defect, boundary, malformed, and adversarial case. The production runtime-gate regression must accept the minimum and higher compatible versions, reject older or malformed versions, respect SemVer prerelease/build precedence, and still reject incompatible capability receipts on higher versions. Static checks do not replace generated Fabric guest declarations.
 
 ## Exact fixed-runner canary
 
-In an authorized Pi session with the pinned Fabric version loaded, pass the exact UTF-8 bytes of [the fixed runner](../workflows/benchmark.ts) as the `fabric_exec` program. Do not wrap, retype, patch, or replace it. Supply the [non-scoring audit request](../validation/fixtures/canary/workflow-request.json) through `strings.request`.
+In an authorized Pi session with Pi >=0.84.4 and Fabric >=0.77.0 loaded and their exact versions recorded, pass the exact UTF-8 bytes of [the fixed runner](../workflows/benchmark.ts) as the `fabric_exec` program. Do not wrap, retype, patch, or replace it. Supply the [non-scoring audit request](../validation/fixtures/canary/workflow-request.json) through `strings.request`.
 
 Write the tool response, type-check result, request bytes/digest, trace/details, phase output, and zero-agent-call assertion under `$RUN_ROOT/fabric-audit/`. A TypeScript diagnostic must occur before any `agents.run` call.
 
diff --git a/skills/agent-benchmarking/tests/test_helpers.py b/skills/agent-benchmarking/tests/test_helpers.py
index 1c020a6..b27606e 100644
--- a/skills/agent-benchmarking/tests/test_helpers.py
+++ b/skills/agent-benchmarking/tests/test_helpers.py
@@ -370,6 +370,105 @@ class WorkflowTemplateTests(TemporaryDirectoryTest):
         self.assertEqual(receipt["status"], "passed")
         self.assertEqual(receipt["bundle_sha256"], lib.sha256_file(ROOT / "workflows/benchmark.ts"))
 
+    def test_runtime_version_minima_preserve_capability_gates(self) -> None:
+        # Exercise the production gate with a fake doctor, never a scored run.
+        probe = r"""
+const assert = require('node:assert/strict');
+const { readFileSync } = require('node:fs');
+const { createRequire } = require('node:module');
+const { resolve } = require('node:path');
+const vm = require('node:vm');
+const ts = createRequire(resolve(process.argv[1], 'package.json'))('typescript');
+const source = readFileSync(process.argv[2], 'utf8');
+const entry = 'return await runBenchmarkStage(π.request);';
+assert.equal(source.split(entry).length, 2);
+const exposed = source.replace(entry, `return {
+  inspectInstalledRuntime,
+  baseline: {
+    schema_version: 1, status: "passed",
+    pi_version: "0.84.4", fabric_version: "0.77.0",
+    effective_max_calls: 100, recursive_agents: true, recursive_custom_cwd: false,
+    absolute_log_roots: ["/tmp/pi-fabric-runs-*"],
+    actor_mesh_default_root: ".pi/fabric/mesh", actor_mesh_root_env: "PI_FABRIC_MESH_ROOT",
+    output_bounds: EXPECTED_OUTPUT_BOUNDS, event_log_bounds: EXPECTED_EVENT_LOG_BOUNDS,
+    supported_agent_request_fields: EXPECTED_AGENT_REQUEST_FIELDS,
+    supported_agent_result_fields: EXPECTED_AGENT_RESULT_FIELDS,
+  },
+};`);
+const checked = ts.transpileModule(`(async () => {${exposed}})()`, {
+  compilerOptions: { target: ts.ScriptTarget.ES2022 }, reportDiagnostics: true,
+});
+assert.equal((checked.diagnostics ?? []).length, 0);
+let capability;
+let doctorCalls = 0;
+const context = {
+  pi: { bash: async ({ command }) => {
+    assert.ok(command.includes(' doctor '), 'only the fake runtime doctor may run');
+    doctorCalls += 1;
+    return { ok: true, output: JSON.stringify(capability) };
+  } },
+};
+(async () => {
+  const gate = await vm.runInNewContext(checked.outputText, context);
+  const versions = [
+    ['0.84.4', '0.77.0', true],
+    ['0.84.5', '0.77.1', true],
+    ['0.85.0', '0.82.6', true],
+    ['0.100.0', '0.100.0', true],
+    ['1.0.0', '1.0.0', true],
+    ['0.84.4+build.7', '0.77.0+build.2', true],
+    ['0.85.0-rc.1', '0.78.0-rc.1', true],
+    ['0.84.3', '0.77.0', false],
+    ['0.83.99', '0.77.0', false],
+    ['0.84.4', '0.76.99', false],
+    ['0.84.4-rc.1', '0.77.0', false],
+    ['0.84.4', '0.77.0-rc.1', false],
+    ['0.084.4', '0.77.0', false],
+    ['0.85.0-01', '0.77.0', false],
+    ['0.84', '0.77.0', false],
+    ['v0.85.0', '0.77.0', false],
+    ['0.85.0 ', '0.77.0', false],
+    ['unknown', '0.77.0', false],
+    ['0.84.4', null, false],
+    ['0.84.4', 77, false],
+  ];
+  for (const [pi_version, fabric_version, accepted] of versions) {
+    capability = { ...gate.baseline, pi_version, fabric_version };
+    if (accepted) {
+      const result = await gate.inspectInstalledRuntime();
+      assert.equal(result.pi_version, pi_version);
+      assert.equal(result.fabric_version, fabric_version);
+    } else {
+      await assert.rejects(gate.inspectInstalledRuntime(), /UNSUPPORTED:.*minimum/);
+    }
+  }
+  const incompatible = [
+    { effective_max_calls: 101 },
+    { recursive_custom_cwd: true },
+    { absolute_log_roots: [] },
+    { output_bounds: {} },
+    { event_log_bounds: {} },
+    { supported_agent_request_fields: [] },
+    { supported_agent_result_fields: [] },
+  ];
+  for (const patch of incompatible) {
+    capability = { ...gate.baseline, pi_version: '0.85.0', fabric_version: '0.82.6', ...patch };
+    await assert.rejects(gate.inspectInstalledRuntime(), /UNSUPPORTED:/);
+  }
+  assert.equal(doctorCalls, versions.length + incompatible.length);
+  console.log(JSON.stringify({ version_cases: versions.length, capability_cases: incompatible.length, model_calls: 0 }));
+})().catch(error => { console.error(error); process.exitCode = 1; });
+"""
+        completed = subprocess.run(
+            ["node", "-e", probe, str(ROOT.parent.parent / "npm/node_modules/pi-fabric"),
+             str(ROOT / "workflows/benchmark.ts")],
+            check=False, stdout=subprocess.PIPE, stderr=subprocess.PIPE,
+        )
+        self.assertEqual(completed.returncode, 0, completed.stderr.decode())
+        self.assertEqual(json.loads(completed.stdout), {
+            "version_cases": 20, "capability_cases": 7, "model_calls": 0,
+        })
+
     def test_execute_publishes_total_mechanisms_for_every_branch(self) -> None:
         profile = ROOT.parent.parent
         with tempfile.TemporaryDirectory(prefix=".agent-benchmark-mechanism-probe-", dir=profile) as temporary:
diff --git a/skills/agent-benchmarking/workflows/benchmark.source.ts b/skills/agent-benchmarking/workflows/benchmark.source.ts
index 770abbe..ebb546e 100644
--- a/skills/agent-benchmarking/workflows/benchmark.source.ts
+++ b/skills/agent-benchmarking/workflows/benchmark.source.ts
@@ -174,8 +174,8 @@ type GradingJob = {
 const SKILL_ROOT = "/home/balauru/.pi-profiles/fabric/skills/agent-benchmarking";
 const FABRIC_ROOT = "/home/balauru/.pi-profiles/fabric/npm/node_modules/pi-fabric";
 const PI_ROOT = "/home/balauru/.local/share/pi-node/node-v22.23.1-linux-x64/lib/node_modules/@earendil-works/pi-coding-agent";
-const VALIDATED_PI_VERSION = "0.84.4";
-const VALIDATED_FABRIC_VERSION = "0.77.0";
+const MINIMUM_PI_VERSION = "0.84.4";
+const MINIMUM_FABRIC_VERSION = "0.77.0";
 const EXPECTED_OUTPUT_BOUNDS = {
   execution_details_max_bytes: 524288,
   execution_trace_max_bytes: 524288,
@@ -193,7 +193,7 @@ const EXPECTED_AGENT_RESULT_FIELDS = [
   "sessionId", "runnerSessionId", "attachCommand", "branch", "worktree", "text", "value", "error", "logFile",
   "residency", "task", "startedAt", "finishedAt", "turns", "toolCalls", "usage", "pendingMessages",
 ];
-// The pinned runtime enforces an effective per-execution ceiling of 100.
+// The historical runtime enforced an effective per-execution ceiling of 100.
 // Keep the coordinator ceiling at or below it even if a caller/runtime advertises more.
 const EFFECTIVE_MAX_AGENT_CALLS = 100;
 const HARD_MAX_CONCURRENCY = 32;
@@ -281,6 +281,24 @@ function unsupported(condition: unknown, text: string): asserts condition {
   if (!condition) throw new Error(`UNSUPPORTED: ${text}`);
 }
 
+function meetsMinimumVersion(value: unknown, minimum: string): boolean {
+  if (typeof value !== "string") return false;
+  const match = /^(0|[1-9][0-9]*)[.](0|[1-9][0-9]*)[.](0|[1-9][0-9]*)(?:-([0-9A-Za-z-]+(?:[.][0-9A-Za-z-]+)*))?(?:[+][0-9A-Za-z-]+(?:[.][0-9A-Za-z-]+)*)?$/.exec(value);
+  if (match === null || match[0] !== value) return false;
+  // SemVer forbids leading zeroes in numeric prerelease identifiers.
+  if (match[4]?.split(".").some((part) => /^[0-9]+$/.test(part) && part.length > 1 && part[0] === "0")) return false;
+  const required = minimum.split(".");
+  for (let index = 0; index < 3; index += 1) {
+    const actual = match[index + 1];
+    const expected = required[index];
+    // Compare canonical digit strings without precision loss or lexical 100 < 84.
+    if (actual.length !== expected.length) return actual.length > expected.length;
+    if (actual !== expected) return actual > expected;
+  }
+  // Both minimum constants are stable releases; their prereleases are older.
+  return match[4] === undefined;
+}
+
 function assertSafePath(value: unknown, label: string): asserts value is string {
   assert(typeof value === "string" && value.length > 0, `${label} must be a non-empty string`);
   assert(value.length <= 512 && SAFE_PATH.test(value), `${label} is not a safe relative path`);
@@ -637,10 +655,10 @@ async function inspectInstalledRuntime(): Promise<RuntimeCapabilities> {
   const capability = JSON.parse(gate.output) as unknown as RuntimeCapabilities;
   unsupported(capability && capability.schema_version === 1 && capability.status === "passed",
     "installed runtime doctor returned no passing capability receipt");
-  unsupported(capability.pi_version === VALIDATED_PI_VERSION,
-    `Pi ${String(capability.pi_version)} differs from validated ${VALIDATED_PI_VERSION}`);
-  unsupported(capability.fabric_version === VALIDATED_FABRIC_VERSION,
-    `Pi Fabric ${String(capability.fabric_version)} differs from validated ${VALIDATED_FABRIC_VERSION}`);
+  unsupported(meetsMinimumVersion(capability.pi_version, MINIMUM_PI_VERSION),
+    `Pi version ${String(capability.pi_version)} does not meet minimum ${MINIMUM_PI_VERSION} (valid SemVer required)`);
+  unsupported(meetsMinimumVersion(capability.fabric_version, MINIMUM_FABRIC_VERSION),
+    `Pi Fabric version ${String(capability.fabric_version)} does not meet minimum ${MINIMUM_FABRIC_VERSION} (valid SemVer required)`);
   unsupported(capability.effective_max_calls === EFFECTIVE_MAX_AGENT_CALLS,
     `effective call cap ${String(capability.effective_max_calls)} differs from validated ${EFFECTIVE_MAX_AGENT_CALLS}`);
   unsupported(capability.recursive_agents === true && capability.recursive_custom_cwd === false,
diff --git a/skills/agent-benchmarking/workflows/benchmark.ts b/skills/agent-benchmarking/workflows/benchmark.ts
index a112cec..5ad2eb9 100644
--- a/skills/agent-benchmarking/workflows/benchmark.ts
+++ b/skills/agent-benchmarking/workflows/benchmark.ts
@@ -1,7 +1,7 @@
 /* GENERATED FILE - DO NOT EDIT.
  * Source: workflows/benchmark.source.ts
  * Module: workflows/artifact_store.ts
- * Source-SHA256: 08ad4f11ee9484b3c7a205e7107491c87e2e6c20852ffb1c682139f8fd244ead
+ * Source-SHA256: 29ca0f4d609ce32f7b28ce84e1cca9da0ecafdfa3471b277df3cf889be86c3f8
  * Artifact-Store-SHA256: 365e111eee46fff16a5c3ab4d343b7afa35aa68b7df41155ce19e1f6d14b6ed5
  * Regenerate: python -B scripts/build_benchmark_bundle.py --write
  */
@@ -181,8 +181,8 @@ type GradingJob = {
 const SKILL_ROOT = "/home/balauru/.pi-profiles/fabric/skills/agent-benchmarking";
 const FABRIC_ROOT = "/home/balauru/.pi-profiles/fabric/npm/node_modules/pi-fabric";
 const PI_ROOT = "/home/balauru/.local/share/pi-node/node-v22.23.1-linux-x64/lib/node_modules/@earendil-works/pi-coding-agent";
-const VALIDATED_PI_VERSION = "0.84.4";
-const VALIDATED_FABRIC_VERSION = "0.77.0";
+const MINIMUM_PI_VERSION = "0.84.4";
+const MINIMUM_FABRIC_VERSION = "0.77.0";
 const EXPECTED_OUTPUT_BOUNDS = {
   execution_details_max_bytes: 524288,
   execution_trace_max_bytes: 524288,
@@ -200,7 +200,7 @@ const EXPECTED_AGENT_RESULT_FIELDS = [
   "sessionId", "runnerSessionId", "attachCommand", "branch", "worktree", "text", "value", "error", "logFile",
   "residency", "task", "startedAt", "finishedAt", "turns", "toolCalls", "usage", "pendingMessages",
 ];
-// The pinned runtime enforces an effective per-execution ceiling of 100.
+// The historical runtime enforced an effective per-execution ceiling of 100.
 // Keep the coordinator ceiling at or below it even if a caller/runtime advertises more.
 const EFFECTIVE_MAX_AGENT_CALLS = 100;
 const HARD_MAX_CONCURRENCY = 32;
@@ -288,6 +288,24 @@ function unsupported(condition: unknown, text: string): asserts condition {
   if (!condition) throw new Error(`UNSUPPORTED: ${text}`);
 }
 
+function meetsMinimumVersion(value: unknown, minimum: string): boolean {
+  if (typeof value !== "string") return false;
+  const match = /^(0|[1-9][0-9]*)[.](0|[1-9][0-9]*)[.](0|[1-9][0-9]*)(?:-([0-9A-Za-z-]+(?:[.][0-9A-Za-z-]+)*))?(?:[+][0-9A-Za-z-]+(?:[.][0-9A-Za-z-]+)*)?$/.exec(value);
+  if (match === null || match[0] !== value) return false;
+  // SemVer forbids leading zeroes in numeric prerelease identifiers.
+  if (match[4]?.split(".").some((part) => /^[0-9]+$/.test(part) && part.length > 1 && part[0] === "0")) return false;
+  const required = minimum.split(".");
+  for (let index = 0; index < 3; index += 1) {
+    const actual = match[index + 1];
+    const expected = required[index];
+    // Compare canonical digit strings without precision loss or lexical 100 < 84.
+    if (actual.length !== expected.length) return actual.length > expected.length;
+    if (actual !== expected) return actual > expected;
+  }
+  // Both minimum constants are stable releases; their prereleases are older.
+  return match[4] === undefined;
+}
+
 function assertSafePath(value: unknown, label: string): asserts value is string {
   assert(typeof value === "string" && value.length > 0, `${label} must be a non-empty string`);
   assert(value.length <= 512 && SAFE_PATH.test(value), `${label} is not a safe relative path`);
@@ -780,10 +798,10 @@ async function inspectInstalledRuntime(): Promise<RuntimeCapabilities> {
   const capability = JSON.parse(gate.output) as unknown as RuntimeCapabilities;
   unsupported(capability && capability.schema_version === 1 && capability.status === "passed",
     "installed runtime doctor returned no passing capability receipt");
-  unsupported(capability.pi_version === VALIDATED_PI_VERSION,
-    `Pi ${String(capability.pi_version)} differs from validated ${VALIDATED_PI_VERSION}`);
-  unsupported(capability.fabric_version === VALIDATED_FABRIC_VERSION,
-    `Pi Fabric ${String(capability.fabric_version)} differs from validated ${VALIDATED_FABRIC_VERSION}`);
+  unsupported(meetsMinimumVersion(capability.pi_version, MINIMUM_PI_VERSION),
+    `Pi version ${String(capability.pi_version)} does not meet minimum ${MINIMUM_PI_VERSION} (valid SemVer required)`);
+  unsupported(meetsMinimumVersion(capability.fabric_version, MINIMUM_FABRIC_VERSION),
+    `Pi Fabric version ${String(capability.fabric_version)} does not meet minimum ${MINIMUM_FABRIC_VERSION} (valid SemVer required)`);
   unsupported(capability.effective_max_calls === EFFECTIVE_MAX_AGENT_CALLS,
     `effective call cap ${String(capability.effective_max_calls)} differs from validated ${EFFECTIVE_MAX_AGENT_CALLS}`);
   unsupported(capability.recursive_agents === true && capability.recursive_custom_cwd === false,
diff --git a/skills/agent-benchmarking/references/evidence/decision-ledger.md b/skills/agent-benchmarking/references/evidence/decision-ledger.md
--- a/skills/agent-benchmarking/references/evidence/decision-ledger.md
+++ b/skills/agent-benchmarking/references/evidence/decision-ledger.md
@@ -1,17 +1,17 @@
-# Durable decision ledger
-
-These decisions convert the v2-v14 failure chain into package policy.
-
-1. **One supported runner.** Callers execute the exact `workflows/benchmark.ts` bytes with a schema-valid request. They do not author, patch, wrap, or paste bespoke TypeScript for a benchmark session.
-2. **Capability before mutation.** Pin Pi/Fabric versions and prove request fields, stage selection, call cap, recursion, cwd, output bounds, and native-log access. Missing or contradictory support yields `unsupported` before assignment or grading.
-3. **Protected state is a launch gate.** Declare protected roots and mutable-state ownership, capture a baseline, and require an isolation plan. A detected protected mutation yields `blocked`; preserve it as evidence rather than cleaning it away.
-4. **Bounded stage plan.** Analyze is preplanned as one explicit zero-call `prepare` transaction, one or more bound `judge` call plans, one or more bound `adjudicate` call plans, and `finalize`. Every call stays below the effective runtime call cap, is independently resumable, and publishes a revision-bound checkpoint.
-5. **Transactional stage closure.** A stage commits inputs and assignments first, publishes all required terminals and derivatives create-only, reconciles exact identities, then publishes its checkpoint last. Finalize performs no model calls.
-6. **Global budgets.** Reserve measured, nested, judge, adjudicator, support, and retry calls before launch. Include recursive descendants in global accounting even when runtimes enforce only per-manager limits. Observational token/cost guards are not hard limits.
-7. **Recursive compatibility.** Recursive attempts omit `cwd`, start from Fabric's supported project root, and receive sealed absolute package/workspace paths. If the installed fixed runner cannot construct and validate that request, the condition is unsupported rather than adapted with custom code.
-8. **Total mechanism evidence.** Every terminal has a mechanism projection. Missing, unreadable, oversized, or unowned source evidence maps to an explicit invalid/unknown state, never absence or success.
-9. **Versioned telemetry.** Preserve native bytes and source semantics. Derived telemetry names its schema and transformer version, input digests, ownership scope, and unknowns.
-10. **Delta revisions.** Never reseal an edited closure. Plan a revision from the prior verified seal plus an explicit added/changed/removed path delta; preserve predecessor seals and the reason confirmatory validity is or is not retained.
-11. **Deterministic resume.** Skip valid terminals, run only never-assigned planned IDs, block assigned-without-terminal IDs, and use new linked IDs for model/tool retries. Serialization-only repair requires immutable contemporaneous source bytes.
-12. **Typed outcomes.** Route and stage outcomes are only `complete`, `checkpoint`, `blocked`, `unsupported`, `inconclusive`, or `failed`. Attempt outcomes remain schema-owned. Explanations live in structured blockers/limitations, not invented status strings.
-13. **External receipts.** Generated validation receipts and raw runtime captures live under `skill-evaluations/agent-benchmarking/`, not in the distributable skill. Fixtures required by tests remain in the package.
+# Durable decision ledger
+
+These decisions convert the v2-v14 failure chain into package policy.
+
+1. **One supported runner.** Callers execute the exact `workflows/benchmark.ts` bytes with a schema-valid request. They do not author, patch, wrap, or paste bespoke TypeScript for a benchmark session.
+2. **Capability before mutation.** Require Pi >=0.84.4 and Pi Fabric >=0.77.0, pin the exact versions used by each run, and prove request fields, stage selection, call cap, recursion, cwd, output bounds, and native-log access. Missing or contradictory support yields `unsupported` before assignment or grading.
+3. **Protected state is a launch gate.** Declare protected roots and mutable-state ownership, capture a baseline, and require an isolation plan. A detected protected mutation yields `blocked`; preserve it as evidence rather than cleaning it away.
+4. **Bounded stage plan.** Analyze is preplanned as one explicit zero-call `prepare` transaction, one or more bound `judge` call plans, one or more bound `adjudicate` call plans, and `finalize`. Every call stays below the effective runtime call cap, is independently resumable, and publishes a revision-bound checkpoint.
+5. **Transactional stage closure.** A stage commits inputs and assignments first, publishes all required terminals and derivatives create-only, reconciles exact identities, then publishes its checkpoint last. Finalize performs no model calls.
+6. **Global budgets.** Reserve measured, nested, judge, adjudicator, support, and retry calls before launch. Include recursive descendants in global accounting even when runtimes enforce only per-manager limits. Observational token/cost guards are not hard limits.
+7. **Recursive compatibility.** Recursive attempts omit `cwd`, start from Fabric's supported project root, and receive sealed absolute package/workspace paths. If the installed fixed runner cannot construct and validate that request, the condition is unsupported rather than adapted with custom code.
+8. **Total mechanism evidence.** Every terminal has a mechanism projection. Missing, unreadable, oversized, or unowned source evidence maps to an explicit invalid/unknown state, never absence or success.
+9. **Versioned telemetry.** Preserve native bytes and source semantics. Derived telemetry names its schema and transformer version, input digests, ownership scope, and unknowns.
+10. **Delta revisions.** Never reseal an edited closure. Plan a revision from the prior verified seal plus an explicit added/changed/removed path delta; preserve predecessor seals and the reason confirmatory validity is or is not retained.
+11. **Deterministic resume.** Skip valid terminals, run only never-assigned planned IDs, block assigned-without-terminal IDs, and use new linked IDs for model/tool retries. Serialization-only repair requires immutable contemporaneous source bytes.
+12. **Typed outcomes.** Route and stage outcomes are only `complete`, `checkpoint`, `blocked`, `unsupported`, `inconclusive`, or `failed`. Attempt outcomes remain schema-owned. Explanations live in structured blockers/limitations, not invented status strings.
+13. **External receipts.** Generated validation receipts and raw runtime captures live under `skill-evaluations/agent-benchmarking/`, not in the distributable skill. Fixtures required by tests remain in the package.
diff --git a/skills/agent-benchmarking/references/evidence/external-research.md b/skills/agent-benchmarking/references/evidence/external-research.md
--- a/skills/agent-benchmarking/references/evidence/external-research.md
+++ b/skills/agent-benchmarking/references/evidence/external-research.md
@@ -1,15 +1,15 @@
-# External and runtime evidence
-
-This package relies on installed primary contracts, not generic assumptions. The detailed declaration map is in the architecture reference; this file records the durable conclusions and transfer limits.
-
-| Evidence | Verified conclusion | Transfer limit |
-| --- | --- | --- |
-| Pi 0.84.4 package manifest, skills documentation, RPC documentation, and agent-session implementation | Skill expansion is path-specific; exact-path canaries must distinguish expansion from literal prompt text. | Recheck after Pi, transport, or invocation-path changes. |
-| Pi Fabric 0.77.0 public exports and generated guest declarations | `agents.run` returns the measurement-bearing result; `workflow.agent` is a lossy convenience projection. | Internal `dist` files are evidence only, never workflow imports. |
-| Pi Fabric execution service and agent manager implementation | Guest checking precedes host-call setup; concurrency and depth controls are normalized; recursive managers do not provide a tree-global semaphore. | Implementation details may change without public API compatibility. |
-| Pi Fabric budget and usage implementation | Direct `agents.run` calls bypass workflow helper token accounting; recursive/token/cost guards can overshoot. | Only a provider-side pre-consumption limit is a hard spend boundary. |
-| Pi Fabric output/detail/trace bounds and historical v4/v7 captures | Large logs cannot safely transit bounded result channels. | Archive authorized native logs by validated absolute path and derive compact evidence locally. |
-| Historical v13 execution receipt | The observed effective per-invocation ceiling was 100 agent calls despite a larger requested/package-level value. | Treat 100 as an observed compatibility bound, not a universal constant; probe and reserve headroom. |
-| OpenTelemetry GenAI semantic conventions, Development status at the recorded research cutoff | Requested/resolved/observed identity and provider-native usage are useful vocabulary. | Development conventions do not establish provider billing semantics or require a collector. |
-
-Compatibility claims are therefore narrow: only the pinned versions and canaried runner/provider path are supported. A version mismatch, absent stage API, recursive-cwd conflict, inaccessible absolute log, or unprovable call ceiling is an `unsupported` result before consequential mutation.
+# External and runtime evidence
+
+This package relies on installed primary contracts, not generic assumptions. The detailed declaration map is in the architecture reference; this file records the durable conclusions and transfer limits.
+
+| Evidence | Verified conclusion | Transfer limit |
+| --- | --- | --- |
+| Pi 0.84.4 package manifest, skills documentation, RPC documentation, and agent-session implementation | Skill expansion is path-specific; exact-path canaries must distinguish expansion from literal prompt text. | Recheck after Pi, transport, or invocation-path changes. |
+| Pi Fabric 0.77.0 public exports and generated guest declarations | `agents.run` returns the measurement-bearing result; `workflow.agent` is a lossy convenience projection. | Internal `dist` files are evidence only, never workflow imports. |
+| Pi Fabric execution service and agent manager implementation | Guest checking precedes host-call setup; concurrency and depth controls are normalized; recursive managers do not provide a tree-global semaphore. | Implementation details may change without public API compatibility. |
+| Pi Fabric budget and usage implementation | Direct `agents.run` calls bypass workflow helper token accounting; recursive/token/cost guards can overshoot. | Only a provider-side pre-consumption limit is a hard spend boundary. |
+| Pi Fabric output/detail/trace bounds and historical v4/v7 captures | Large logs cannot safely transit bounded result channels. | Archive authorized native logs by validated absolute path and derive compact evidence locally. |
+| Historical v13 execution receipt | The observed effective per-invocation ceiling was 100 agent calls despite a larger requested/package-level value. | Treat 100 as an observed compatibility bound, not a universal constant; probe and reserve headroom. |
+| OpenTelemetry GenAI semantic conventions, Development status at the recorded research cutoff | Requested/resolved/observed identity and provider-native usage are useful vocabulary. | Development conventions do not establish provider billing semantics or require a collector. |
+
+The runner requires Pi >=0.84.4 and Pi Fabric >=0.77.0. These are minimum versions, not exact pins or a blanket claim about future runtimes. Record the exact installed versions and recheck the runner/provider path. A malformed or below-minimum version, mismatch with a sealed run, absent stage API, recursive-cwd conflict, inaccessible absolute log, or unprovable call ceiling is an `unsupported` result before consequential mutation.
