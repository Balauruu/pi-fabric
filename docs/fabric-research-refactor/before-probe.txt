import vm from 'node:vm';
import {readFile} from 'node:fs/promises';
const root='/home/balauru/.pi-profiles/fabric';
const files=['SKILL.md','references/stream-contracts.md','references/synthesis-and-reporting.md','references/workflow-program.md','scripts/workflow-program.js'];
const contents=await Promise.all(files.map(f=>readFile(`${root}/skills/fabric-research/${f}`,'utf8')));
const source=contents[4];
const program={routeReason:'Five distinct evidence questions',temporalMode:'current',requirements:Array.from({length:5},(_,i)=>({id:`r${i+1}`,text:`Resolve question ${i+1}`,required:true,evidenceStandard:'Exact primary passage'})),coordinatorRequirementIds:[],streams:Array.from({length:5},(_,i)=>({id:`s${i+1}`,label:`s${i+1}`,kind:'standard',requirementIds:[`r${i+1}`],requiredSourceClasses:['primary'],maxRetrievalSteps:8,contract:`Find primary evidence for question ${i+1}.`})),concurrency:5};
const calls=[],assignments=[];let active=0,peak=0;
const context={π:{task:'Resolve five independent questions',program:JSON.stringify(program)},pi:{read:async({path})=>{calls.push('read');return readFile(path,'utf8');}},tools:{list:async()=>{calls.push('list');return [{name:'fetch_content'}];}},agents:{run:async request=>{calls.push('agents.run');assignments.push(request);active++;peak=Math.max(peak,active);await new Promise(setImmediate);active--;const value={conclusion:'Direct primary support',evidence:[{requirementIds:request.task.match(/"id":"(r\d+)"/).slice(1),claim:'Supported claim',finding:'Exact supporting passage',url:'https://example.org/primary',status:'documented fact',confidence:'high',provenance:{sourceType:'primary',locator:'Section 1',method:'Primary record',origin:'Original'}}],contradictions:[],gaps:[],stopReason:'requirements-covered'};return {id:request.name,runner:'pi',status:'completed',value,text:JSON.stringify(value)};}},workflow:Object.fromEntries(['configure','item','event'].map(n=>[n,async()=>calls.push(n)])),phase:async()=>calls.push('phase'),parallel:async(thunks,{concurrency})=>{calls.push('parallel');let cursor=0;const out=[];await Promise.all(Array.from({length:Math.min(concurrency,thunks.length)},async()=>{while(cursor<thunks.length){const i=cursor++;out[i]=await thunks[i]();}}));return out;},budget:{total:Infinity,spent:()=>0,remaining:()=>Infinity}};
const result=await vm.runInNewContext(source,context);
console.log(JSON.stringify({kind:'deterministic mock of unchanged program, not native timing',readBytes:Object.fromEntries(files.map((f,i)=>[f,Buffer.byteLength(contents[i])])),manifestBytes:Buffer.byteLength(context.π.program),requestedConcurrency:5,peak,operationCounts:calls.reduce((a,c)=>(a[c]=(a[c]||0)+1,a),{}),assignmentBytes:assignments.map(x=>Buffer.byteLength(x.task)),schemaBytes:Buffer.byteLength(JSON.stringify(assignments[0].schema)),resultBytes:Buffer.byteLength(JSON.stringify(result)),usefulReportsRetained:result.outcomes.filter(x=>x.report?.evidence.length).length,candidateUsableWithoutRetrievalTime:result.researchReceipt.coverage.usable,status:result.status},null,2));
