import { writeFileSync } from 'node:fs';
import { providerInputSchema, providerOutputSchema } from '../../src/evaluators/catalog.js';
import { definitionSchema } from '../../src/evaluators/contracts.js';
import { ACTION_MANIFEST, ACTOR_PROPOSAL_SCHEMA } from '../../src/research/contracts.js';
import { SUBSTRATE_ACTIONS, ARBOR_OWNER_REFS } from '../../src/managed/contracts.js';
const manifest = { version: 2, evaluationDefinition: definitionSchema(), evaluatorProvider: { catalog: "owning Pi profile/arbor.evaluators.json; read once before definition registration", maximumEntries: 8, requires: "configured exact refs with optional:true; no run-time widening", descriptorBinding: "tools.catalog descriptorHash plus effective input/output/risk/effect validation", inputSchema: providerInputSchema(), outputSchema: providerOutputSchema(), risk: "execute", effect: { kind: "emission", ordering: "ordered" }, maintenance: "quiesce and inspect affected runs, explicitly reload/re-register the derived definition, then immutable-bound owning-Pi resume; changed definition/catalog needs a new measurement" }, facade: ['arbor.start','arbor.inspect','arbor.control','arbor.export'], research: ['arbor.propose','arbor.dispatch','arbor.collect','arbor.evaluate','arbor.distill','arbor.decide'], ownerRequires: ARBOR_OWNER_REFS, actorRequires: ['agents.self'], actorProposalSchema: ACTOR_PROPOSAL_SCHEMA, configuration: { component: 'arbor', id: 'arbor', config: { stateDirectory: 'absolute path outside material' } }, commandsWithoutProvider: { setup: 'owning Pi configuration only', doctor: 'read-only diagnostics', availability: 'read-only package status', assets: 'read-only packaged assets' }, cli: 'read-only; no attachment in any mode', browser: 'read-only assets/projections; no mutation routes', actions: [...ACTION_MANIFEST, ...SUBSTRATE_ACTIONS.map(a => ({ref: `arbor.${a.name}`, caller: a.risk === 'read' ? 'reader' : 'owning-Pi-diagnostic', commands: [], actorCommitment: false, ...a}))] };
writeFileSync('docs/pr3-action-manifest.json', JSON.stringify(manifest, null, 2)+'\n');
console.log(`Manifest: ${manifest.actions.length} actions; ${Buffer.byteLength(JSON.stringify(manifest))} compact bytes`);
