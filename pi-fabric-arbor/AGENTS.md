# pi-fabric-arbor guidance

## V2 replacement work

- Follow `../docs/Arbor/deep-refactoring-plan.md` for the v2 replacement. The owner-local PR0 gate is recorded in `acceptance-ledger.md`; textual checks never substitute for its E2E lane. Use `src/managed/` for the production owner execution path and `npm run test:pr2:e2e` for its real-host gate; the PR0 probe is not the production driver. Keep broader acceptance and observed limitations in the ledger, never infer later production acceptance from fixture results.
- PR3's acceptance and native evidence are recorded in `acceptance-ledger.md` and `docs/pr3-interface-evidence.md`. Use `npm run test:pr3:e2e` for its real native gate, not deferred-only review fixtures. Preserve the actor -> settlement -> RPC user review path, exact stale bindings, approved-direction dispatch admission, renewed-review revocation (including rejection and historical receipt replay), immutable artifact identities/provenance, terminal monotonicity, and successful host-exit assertions.
- PR5 material ownership lives in `src/material/` and is reached through the existing ResearchService/ResearchStore/OwnerExecution/EvaluationEngine path. `execution: material` is one bounded owner-selected candidate journey; it is not PR6's autonomous policy loop. Run `npm run test:pr5` and `npm run test:pr5:e2e`, plus impacted prior native gates.
- Keep original source, owned repository/baseline, candidate, evaluation input and exact evaluator identities separate. Never mutate the original Git index/refs during capture/export/restore. Snapshot current indexed paths plus explicit untracked selections; do not resurrect staged deletions from HEAD. Refuse ignored selections, unresolved merges, submodules and sparse cases. Only settled proven writers may freeze/restore; ambiguous cleanup retains workspaces.
- Resolve full evaluation symlink chains against the immutable tree before materializing and when verifying; reject transitive escapes/cycles as exact-input identity failures, not a sandbox guarantee. Cancellation settlement must include unresolved evaluator invocations, not just candidate workers. Resume quiescent paused material runs explicitly without approving pending review or bypassing the command execute-policy route. Actual attempt IDs take precedence over baseline evaluator sentinels. Terminal material runs cannot pause/resume into new dispatch, and pause cannot hide interrupted/cleanup-pending writers; explicit reconciliation must retain unresolved state. Keep publication status and exact gate counts in the evidence/ledger, not inferred from full-journey claims.
- Keep the exact current-incumbent BigInt/quality/noise oracle authoritative. Use unrounded task/repeat numerators and counts for gain, relative thresholds, direction and noise decisions; rounded means are presentation only. Persist integration intent before owned Git CAS, finalize the database decision atomically and re-evaluate changed combined material. Real exact-evaluation review receipts remain separate from Fabric permission. Export only the captured-baseline delta. Explicit PR8 source apply/undo uses the separate owner UI and per-path journal below; no cleanup of retained runtime artifacts is authorized.
- PR4 evaluator ownership lives in `src/evaluators/` over the existing managed execution adapter. Run `npm run test:pr4` and `npm run test:pr4:e2e`; the latter uses the actual installed/source product, native Pi/Fabric and deterministic local inference. Keep exact committed-material evaluation separate from later dirty capture/candidate generation and incumbent adoption.
- Snapshot detached, recursively immutable expected native/provider bindings before awaits; never validate replies against callee-mutable requests or retain reply aliases across further awaits. Preserve committed executable flags in exact-material identities and verify mode drift. Treat filenames as data using prototype-free maps; verify persisted selected-file coverage and reject inherited prompt entries.
- Persist evaluator invocation reservations and native completion before grading. Judge infrastructure faults must interrupt, not become invalid grades; completion requires every invocation ingested. Resume must explicitly validate the saved owner/component/material/OID/epoch/definition/catalog/request bindings, reconstruct the native owner, and re-observe known handles. Unknown handles block without redispatch. Keep generation history, native execution, validity, quality, descriptive interpretation and incumbent decisions separate.
- Command effects must enter through execute-risk `arbor.evaluate` or autonomous `arbor.runResearch`; an agent-risk `arbor.start` or `arbor.control` cannot directly run/resume a command evaluator. Owning-Pi command programs compose the public actions under normal Fabric policy. Keep the CLI/browser read-only, including example assets: do not add a mutating helper CLI.
- Optional evaluator refs come only from the finite catalog read before definition registration. Validate the committed descriptor identity and effective schemas/risk/effect before calling a selected action through captured `context.call`. Catalog changes require explicit quiescent maintenance/re-registration and reconciliation, never a run-triggered reload or generic provider transport. Preset selection uses property presence: explicit/project null disables lower-precedence inheritance without reading the shadowed file.
- Keep `npm run test:source:retained` source-executed and included in normal `npm test`/`npm run check`. It must retain the named model, Git, persistence/artifact, recovery/report, concurrency, evaluator-parser, and component/provider assertions without emitted paths. Superseded v1 certificate/admission/containment/writable-Web suites remain historical, not active gates, until PR13.
- The managed Arbor component in the owning Pi host owns operational services and deterministic bounded dispatch/collection. Use captured post-activation `FabricComponentContext.call` only for exact declared public Fabric refs.
- The persistent Pi actor chooses research direction through structured `agents.ask` proposals. It cannot mutate Arbor services, approve itself, dispatch workers, or commit domain transitions.
- Only the owning Pi session may mutate, review, apply/undo, or generate exports. CLI and web are strictly read-only in every mode and gain no attachment transport.
- No Fabric API additions, private runtime imports, replacement runtime, broker, daemon, second participant registry, profile-local benchmarking-skill dependency, paid experiment inference, or dataset download.
- Import pi-fabric only through package exports. Never import `pi-fabric/src` or unexported `dist` paths.
- Keep v2 schemas bounded and closed, use exact decimal arithmetic for promotable values, and preserve transactional revision/material/evidence bindings.
- Preserve existing runtime data and artifacts. Update `acceptance-ledger.md` without upgrading evidence status unless the cited behavior was actually run and retained.

## PR6 owner-led research

- Keep operational bootstrap in `src/managed/RoleBundle.ts` and request assembly in `OwnerExecution`. New inspect/material/research runs snapshot package-resolved assets outside material before effects; resume uses that saved bundle. No candidate-relative fallback, role skill registration or transcript mirror.
- Record native operational request/bundle/reference identities before create/ask/spawn. Prior invocation attribution is immutable; native ID attachment is monotone. The explicit owning-Pi `reviseRoles` control requires quiescent pause and records a new binding/revision without rewriting saved spec/evaluation or prior invocation history.
- Finish all awaited admission work before the final duplicate-receipt check and synchronous reservation. Moving a role read between them caused a real duplicate native launch; retain the PR3 concurrent-dispatch regression.
- `docs/pr6-research-evidence.md` is the precise implementation/resume checkpoint. The autonomous `research` mode reuses OwnerExecution's actor lifecycle and MaterialJourney/EvaluationEngine operations. Run `npm run test:pr6:e2e` for both five-stage journeys, policy/refusal/stop/reload gates; source fixtures are not substitutes. Preserve the independently accepted serial PR6 gate as regression evidence. PR7 has its own scoped gate below; do not infer PR8-PR13 acceptance. Preserve episode-unique proposal receipt IDs on stopped-actor resume and unresolved evaluator states during actor cleanup.
- Preserve material/evaluator uncertainty across every actor outcome. Fresh review may advance only at successful, unchanged-binding quiescence, including research pause; stale/forged reviews remain rejected. Persist exact attempt/evaluation links and pin decided evidence, never infer by OID. Authorized generation lineage admits original settled evidence without rewriting provenance; known terminal worker failure is not a retirement/storage failure. Check applicable cumulative budgets immediately before every new native effect, including post-baseline actor creation, command checks and provider calls after awaited descriptor validation. A pre-dispatch refusal leaves the invocation reserved, not ambiguously launching. Keep commands and integration both require exact attempt-linked evidence; nullable historical links cannot authorize a keep. Charge owned files plus canonical retained evaluator/log records, regardless of optional derived exports.
- Keep `policy.ts` factual and bounded: no owner hypothesis selection. Main sequences only public start/resume/control, never the next research action. Worker-driven scored feedback is unavailable, reports/diagnostics remain unscored. Budget labels distinguish transactional invocation limits from active-time/owned-artifact admission and unavailable observational cost totals.

## PR7 branching and bounded waves

- `src/research/tree.ts` owns factual topology, eligible selection slots and exact min/max ranking, not hypothesis choice. Actor selections must include kind/slot/reason and the exact fallback; measured valid comparisons drive convergence in serial decision order, with failed checks/infrastructure excluded from no-gain.
- Keep whole-wave attempt/evaluator-credit reservation transactional. One or two fixed-parent candidates share one MaterialJourney boundary; concurrency one reuses a reserved native slot without another actor ask, while concurrency two awaits independent owner-held native operations. Measurements and shared Git collection/integration remain serial. Never borrow a sibling's unconsumed evaluator credits.
- Recheck controls, active/artifact limits, convergence and failures after awaited admission and immediately before each native launch. Release only proven unlaunched reservations. Preserve uncertain native handles, generation cleanup and immutable operational bindings; pruning is selection-only, not a stop result.
- Preparation errors release every proven-unlaunched reservation through the existing refusal path, retain partial workspaces and persist a blocked dispatch receipt. A missing native reply or persisted dispatch intent is not proof of non-launch. Preserve wave errors without looking up a nonexistent single attempt ID. Freeze and restore have independent error collection for proven-settled writers; never weaken protected-input checks. Pending review rejects expansion transactionally without rebasing the dialog; root hypotheses use their stable lineage root for exploitation and shifts.
- Append evidence-linked ancestor lessons under current transactional revisions. Check the existing100-insight capacity of every ancestor before any write; rejecting overflow must preserve every sibling lesson, receipt and revision. Reject stale sibling updates and duplicate wave identities before new reservation/effects. Revalidate every keep against the latest incumbent and newly evaluate changed combined material; old branch scores cannot validate it.
- Use `npm run test:pr7` and `npm run test:pr7:e2e` (timing and actual-owner pause/cancel files run serially), plus impacted prior lanes. `npm run audit:pr7` verifies locally retained exact gate evidence, manifest/package/import/read-only/fingerprint preservation and native event bounds. Keep A12's identical two-candidate reservation/collection boundary, one-second workloads, three warmed waves per mode and <=80% target. Retain environment, overhead and actual overlap; historical split-dispatch timing is not final evidence.
- Additive PR7 projection fields must remain optional on retained PR6 rows, including saved config origins and node insights. Never backfill an immutable saved spec merely to satisfy an output schema; validate present fields and keep new-run defaults bounded.
- `docs/pr7-parallel-evidence.md` and `acceptance-ledger.md` record observed implementation scope and independent-review status. No PR8 continuation/apply or later milestone acceptance follows from this gate.

## PR8 controls, recovery and source operations

- Use `npm run test:pr8` and `npm run test:pr8:e2e`, plus impacted prior lanes. `npm run audit:pr8` checks the exact local evidence inventory and unchanged package/import/fingerprint/read-only boundaries. Keep acceptance and native limitations in `docs/pr8-recovery-evidence.md` and the ledger; nine owner-loss block proofs are not nine successful resumptions.
- Resume must preserve original owner/spec/epoch/role/native attribution and return its exact reconciled revision, not rebase a stale caller after awaits. Preflight unknown evaluator handles before any material rebind; preserve `cleanup_pending` until all affected native/material boundaries are proven. Agent-risk control cannot commit pending Git integration; the command selects the execute-policy route.
- Journal exact frozen and retained partial artifact lineage before restore. Recovery must not freeze a reset parent as the original completed worker's result. Freeze refusal and restoration remain independent for proven-settled writers. Never use native private history, guessed IDs/PIDs, a new participant registry or synthetic recovered success.
- `resumeAttempt` is a new charged native-only invocation. `continue-partial` and `restart-parent` must retain distinct source OIDs, explicit same-hypothesis summary and append-only prior/root linkage. Never redispatch the original uncertain invocation, rewrite earlier provenance or treat a failure as a score.
- Pending material/research review after same-owner reload has an explicit inert resume boundary: verify saved role/material/native quiescence, renew only the pending generation/revision binding, retain the choice without approval/evaluation/dispatch, and require a fresh owning-Pi dialog before continued research. Old-generation dialogs and intervening revision changes still reject.
- Recover a fully written source operation through the existing apply ref using its original operation ID and a fresh current-owner command/UI receipt under normal Fabric write policy. Verify complete exact postimages; never replay preimages or mixed outcomes. Preserve original intent identity and approval provenance, append a separate bounded adoption receipt, and retain exact undo against the original apply. Material control/evaluate resume receipts must be atomic with the renewed pending-decision revision. Final-journal and final-research-receipt persistence gaps both require native reload/public-command regressions, including resumed research advancing the incumbent: adoption/undo admission and UI still bind the original recorded measured target, never requiring it to remain incumbent.
- Terminal source operations use `authorizeSource`, never research generation rebinding. Same-owner/component, immutable quiescent material, original apply intent, fresh write-policy/UI approval and exact postimages are mandatory. Source receipts retain the research generation/state; current command generation lives in the separate source journal receipt. Do not reopen cancelled research to make undo/adoption reachable. Test actual apply and both postwrite gaps -> cancel -> reload -> public recovery/undo, plus different-owner, policy and newer-edit denials.
- Continuation consumers must follow exact attempt/evaluation links, not the first attempt at a hypothesis. Discard resets only the latest reserved continuation with its native-digest guard, retaining the original. Continuation finalization after awaited collection must not overwrite cancelled/terminal, interrupted, cleanup-pending or retired state. Preserve the native cancel-during-collection regression.
- `SourceApply` writes durable per-path pre/postimage intent before effects, never source Git index/refs. Inspect parent entries with `lstat` during whole-set preflight; `existsSync` alone misses dangling symlinks and can permit earlier partial writes. Require exact current measured keep for new source writes, quiescence and a genuine owning-Pi UI receipt under separate Fabric write policy. Recheck revision/retirement after awaits, single-flight identical commands, and retain journals across storage/crash gaps. Unknown/mixed outcomes or newer affected bytes/modes block; undo is exact postimage verification, never a blind inverse. Preserve patches for read-only retrieval.

## PR9 held-out and final validation

- `src/evaluators/validation.ts` owns the frozen selected/final protocol and exact split linkage. `split`/`developmentId` remain separate from native execution, validity, quality and measured keep. Only development evidence and development-linked decisions may drive ordinary actor ranking/ideation/convergence; filter test-linked lessons and decisions as well as evaluation records. Do not leak held-out task/grade details into that context. Verify prerequisite development/held-out snapshot bytes and post-await revision before consuming the untouched final split, not only at keep/apply.
- Both integration transactions and new source apply require the selected validation gate. Re-evaluate changed combined material against the current incumbent; development scores, review flags, stale snapshots and native failures never override held-out/final vetoes. Source recovery/undo retains PR8's original immutable intent rather than reinterpreting a historical source operation as a new keep.
- `/arbor validate RUN ATTEMPT` is the explicit quiescent owner-only execute-policy final-selection route, excluded from actor proposals and read-only CLI/browser. Reserve one exact final pair only after capacity admission; interrupted use is still consumed. Exact replay/recovery may finish saved work, never select a second pair or reopen candidate search. Repeated held-out comparisons are capped adaptive reuse, not untouched final evidence.
- Count required checks with separately reserved IDs and durable native results alongside measurements, baseline/held-out/final, retries/rechecks and judges. Retain a sibling's held-out evaluator credits until its exact comparison settles. Persist completion before ingestion; unknown check/subject handles block without redispatch. Keep per-split configured long deadlines and public exact-owned short-deadline stops, admission-time active/artifact bounds and observational cost labels.
- Run `npm run test:pr9`, `npm run test:pr9:e2e`, `npm run audit:pr9` and normal `npm run check`, plus impacted prior native gates. The command budget fixture must count its two measurements plus two required checks; never weaken its zero-worker/one-baseline assertions. Preserve unchanged PR6 five-stage journeys, PR7 A12 boundary/workloads/overlap/<=80% assertion and PR8 source recovery. Record exact results in `docs/pr9-held-out-evidence.md` and the acceptance ledger; local gates do not imply independent review, publication or PR10-PR13 acceptance.

## PR10 grounding, experience and trajectories

- `src/research/Experience.ts` owns bounded lesson selection and compact proposal trajectory shapes. `ResearchStore` remains the only domain authority. Retrieve fresh-v2 same-project lessons without creating files, exports or DB state; selected lessons are hypotheses to retest, never grades. Preserve limitations, contrary findings and explainable duplicate provenance. Validate exact source-run/lesson/revision/digest references before committing a new hypothesis.
- Persist actual native proposals before effects in existing operation rows. Keep a pending trajectory distinct from a completed operation receipt; preserve trajectory data through generic/evaluation/integration/failure receipt writes. Capture each receipt's revision/incumbent atomically, never infer a delayed completion from the latest run. Finished trajectory replay is immutable. Retain exact development evidence/attempt/material/insight links; reject held-out/final links at ordinary actor and trajectory admission rather than silently dropping them.
- Derive recallable outcomes only from development-linked decisions. Validate accumulated provenance bounds within the distillation transaction before returning. Include mandatory lessons and trajectories in native artifact admission. Free-form source strings remain `uninspectedSourceRefs`, not inspected source IDs. Inspected IDs propagate from checked ancestor hypothesis references; they remain separate from experiment grades. No novelty subsystem or fallback runtime is implemented.
- `Grounding.ts` owns one selected bounded batch, not a search runtime. Read `arbor.sources.json` only before definition registration, freeze the selected catalog/committed bindings in the run, and invoke exact public search/fetch refs only through captured `context.call`. Missing tools block required grounding only. A component reload cannot reread configuration or widen prior availability. Reserve each call before dispatch, recheck revision/generation/retirement after awaits, and admit active/artifact budgets before effects.
- Only owner-captured fetched text and a native literature completion may produce source inspections. Preserve URL/title, accessed artifact/digest, exact passage, supported claim/limitations and source/run/material/spec/epoch/catalog/native/request provenance. Discovery snippets never become facts. Reject unvisited or changed artifacts and forged/cross-run/revision source refs at proposal admission. Required hypotheses need inspected references; source claims never substitute for experimental grades. Completed grounding is immutable; interrupted/reserved batches never automatically redispatch.
- Literature uses the same frozen `RoleBundle`/`OwnerExecution` adapter, explicit evidence procedure, exact model, `read` only and closed structured result. Package or same-path candidate changes cannot replace saved bootstrap; explicit role revision includes literature attribution. Preserve legacy four-file bundles for runs without literature. One public skill only, no private runtime, broker, novelty or fallback subsystem.
- Use `npm run test:pr10`, `npm run test:pr10:e2e`, normal checks and impacted prior native lanes. `docs/pr10-grounding-evidence.md` owns exact counts, independent findings/repairs and pending scope. Preserve PR6 five-stage journeys, PR7 A12 unchanged controlled-wave gate, PR8 recovery and PR9 held-out filtering. Do not infer PR11-PR13 acceptance or publication from subset gates.
## PR11 presets and example preparation

- `src/presets/` owns closed data contracts and bounded local preparation, not another research runtime. Ship `examples/manifest.json` and every declared code/agent/recipe asset in the source package. `examples/README.md` is the authoritative runnable/environment/upstream-template guide.
- Only owner-identity-checked write-risk `arbor.scaffold` may generate new pack directories. Preserve normal Pi/Fabric permission, reject existing destinations and unsafe paths, never override source/artifacts or initialize source Git. Recheck generation/cancellation after awaited filesystem work and before writes/return. Partial output stays retained and unvalidated, never cleaned automatically.
- Preset defaults precede profile/project/explicit settings; property-presence null disables inheritance. Keep immutable preset path/content identity separate from overrideable source refs, subject task/model/evaluator definitions and operational roles. Align source-reference/selected-file bounds across public and resolved contracts.
- Code uses three trials, median and 1% relative practical gain; ties never win. Agent tasks use declared paired repeats and descriptive analysis, not blind whole-suite replication. Held-out task content must be disjoint, not renamed development tasks. Grounding/held-out activation stays optional by saved run choice.
- Optional upstream preparation is only the maintained local command template with explicitly prepared bounded inputs and pinned declared provenance. Never acquire upstream coordinator/tree, data/services, paid models or a profile-local benchmarking skill. Declare project environments, never system Python pip. Scaffolds remain unvalidated until actual baseline evidence; no upstream benchmark validation claim.
- `npm run test:pr11` is included by normal research source tests. `npm run test:pr11:e2e` runs all three source and clean-installed packs through actual owner/coordinator/evaluator with fake inference only in native providers. Preserve unchanged PR6 journeys, PR7 A12 boundary/oracle, PR8 recovery and PR9/10 held-out/grounding gates. `npm run audit:pr11` checks public refs, scripts, assets and evidence; exact counts/review/publication belong to `docs/pr11-examples-evidence.md`. PR12-13 remain separate gates.

## Legacy v1 guidance

The rules below describe the existing v1 certificate/web-intent implementation only. They preserve untouched legacy code during the cut-over, but they do not govern the replacement v2 architecture or create v2 acceptance gates.

- Treat `ArborApplication` as the only legal v1 domain-transition owner. Stores persist atomically but do not invent transitions.
- Keep legacy public, evaluator, and browser schemas versioned, bounded, closed, and synchronized with tests.
- Use integer decimal arithmetic only. Never convert promotable values through `number`.
- Journal durable intent before any external operation and observe interrupted effects before retry.
- Reconcile legacy effects through `EffectRecoveryCoordinator` with exact revision/fence/identity bindings. Never turn `UNCERTAIN` into replay; never signal an unverified PID or descendant unit.
- Keep legacy detached Web code read/inbox-only. Its v1 intent and certificate behavior must not be carried into the strictly read-only v2 web or CLI.
- The private-repository, owner-TTY, certificate, containment, fingerprint, signed Phase 7, hashed `dist/web-assets`, and retained certification mandates are legacy-only. Do not reproduce them in v2.
