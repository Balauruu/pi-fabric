# PR11 presets, scaffolds and runnable packs

> Historical milestone evidence at or before PR12 `b509ae7ccb1db2da02272d9eb9d304267c59119f`, retained in the repository but not shipped. Commands, fixed counts and source paths below refer to that historical checkout, not current release gates. See [current cutover evidence](pr13-cutover-evidence.md) and the acceptance ledger. Existing runtime evidence remains untouched.

Status: independently reviewed deterministic local PR11 gate PASS. Publication identity is verified separately at commit/push time. PR12-13 remain pending; PR #3 stays draft.

## Starting point and authority

Exact root `/home/balauru/.pi-profiles/fabric/.worktrees/arbor-refactor`, clean branch `arbor/refactor-pr0-pr1`, full PR10 HEAD `c8abf2a20e4d72d495cb744ec6589909ee2f4f1c` verified equal to tracking and origin before edits. Root/app AGENTS, authoritative PR11 plus binding evaluation/material/preset/upstream provisions, latest PR10 evidence, relevant public consumer/source docs and Fabric provider/component lifecycle contracts were read. Explicit Fovea discovery reports unknown action; source tracing and subsequent Fovea drift messages provide navigation. Main is exclusive app/plan editor; all children are read-only.

## Acceptance checks and implementation

- **A04/A16 pack scope:** all three source-installed packs prepare through owner-only policy, then actual owner/coordinator baseline -> candidate -> independent measurement -> measured keep. Recipe material is non-Git with explicit selected files. Original sources, Git index/refs and unrelated fixture bytes remain unchanged. Fake inference supplies only native provider outputs, never owner/evaluator logic or grades.
- **Preset contracts:** public `presetSchema`, `packManifestSchema`, `readPackManifest`, `loadPreset` and `SCAFFOLD_SCHEMA`; packaged manifest/JSON/tasks/code. Defaults precede profile/project/explicit settings; null disables inherited preset loading. Independent saved `presetSource` path/digest survives overrideable source references; legacy saved specs remain accepted without rewriting. Frozen subject model/tasks/evaluator remain distinct from coordinator/executor/literature and mutable material.
- **Measurement:** code uses three trials, median, 1% relative practical gain and an actual unchanged second candidate that never wins. Agent uses one trajectory per paired task, independently graded labels, failure-inclusive veto and descriptive observational native usage/latency, not blind three-suite repeats or an unsupported seed claim. Recipe uses a fixed local synthetic dataset and independent command evaluation.
- **Optional research:** presets select optional grounding without requiring configured sources. Agent scaffold optionally activates two disjoint held-out positive/negative tasks; zero is development-only. Grounding can be off/optional/required through normal precedence. Held-out can be disabled. No held-out/generalization or scientific superiority claim is inferred from development gains.
- **Scaffold:** owner-identity checked write-risk `arbor.scaffold` and `/arbor scaffold`, normal Fabric permission, exclusive canonical new destination, no overwrite/source application. Request/environment and byte/executable preparation identities are retained. No subprocess, install, download, service launch or source Git initialization. Partial artifacts remain unvalidated and retained. Generation/cancellation is rechecked after awaited filesystem work and before subsequent writes/return.
- **Optional upstream:** small maintained local command template with an explicitly selected prepared file list, declared source URL/full revision and command/environment. It neither acquires nor adopts the upstream coordinator/tree. Copied inputs retain hashes/modes; revision authenticity is user-declared. This template is explicitly **unvalidated**, not an executed upstream benchmark. External datasets/services/Python/GPU environments are optional, user-provisioned and never downloaded or installed into system Python.
- **A22/A30:**21 exact public refs, unchanged10 native owner requirements, one public skill and packaged operational roles/references. CLI/browser remain read-only in every mode; scaffold/prepare/init/create/create-export/export deny. No private imports, replacement runtime, benchmark-skill copy/helper or new dependency/lock change. Source package includes every runnable asset.

## Red-to-green chronology

Logs are under `.runtime/pr11-gates/`; all native roots retain trace/stdout/stderr/exit artifacts.

1. `targeted-red.log`: expected missing scaffold module failure before implementation. `targeted-first.log`:6/6 and source typecheck pass. Source-only `native-source-first.log`:3/3 actual pack journeys pass.
2. `normal-first.log` stops on the expected explicit public action inventory delta after package/retained lanes pass. Main updates only the exact17->18 research /20->21 total inventory. `normal-second.log` passes313 tests including6 new cases. `native-pr11-first.log` passes8/8 source/installed packs and permission denials.
3. Independent reviewer `c684b7f016974a5f923c70fd5eb984be` completes read-only and finds three issues: post-parent-mkdir retirement permits writes, public source-ref/file bounds overflow downstream config, and renamed identical zero held-out data. `review-red.log` reproduces all3 failures (9/12 pass). Main adds post-await admission,29+3 source-ref and32-file bounds, and disjoint held-out content. `review-green.log`:12/12 pass with test typing. Saved preset provenance, source copy/concurrency/path checks, executable mode and CLI denial coverage are strengthened.
4. A test-only missing object brace stops the first `targeted-final.log` attempt before execution; repaired before the final successful gate. No native/product failure is attributed to that syntax issue. Main identifies generated explicit limit defaults shadowing configured budgets; `limits-red.log` reproduces400 instead of profile99 in all three packs. Optional `limitDefaults` now merges below profile/project/explicit, with generated start selecting only local paths/models/scope. Native tests explicitly choose only attempt count. All6 source/installed journeys and deny/ask cases pass again in `native-pr11-limits-final.log` + `.exit` (8/8, exit0).
5. Independent final-delta reviewer `a0cd890d13e74360a94c6295acead2ab` completes successfully, verifies earlier repairs and400 -> profile99 -> explicit70 precedence, and finds oversized serialized argv/check definitions could prepare successfully but fail the64KiB start loader. `definition-red.log` reproduces it. Main bounds serialized evaluation/preset/validation artifacts before destination creation. Final targeted14/14 and normal321 pass. `native-overflow-final.log` + `.exit`: actual clean-installed owner rejects an oversized prepared definition before any destination exists (1/1, exit0). This adds one distinct guard to the earlier8 native cases; it does not relabel them as a single nine-test invocation.

## Executed measurement evidence

Latest six entries by pack/install mode in `native-examples.jsonl` identify the exact roots, preparation identities, captured baseline/current incumbent OIDs, task summaries and charged invocation counts. Every host exits successfully and native completion is ingested before grading. No fixture driver sequences research operations: Main submits only scaffold/start; the real persistent coordinator chooses proposals/dispatch/evaluate/keep/distill.

| Pack, source and clean install | Observed baseline -> candidate | Interpretation |
| --- | --- | --- |
| Code |7910 ->405 input reads; three median trials; second identical candidate ties and is not kept | Deterministic operation proxy with correctness checks, not measured wall-clock speed |
| Agent |2/3 ->3/3 fixed development labels; no lost correct task; selected held-out non-regression passes | Paired descriptive native evaluation; no significance or broad model-quality claim |
| Recipe |2/4 ->4/4 synthetic labels with unchanged data/evaluator | Useful local recipe measurement, no transfer claim |

Latest `normal-final.log`:321 PASS = package5 + retained92 + managed20 + research71 + evaluator45 + material88; source/test no-emit typechecks pass. `targeted-final.log`:14/14 PASS. PR11 native evidence:8/8 plus installed overflow1/1, all exit0. Unchanged full native PR6/7/8/9/10 gates21/5/42/13/4 now pass. Final audit and independent repair closure are recorded below.

## Final deterministic gate closure

All logs are under `.runtime/pr11-gates/`:

| Gate | Observed result |
| --- | --- |
| `npm run check`, `normal-final.log` |321/321 PASS; both no-emit typechecks, unchanged92 retained/fingerprint tests |
| `npm run test:pr11`, `targeted-final.log` |14/14 PASS, including all independent review repairs and configured-limit precedence |
| Required native packs, `native-pr11-limits-final.log` + `.exit` |8/8 PASS, exit0: all three source and clean-installed packs plus write deny/ask |
| Final installed bound, `native-overflow-final.log` + `.exit` |1/1 PASS, exit0, actual owner rejects before destination creation |
| Full unchanged PR6, `native-pr6.log` + `.exit` |21/21 PASS, exit0; both five-stage journeys retained |
| Full unchanged PR8, `native-pr8.log` + `.exit` |42/42 PASS, exit0 |
| Full unchanged PR9, `native-pr9.log` + `.exit` |13/13 PASS, exit0 |
| Full unchanged PR10, `native-pr10.log` + `.exit` |4/4 PASS, exit0 |
| Isolated unchanged PR7, `native-pr7.log` + `.exit` |5/5 PASS, exit0; A12 without competing test lanes |
| `npm run audit:pr11`, `audit-first.log`, `audit-final.log` / `audit.json` |PASS:21 refs,10 requirements,one skill,39 reachable source modules,87 packed files,seven latest clean native roots, maximum six-pack trace event41359 bytes |

A12 keeps identical atomic two-candidate setup/dispatch/collection boundaries, fixed one-second worker workloads, one discarded warmup and three warmed waves per mode, positive parallel overlap and unchanged <=80% threshold. Current serial median3550ms, parallel2001ms, ratio0.5636619718309859. Nodev26.7.0, Linux7.2.2-1-cachyos, i7-13620H/16 logical CPUs, measurement concurrency1. Detailed overhead/overlap artifacts remain in `.runtime/pr7-gates/a12.json`. This is wave timing, not campaign speed or scientific quality.

Final independent read-only reviewer `bcf39f2b8313496db77dcc90098f9f6e` **completed successfully with no blocking findings**. It verified final serialized bounds/loader composition, earlier retirement/reference/file/held-out repairs, configured-limit precedence, public owner/read-only/package contracts and exact pack/guard evidence. It performed a final repair/publication-delta review, not another full-product review or test execution. At its read boundary, the last regressions/audit were pending; Main subsequently observed their natural passing completions above. Earlier reviewers both completed successfully and their concrete findings were reproduced/repaired, not dismissed.

The optional upstream template remains unvalidated. No external upstream benchmark, dataset, heavy service or paid research inference was executed. The source/installed native examples exercise real product ownership/FS/Git/evaluation with deterministic provider inference; they do not establish real-model research quality, containment or generalization.

## Publication boundary and limits

`npm run audit:pr11` mechanically verifies exact logs, refs/schemas/risks/config, imports/scripts/dependencies, source-package assets, successful native roots and unchanged retained/fingerprint/PR6/PR7/PR8/PR9/PR10 files. Older milestone audits pin obsolete counts or HEADs and are historical, not claimed current passes.

Independent repair closure, required native regressions and current audit PASS. Main completed the full bounded cached-diff review of all39 explicitly staged paths (3239 lines before this closure-only update), including every generated schema hunk and all new assets/source/tests. No blocking findings. Both `git diff --check` and `git diff --cached --check` pass; final audit and test typecheck pass. The closure-only documentation delta is reviewed before restaging. Normal commit/push and exact remote verification remain publication-time steps. Runtime/native artifacts, credentials, dependencies and lockfiles stay unstaged. No force/amend/rebase, paid research/datasets, installed/original-checkout mutation, blacklisted profile or PR12-13 work. Exact publication HEAD/tracking/remote equality and PR #3 comment URL are publication-time observations, never preclaimed.
