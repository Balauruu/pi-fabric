# PR9 held-out evidence

Status: independent Main review of the supplied PR9 diff and repaired scoped gates PASS. User-authorized publication requires full staged review and ordinary push/remote equality; PR #3 remains draft. PR10-PR13 are outstanding.

## Independent review and repair checkpoint

Verified exact root/branch/start HEAD `7a03483910d0d98e2ac1ee7feccbb295cf002482`; all supplied PR9 changes and artifacts retained. Main owns all edits. Fovea discovery returned no tools; its injected dependency map confirmed the service/store/workspace/owner/evaluator seams, then source reads established behavior. Acceptance checks: frozen split identity, current-incumbent and same-OID attribution, independent native grading/checks/deadlines, immutable single final use and recovery, adaptive capacity, review/permission separation, ordinary ideation filtering, read-only/public packaging, clean-installed native and retained regression gates. Publication is now user-authorized only after sound scoped gates and full staged review; prior no-authorization statements below are historical.

Red probes `review-red.{log,exit}`: **0/2**, actual service final selection consumed a comparison despite held-out snapshot byte drift; service-created held-out lessons/decisions leaked through the actor observation builder. Repairs add prerequisite snapshot verification plus post-await revision checking before final use, and exclude test-linked interpretations from ordinary observations while retaining owner-visible evidence. The adjacent test-only discard also advanced development no-gain and exposed held-out attribution: `review-convergence-red.{log,exit}` **0/1**, then repaired by filtering decision facts/order without dropping any invocation charges. The two original red product probes pass in `review-green.log`; the expanded tests below cover all three symptoms.

| Independent review gate | Observed result | Retained evidence under `.runtime/pr9-gates/` |
| --- | --- | --- |
| `npm run check` | **292/292**, both no-emit checks; 5 package + 92 unchanged retained + 20 managed + 46 research + 45 evaluator + 84 material | `review-normal.{log,exit}` |
| `npm run test:pr9` | **17/17**, original13 plus four adversarial cases | `review-target.{log,exit}` |
| `npm run test:pr9:e2e` | **13/13**, original12 plus actual clean-installed stale-final-admission regression | `review-pr9-native.{log,exit}` |
| Native repair-only probe | **1/1** after source/test typechecks | `review-repair-native.{log,exit}` |
| Native PR6 / PR7 full reruns | **21/21 / 5/5**; both unchanged five-stage journeys and unchanged A12 gate | `review-pr{6,7}-native.{log,exit}` |
| `npm run audit:pr9` | PASS; exact latest13 native roots, clean exits,19 actions,66 packed files, unchanged physical dependencies/lock/fingerprint/public skill/browser | `review-audit.{json,log,exit}` |

The audit also verifies prior **288/13/12** logs rather than relabeling them, and retained native PR3/4/5/8 **20/21/9/42**; those four lanes were not rerun during this independent review. The prior PR6 20/21 plus repaired1/1 composite remains historical; the new21/21 is a real full rerun. Latest A12 serial **3493ms**, parallel **1976ms**, ratio **0.5657028342399084**, all three warmed parallel waves overlap; identical one-second/two-candidate boundary and <=80% assertion preserved. Maximum latest PR9 trace **500699 bytes**, event **40650 bytes**. The audit is bound to the exact starting commit, not a moving `HEAD`, and requires complete non-skipped test summaries. Audit-only edits were independently test-typechecked after the product gates.

Adversarial acceptance additionally passes two genuinely settled equal-tree/same-OID candidate attribution, stale exact-review refusal, development-recheck-specific final evidence, second-final rejection with zero native calls, negative maximize/minimize held-out ties/losses through actual command/parser execution, nested policy aliases and deadline/use bounds. Existing exact-decimal/noise and changed-combined-material cases remain in the normal product selection. Native roots are the latest per-case entries in `native-cases.jsonl`/`native-evidence.jsonl`; source/native tests use the real service/store/workspace/evaluator/owner path, not an alternate acceptance implementation.

Phase 4 complete: independent source review, three reproduced symptoms repaired, expanded source/native/regression/audit gates PASS. No additional reviewer was delegated; Fovea's explicit action is unavailable, while its injected dependency maps were used as navigation evidence. Phase 5 publication is conditional on full cached-diff review, explicit-path staging, both whitespace checks and ordinary remote equality. No claim of PR10-PR13 completion, sealed data, inferential generalization or hard monetary enforcement follows.

### Final evidence-accounting repair and publication checkpoint

Whole-diff review also found that the native test evidence helper's `calls` field omitted separately reserved command checks, although product budgets and assertions already charged them. The strengthened artifact-linked audit reproduced **2 versus4** for the initial command evaluation in `review-count-audit-red.{log,exit}`. The helper now uses the product `evaluationCalls`, and the audit compares every reported count to its exact durable evaluation artifact. No scoring, budget or native runtime behavior was changed.

After this evidence-only repair: test typecheck PASS (`publication-test-typecheck.log`), **full native13/13** PASS (`publication-pr9-native.{log,exit}`) and exact audit PASS (`publication-audit.{log,exit}`, `review-audit.json`). Earlier review/native logs and all roots are retained. This is a complete final native rerun, not just the two command cases. Source292/target17 and native PR6/PR7 21/5 remain the prior passing review runs because product code is unchanged. Explicit staging includes only the29 reviewed app/plan paths; the generated action manifest is byte-identical to a full regeneration. Main full staged review PASS: the complete cached diff is retained in `review-cached.diff`, authored sections reviewed against tested working bytes, and all158 generated-schema changes are fully regenerated/semantically inventoried in `manifest-semantic-review.json`. `staged-review.json` binds every one of the29 exact indexed paths to its reviewed content; `review-name-status.txt`, `git diff --check` and `git diff --cached --check` pass. No runtime, dependency tree, credentials or temporary file is staged. Only ordinary commit/push and remote-equality verification remain publication effects.

## Historical supplied local gate and acceptance ledger

The remaining entries preserve the earlier implementation chronology. Their pending/no-publication statements and 288/13/12 counts describe that earlier checkpoint, superseded only by the observed independent review above.

All paths below are relative to the app. Logs and exact disposable owner roots remain local under `.runtime/`; no runtime artifacts are committed or deleted.

| Gate | Observed result | Retained evidence |
| --- | --- | --- |
| `npm run check` | 288/288; both no-emit typechecks pass | `.runtime/pr9-gates/verified-normal.{log,exit}` |
| `npm run test:pr9` | 13 source/contract cases, also included in normal selection | `.runtime/pr9-gates/verified-target.{log,exit}` |
| `npm run test:pr9:e2e` | 12/12 actual source/clean-installed native cases | `.runtime/pr9-gates/final-pr9-native.{log,exit}` |
| Native PR3/PR5/PR8 | 20/9/42 pass | `.runtime/pr9-gates/final-pr{3,5,8}-native.{log,exit}` |
| Native PR4/PR7 | 21/5 pass | `.runtime/pr9-gates/regression-pr{4,7}-native.{log,exit}` |
| Native PR6 | 20/21 full lane, then repaired budget 1/1; both five-stage journeys unchanged/pass | `.runtime/pr9-gates/regression-pr6-native.log`, `repaired-pr6-budget-native.{log,exit}` |
| `npm run audit:pr9` | exact gate/public/package/read-only preservation PASS | `.runtime/pr9-gates/verified-audit.{json,log,exit}` |

Audit verified 12 exact latest native roots with clean host exits, 19 public actions and 66 packed files; maximum trace 500699 bytes and event 40650 bytes. Dependencies and lock, owner native ABI/role sentinels, workspace/source journal implementation, public skill and browser files are unchanged. A12 serial median **3502ms**, parallel **1971ms**, ratio **0.5628212450028555**, all three warmed parallel waves overlap. The npm pack audit handles both supported array and object JSON output shapes. The final audit-only compatibility repair was typechecked separately; no product/native behavior changed.

The 12 native PR9 cases cover: clean-installed adaptive win/source dev-winner held-out loser; clean-installed final-only win/source final loser with no automatic final use and exact replay; command held-out win/required-check veto; real owning-Pi UI approval still cannot keep/apply a held-out loser; actual >30000ms selected held-out subject with 180000ms frozen deadline despite a 30000ms host default; 10ms deadline with exact public owned stop; separately charged malformed judge/retry veto; same-owner reload after durable native completion before ingestion; unknown-handle reload blocks without duplicate launch. Exact roots, split/evaluation IDs and counts are in `native-evidence.jsonl` and `native-cases.jsonl`; host `exit.json`, `trace.jsonl`, `stdout.log`, `stderr.log` and `rpc-ui.json` retain native proof. The final authoritative entries are the latest entry per case, not aggregate counts across reruns.

Source probes additionally cover changed combined material remeasured on both splits, held-out snapshot drift, frozen policy-file independence, adaptive cap across rechecks, held-out wave-credit ownership, ordinary ideation filtering, public export/execute route and final exclusion from actor proposals. Final admission refuses an unaffordable untouched comparison before consumption. A separate selected-policy final loser vetoes promotion; no-validation keeps remain development-only. CLI rejects `validate` in ordinary/attached/offline modes; browser assets gain no mutation route.

PR6's sole repair is accounting, not weakened acceptance: the baseline performs two measurements and two checks, so the saved budget is 4 instead of 2 while still requiring zero attempts and one evaluation. Standalone PR4 rechecks remain available after a completed exact-pair run; terminal material research remains closed. PR8 native 42 cases were rerun after adding new-apply evidence verification, preserving historical postwrite adoption/undo.

**Limits:** local deterministic fake inference only, not a benchmark or transfer result. Adaptive held-out reuse is not pristine final generalization; trusted local files are not sealed/secret. Active/artifact bounds apply at admission and may be exceeded by in-flight work. Required checks have separate reserved IDs/native results; unresolved launches retain artifacts and block. Usage/cost is observational, not a hard monetary cap. No new dependency, Fabric API/private runtime, broker, sandbox, dataset, benchmarking skill, original-checkout or blacklisted-profile operation. Native PR2 was not rerun in PR9; its owner implementation and role/worker sentinels are unchanged and normal managed tests pass. Independent review remains pending.

Phase 3 complete: final expanded targeted **13/13**, complete normal **288/288**, native **12/12** and audit PASS with exact above paths. Public `validationSchema` export and `validate` denial in ordinary/attached/offline CLI are directly tested. All implementation repairs are complete for the observed PR9 scope; independent review is the remaining non-implementation status. No commit.

## Verified entry

Exact worktree `/home/balauru/.pi-profiles/fabric/.worktrees/arbor-refactor`, branch `arbor/refactor-pr0-pr1`, clean HEAD and local tracking ref `7a03483910d0d98e2ac1ee7feccbb295cf002482`. Physical app-local `node_modules`, Pi and Fabric verified, no installed edits. PR8 evidence read; prior counts remain historical, not reruns. Root/app guidance and complete authoritative plan read in bounded ranges. Fovea discovery reports unknown action, so targeted source tracing is used honestly.

## Acceptance ledger and phase log

- A14: frozen development/held-out/final task and grading policy, separate exact evidence; selected dev winner/held-out loser veto; no-evidence dev-only labels; bounded adaptive reuse and one final untouched comparison.
- A05/A06/A08: exact current incumbent, native validity, checks, decimal/noise oracle and changed combined-material remeasurement remain mandatory before review/keep/apply.
- A20: real source/clean installed Pi/Fabric owner/evaluator/research journeys, independent deterministic grades/judges, native completion before ingestion, explicit same-owner recovery, unknown handles block without duplicate launch.
- A21: baseline/held-out/retry/recheck/judge IDs and invocation limits; admission-time active/artifact bounds, observational cost, public exact-owned stop on short deadlines and configured long agent deadlines.
- Public contracts: closed schema/manifest/preset default/epoch identity, owner-only final selection, read-only CLI/browser in every mode, no private APIs or extra runtime.
- Regression: normal source suite, required PR9 native lane and impacted PR4/5/6/7/8 native lanes, preserving PR6 two journeys, A12, PR8 source recovery and role sentinels.
- Documentation: exact commands/counts/proof paths below only after execution; README/AGENTS/plan/ledger updated; independent review pending.

Phase 2 (resumed): preserved supplied six modified files and three new files. Current red reproduced in `.runtime/pr9-gates/resume-red-source.log` (0 held-out records instead of 2). Wired split evaluation, transactional promotion veto, capture binding, development-only observation/command selectors and exact label projection. `typecheck` PASS; targeted win/lose source 2/2 PASS (`targeted-source.log`); actual clean-installed win/source loser native 2/2 PASS (`native-first.log`, exact roots in `native-evidence.jsonl`). Expanded source/contract 10/10 PASS (`targeted-expanded.log`) and actual native 8/8 PASS (`native-expanded.log`, roots in `native-cases.jsonl`): explicit final win/lose once; command held-out win/check veto; native completion-before-ingestion reload recovery; unknown-handle reload blocks with zero duplicate launches. Check invocations now have separately reserved IDs and count against the saved limit. Held-out wave credits remain owned until exact split settlement. Native deadline/judge additions 3/3 PASS (`native-deadlines-judges.log`): selected 180000ms held-out completed an actual >30000ms subject despite host 30000ms default; selected 10ms uses public exact-owned stop and invalidates scores; malformed native judge + retry remains vetoed, all 22 calls charged. First normal check exposed overbroad completed-state rejection for standalone PR4 rechecks; repaired without changing its tests. PR4 source 45/45 and material source 77/77 PASS (`material-regression-first.log` retains material output). Impacted native PR4/5/7/8 PASS **21/9/5/42**, all serial, exit files retained as `regression-prN-native.{log,exit}`. PR7 A12 unchanged one-second/two-candidate/three-warm-wave <=80% assertion PASS. PR6 20/21 passed, including both unchanged five-stage journeys; the old evaluator-budget fixture allowed 2 measurements but omitted its 2 required checks. Updated only that budget from 2 to 4, preserving zero attempts/one evaluation assertions; red log retained. Final native PR9/3/5/8 **12/20/9/42** PASS and repaired PR6 budget **1/1** PASS. A type-only `keep` narrowing error in the new source-apply revalidation guard was repaired; normal **288/288** PASS in `normal-check-repaired.log`. No independent acceptance upgrade.

Phase 1: traced `resolveSpec -> ResearchService -> MaterialJourney -> EvaluationEngine -> OwnerExecution`, `ResearchStore.prepareIntegration/completeIntegration`, `researchObservation`, command review/apply selectors. Installed public Pi/Fabric docs read completely in bounded calls; existing exact public native requirements retained. This is the preserved initial checkpoint; its next-step instruction was completed in Phase 2.
