# Acceptance ledger

## PR13 hard cutover (in progress)

PR12 published at `b509ae7ccb1db2da02272d9eb9d304267c59119f`, with exact origin equality and clean checkout verified. The spec supervisor authorized immediate PR13. A18/A19/A22/A30 checks and retained-contract ports are in [cutover evidence](docs/pr13-cutover-evidence.md). The old runtime/test bridge and obsolete launchers/docs are deletion targets; existing runtime/user artifacts are not. No PR13 acceptance is claimed before mechanical, fresh-install and native gates pass.

Milestone records below are historical; old fixed counts and audit commands are not current cutover requirements.

## PR12 final implementation and reconciliation review (accepted; publication authorized)

The user explicitly approved revising **A02/A22/A26** and addressing F2–F5. One configured `arbor` application permits one operational `arbor.owner` plus non-operational sibling `arbor.drain`. The guard requires/acquires only `arbor_lifetime.lease`; the owner retains its exact ten native requirements, original `context.call` and signal. The discoverable capability has closed empty args, null output, agent risk, scoped/ordered effect, no command mapping and no actor commitment. Complete metadata belongs in `docs/pr3-action-manifest.json`.

Revision acceptance checks:
- **F2:** generation-local acquired/unreleased lease plus active guard gates every public mutation. Existing post-await monotone retirement checks and internal exact-owned cleanup remain. Cover waiting/failed guard, diversion, replacement and stale generation.
- **F3:** admission retirement -> real scoped drain settlement -> native owner unload/abort -> eventual `arbor.close()` storage release. Cover partial/landed activation, retained reads and cleanup failures, without context mutation or private imports.
- **F4:** mechanically verify both provisions, guard requirements, schemas/caller/risk/effect, and generated manifest. Ordinary refs stay21; actor requires only `agents.self`.
- **F5:** rerun source/clean-install held create/ask/spawn/wait/stop and actual application/whole-Pi reload cases. Keep exact coverage limits: internal-owner reload returned `Unknown Fabric component: arbor.owner`, not a proven lifecycle outcome. No universal dependency/provider-replacement guarantee. Catalog maintenance remains quiescent.

**Final local gates PASS:** normal **391/391** with both typechecks, targeted **41/41**, full source/clean-installed native/browser PR12 **35/35**, and prior-native PR2–PR11 **8/20/21/9/21/5/42/13/4/9** (152 total). Both `audit:pr12:lifetime` and `audit:pr12` pass against retained current exit-checked logs and clean-installed source/asset identity. A12 preserves its workload, overlap and 80% oracle, measuring **56.83%**. Reconciliation repaired the real already-v2 metadata restamp, retained all ownership/evidence/budget assertions, distinguished new resume intents from exact idempotent replay, and strengthened byte/inventory/no-effect proof. The full audit mechanically preserves prior executable assertions rather than requiring obsolete fixture construction to remain byte-identical. Exact commands, roots, assertions and limits are in [the final current gate](docs/pr12-presentation-evidence.md#final-current-gate). Independent scoped lifetime and final reconciliation reviews **PASS**, with no required fixes. The latter independently checked current fixture/module hashes, every final exit/count, exact replay observations and bounded claims; it did not rerun tests or conduct a fresh full-application audit. Review evidence is retained at `.runtime/pr12-design/final-reconciliation-review.md`. The spec supervisor accepts required A09/A10/A11/A17/A22/A26–A30 evidence within the documented scoped limits and authorizes PR12 commit/publication. PR13 is dependency-ready, with code deletion authorized separately from runtime/user-artifact cleanup. Published PR12 at `b509ae7ccb1db2da02272d9eb9d304267c59119f`; ordinary push and exact origin equality verified before PR13 began.

## Historical PR12 checkpoint before scoped revision (blocked)

**A17 strict cold-read repair verified:** source and clean-installed storage tests pass **8/8 each**, including the unchanged external journal-switch byte/inventory assertion. Cold readers use disposable read-only EXCLUSIVE admission; writers and journal setup are unchanged. Both typechecks and normal checks pass **373/373**. Actual source/installed Pi/CLI/browser journeys pass **2/2**. Exact commands and artifacts are in [the current PR12 checkpoint](docs/pr12-presentation-evidence.md#current-implementation-checkpoint-fail-closed-cold-readers).

**Historical A09/A11/A27/A29 blocker:** the unchanged held-spawn reload cases fail **0/2** on the declared runtime, retaining active1 instead of0. A disposable exploration of the latest published Fabric also fails **0/1** with the same assertion; no host upgrade or Fabric-internal change was made. Authoritative launch delivery and native cleanup settlement remain an external prerequisite. The strict milestone audit still stops at its historical checkpoint and prior full native lanes remain required. HEAD remains `823f5490e86179391505710ff66e7bd580e8aa92`; PR12 stays unstaged/uncommitted/unpublished and PR13 gated.

[PR12 evidence](docs/pr12-presentation-evidence.md) owns the current check ledger. Started from clean full `823f5490e86179391505710ff66e7bd580e8aa92` on `arbor/refactor-pr0-pr1` in the exact refactor worktree. Main remains the sole application/plan editor.

Implemented the shared Pi/CLI/browser projection, no-ID owning-Pi controls and bounded exports. Retained review repairs include two ordinary agent/execute admissions, bounded background refusal, fresh held-ask steering, complete-inventory ambiguity and retired/stale presentation guards. Earlier 36/37 targeted and 21/23 native results are historical, not the current storage or normal-check status. The current checkpoint separates repaired A17 from unresolved native settlement. No PR12 acceptance, staging, commit, publication or PR13 deletion is claimed.

## PR11 presets, scaffold and runnable packs

[PR11 evidence](docs/pr11-examples-evidence.md) owns exact gates, independent findings/repairs and publication status. Started from clean full PR10 `c8abf2a20e4d72d495cb744ec6589909ee2f4f1c`, verified against tracking/origin; Main exclusively edits app/plan paths. Independent final repair review, full required regressions and current audit PASS; exact publication identity remains a separate observation.

- **A04 PR11 deterministic scope PASS:** code and non-Git recipe packs now run source/clean-installed owner/coordinator baseline -> candidate -> independent measurement -> measured keep. Sources/index/refs remain unchanged. Code7910 ->405 deterministic reads, recipe2/4 ->4/4 synthetic labels. These are useful local measurements, not wall-clock/generalization claims. Earlier non-Git material/dirty-source scopes are retained.
- **A16 PR11 pack/preparation scope PASS:** all three packaged runnable packs, including native agent2/3 ->3/3 task labels with selected held-out non-regression. Closed preset/manifest/source contracts, preserved settings precedence and independent frozen preset identity. Code three trials/median/1% relative/ties-never-win; agent paired repeats/descriptive quality and observational usage/latency. Optional grounding and selected held-out activation. The maintained upstream local command adapter pins explicitly prepared bytes/modes and declared source revision but remains unvalidated, not an executed upstream benchmark. Earlier PR4 finite-provider discovery/schema/precedence/execution scopes remain preserved, not replaced by pack tests.
- **A22 PR11 command/contract scope PASS:** owner-only write-risk `arbor.scaffold` and `/arbor scaffold`, exact21-action manifest and unchanged10 native requirements. Actor commitments exclude Arbor mutation refs; CLI/browser deny preparation/init/create/scaffold/export-generation in every mode. One unchanged public skill; operational roles and references remain packaged, not separately registered. Broader PR12 presentation/public-skill scope remains outstanding.
- **A30 PR11 installation scope PASS:** source and clean installed package includes manifests, presets, task data and runnable code/recipe scripts through public source exports. No new dependencies/lock changes, profile benchmarking skill/helpers, private Fabric imports or replacement runtime. Broader PR12 views/PR13 deletion remain outstanding.
- **Final observed gates:** normal321 (5+92+20+71+45+88), targeted14, six actual source/installed packs plus permission deny/ask8 and final installed serialization guard1. Full unchanged native PR6/7/8/9/10 pass21/5/42/13/4. Isolated A12 retains its boundary/one-second workloads/three warmed waves and <=80% oracle: serial3550ms, parallel2001ms, ratio0.5636619718309859 with positive parallel overlap. Audit PASS:21 refs,10 requirements,one skill,39 reachable source modules,87 packed files,seven clean latest native roots. Final read-only reviewer `bcf39f2b8313496db77dcc90098f9f6e` completes successfully with no blockers after all reproduced repairs. See the evidence closure for exact scope, limits and publication boundary.

PR #3 remains draft. PR12-13 are not started or accepted by this checkpoint. Prior milestone sections below retain historical scopes/counts.

## PR10 delayed review repairs (reopened)

The earlier A15/A25 acceptance below is historical and reopened by the delayed review. The five repairs are narrowly verified in [PR10 evidence](docs/pr10-grounding-evidence.md#delayed-review-repair-checkpoint): source29/29, native PR10 4/4 with strengthened source/installed grounding2/2, impacted PR9 native13/13, evaluator45/45, material93/93 and both typechecks pass. Actual completed-native ingestion-crash/reload replay adds zero search/fetch/spawn, and ten negative binding cases per host remain blocked. Final read-only review found no blockers. The whitespace finding is corrected: owner trim already existed, while native schema constraints were missing.

Normal remains red solely on strict PR12 journal admission: groups5/92/20/116 pass5/92/20/115, then the chain stops. Both unchanged held-spawn reload probes still fail active1 versus0. PR12 R2/R3 are not repaired or accepted. All original untracked hashes and user-added tracked lines are preserved, fingerprint unchanged, with all mixed changes unstaged and no commit/publication.

### Historical independently reviewed deterministic local gate

[PR10 phase evidence](docs/pr10-grounding-evidence.md) records implementation, exact test logs and two independent read-only review/repair passes. The start baseline was clean `2cfc98c559bd357d8b75e8b44008f333ea0f5859`; Main exclusively edits app/plan paths.

- **A15 tested PR10 scope PASS:** bounded same-project fresh-v2 lesson retrieval, leaf/direction distillation provenance, negative/contrary observations and exact recalled-hypothesis references; actual native proposal/context-reference/action/outcome exports with existing CLI artifact retrieval have source and clean-installed execution evidence. No adopted grade comes from recall. Finite optional source catalog, owner-visited passage/native inspection records and checked grounded source-ID admission now have source/installed native evidence. Normal307, targeted15 and native PR10 four source/installed cases pass, preserving prior native PR6/7/8/9 gates21/5/42/13. Uninspected metadata is never inspection evidence; project recall remains a lightweight fresh-v2 index, not another authority.
- **A25 tested PR10 literature portion PASS:** native literature role/tools/model/result, actual artifact reads, evidence-phase bootstrap, frozen owner reload/resume and same-path candidate separation now pass source/installed lanes; package-change reconstruction is source-tested. Successful independent read-only full review found no blocking issue after the direction-source repair. Earlier PR4/PR6 operational/subject separation is preserved, not expanded by experience tests.
- **Reviews:** first four findings reproduced/repaired; second review verified those repairs and found delayed first-completion attribution and omitted mixed/test trajectory links. These were reproduced and repaired by atomic receipt snapshots and rejecting held-out/final ordinary actor evidence. Counts and final reruns belong to the evidence document. A subsequent full reviewer found dropped descendant grounding on direction lessons; Main reproduced/repaired it. Successful final full reviewer `04d5d60dbae443ddb4da68cb14fc8e57` found no blocking issue. This is deterministic local acceptance, not scientific endorsement or containment.
- **Publication:** successful final documentation/audit/assertion delta reviewer `0d217717781945a1ba6aef2c3706fc12` reports no blocking findings. Main must complete bounded whole cached-diff review and ordinary remote verification; exact publication identity and results are recorded in PR #3's PR10 evidence comment. PR #3 stays draft; PR11-PR13 remain outstanding. Prior milestone sections below are historical scoped evidence, not current PR10 gates.

## PR9 held-out validation: independently reviewed scoped gate

**Independent Main review and repaired scoped gates PASS.** Verified exact root/branch/start HEAD `7a03483910d0d98e2ac1ee7feccbb295cf002482`; all24 supplied modified and five untracked PR9 paths are preserved. No secondary reviewer was delegated. User-authorized publication requires full staged review and ordinary push/remote equality; PR #3 stays draft. [PR9 evidence](docs/pr9-held-out-evidence.md) owns exact commands, roots and red-to-green chronology.

**Executed independent review gates:** normal no-emit/typecheck + **292/292** source tests (5 package + unchanged92 retained +20 managed +46 research +45 evaluator +84 material); targeted PR9 **17/17**; actual source/clean-installed native **13/13**. Three symptoms red then green: drifted prerequisite snapshots consuming untouched final evidence, test-linked lessons/decisions leaking into ordinary ideation, and test-only discards driving development convergence. Additional same-OID/stale-review/final-reuse/minmax/alias/bound probes pass. Full native PR6/PR7 reruns **21/5** preserve both five-stage journeys and A12's identical one-second/two-candidate/three-warmed-wave <=80% assertion: serial3493ms, parallel1976ms, ratio0.5657028342399084 with all parallel waves overlapping. Prior native PR3/4/5/8 **20/21/9/42** and original normal/target/native **288/13/12** are mechanically audited historical evidence, not review reruns. Prior PR6 composite20/21 plus repaired1/1 remains historical; the budget fixture still allows exactly its two measurements plus two checks and asserts zero attempts/one baseline. Audit PASS:13 latest clean native roots,19 actions,66 packed files, maximum trace500699/event40650 bytes. Logs are `review-{normal,target,pr9-native,pr6-native,pr7-native,audit}` under `.runtime/pr9-gates/`; original artifacts are retained.

**Scope:** A14 and the PR9 exact-pair/current-incumbent/native-validity/recovery/accounting portions of A05/A06/A08/A20/A21 have local deterministic evidence. Development/held-out/final policies and task/grading deadlines are frozen; adaptive uses are counted/capped, final selection is explicit once against one exact pair, and native/review/dev-score veto bypasses reject. New source apply revalidates exact kept evidence; PR8 historical source adoption/undo still binds its original intent. No held-out means development-only, never transfer. Costs remain observational; time/artifacts are admission bounds, trusted workers are not sealed or contained. Original source/index/refs, physical app lock dependencies, public Fabric API, one public skill and read-only CLI/browser boundaries are preserved. Independent acceptance is limited to the reviewed PR9 gates; no broader generalization or PR10-PR13 completion is inferred.

Prior sections below retain their historical milestone scope and counts.

## PR8 controls, recovery and source apply: observed scoped gate

**PR8 scoped gates PASS.** Repair continued at the exact requested root/branch from `f1c816c5e54218066b99cf45fafce80b18afad11`, preserving all33 supplied staged paths and adding only `src/research/tree.ts`. Final independent guard review and Main whole staged-diff review PASS; publication guard rerun23/23 and exact audit PASS. User-authorized publication requires ordinary push/remote equality. PR #3 stays draft; PR9-PR13 remain unstarted. [PR8 evidence](docs/pr8-recovery-evidence.md) is authoritative for exact logs, retained roots and limitations.

**Resolved:** cancel -> reload -> original source undo/adoption now uses a separate same-owner terminal-source authorization path under normal public write policy, without renewing the research generation or reopening terminal research. Actual-native completed apply and both postwrite persistence gaps pass; another intrinsic owner, Fabric write deny/ask, new terminal apply, newer affected bytes/modes and terminal research resume still reject. Original approval/intent/spec/epoch/prepost target and native histories remain bound. Further independent-review repairs cover dangling-parent whole-set preflight, exact continuation refinement/discard selection, and cancel-during-continuation-collection terminal monotonicity. Each has red/green evidence; both continuation journeys also run natively.

**Final executed gates:** both no-emit checks and normal **275/275** (5 package + unchanged92 retained +20 managed +42 research +45 evaluator +71 material); targeted PR8 **23/23**; full PR8 native **42/42**, no skips. `verified-final-{normal,target,pr8-native}.log` naturally end with complete totals and exit0. Impacted native PR3/5/6/7 reruns **20/9/21/5** are in `final-pr{3,5,6,7}-native.log`; PR2/4 **8/21** are retained prior repair gates, not relabeled reruns. Final continuation-only cancellation guard is covered by the last normal/fullPR8 run. Older incomplete exit0 and output-overflow39/40 logs remain failed/non-completion evidence, not accepted totals. Full-record comparison before output compaction preserves the2-MiB host cap and exit guards.

**Audit PASS:**19 refs /10 owner requirements /one unchanged public skill /64 packed files /31 reachable modules /180 static imports. Inclusive inventory **639 clean +6 synthetic PR2 guard failures +65 SIGKILLs +1 identified failed output-overflow checkpoint =711 exits**. Event maximum56,000 bytes, trace maximum1,369,835 bytes. Latest A12 serial3506 ms, parallel1981 ms, ratio **0.565031374786081 <=0.8**, all three warmed parallel waves overlap. Identical atomic two-candidate boundary and fixed one-second workloads; not campaign speed or research quality.

**Scope:** tested A09/A10 and PR8 portions of A11/A24/A27/A28/A29, preserving prior accepted scopes. All nine actual killed/reopened hosts prove **owner-loss BLOCK**, not portable resume; five original same-owner recovery cases include exact ref adoption across component generation replacement. Pending research/material reviews rebind inertly before fresh UI; source recovery binds its original measured target even after a newer incumbent. Different native owners, unknown handles, uncertain cleanup and mixed/newer source effects remain blocked/retained. No arbitrary-descendant containment, free retry, worker-scored feedback, monetary ceiling or later milestone acceptance. Dependencies/lock, native ABI/config, original fingerprint, retained selection, CLI/browser read-only boundaries and public skill remain unchanged. Runtime artifacts stay local and untracked.

## PR7 repaired scoped implementation gate

**PASS for repaired observed scope; initial four-repair review passed; final restaged review required.** Exact root `/home/balauru/.pi-profiles/fabric/.worktrees/arbor-refactor`, branch `arbor/refactor-pr0-pr1`, base HEAD `e8abded64db245ebfca2725bdeca486094e0c1a4` verified; existing diff preserved. User authorizes reviewed app/plan commit/push and exact-evidence comment on draft PR #3, subject to full staged review and HEAD/tracking/remote equality. [PR7 evidence](docs/pr7-parallel-evidence.md) records all current and historical checkpoints.

**Repairs:** preparation failure releases only proven-unlaunched reservations, retains partial workspaces/error and blocks duplicate receipt replay; freeze refusal independently attempts settled-writer restore and retains both errors; pending review transactionally blocks expansion without rebasing; root hypotheses/refinements share stable exploitation/shift lineage. Adjacent admission failures require absence of persisted dispatch intent before release; wave errors no longer mask their cause with an undefined single-attempt lookup. Lost replies remain cleanup-pending without replay.

**Gates:** seven exact-symptom probes red then green; targeted **67/67**; final normal **252/252** (5 + unchanged 92 + 20 + 42 + 45 + 48), both no-emit checks; native PR2/3/4/5/6 **8/20/21/9/21**; final native PR7 **5/5** (A12 1, actual pause/cancel 2, preparation/protected committed-worker failure 2). No skips/exclusions. Initial static review found no four-repair/parity blockers. Whole staged review then found retained-PR6 output compatibility; a red/green reopen/projection regression fixed additive field requiredness without rewriting saved identities. Full schema-impacted native PR3/PR6 reruns pass **20/21**; those43 exits are included. The next whole review found shared-ancestor insight overflow; the 101st lesson now rejects transactionally against the unchanged100 bound, with red/green sibling regression and another full PR6 native **21/21**, adding21 exits. Audit **18 refs / 10 requirements / one unchanged public skill / 62 packed files / 30 modules / 174 imports / 163 exact two-window exits**: **160 clean + three expected synthetic PR2 exit-guard failures**, including all eight earlier PR7 checkpoint hosts, not treating red test assertions as passes. Max event **56,000 bytes**, max trace **1,368,686 bytes**. Evidence prefixes `.runtime/pr7-gates/repair-` `compat-` and `insight-`.

**Final A12:** identical two-candidate atomic waves, one discarded warmup then exactly three measured one-second waves per mode. Concurrency one median **3524 ms**, concurrency two **1962 ms**, ratio **0.5567536889897844 <= 0.8**, overlap **988/960/984 ms**. Environment/roots/setup/dispatch/collection retained in `a12.json`. Reviewer-verified historical 1959/3497 and 972/962/980 overlaps remain in `a12-reviewed.json`. Actor/evaluator turns outside the execution-wave boundary are excluded; controlled-wave evidence only, not campaign-wide speed or research quality.

**Scope:** tested A12/A13 only, preserving read-only/public/native/role/evaluator/fingerprint/retained-test boundaries and both PR6 five-stage native journeys. Broader A09/A10/A11/A15/A21 and product acceptance remain partial. PR8-13 are not started here. Budgets remain admission-based, costs observational/unavailable, diagnostics unscored and worker-scored feedback unavailable. No dependency/lock, Fabric API/private-import/new-runtime, original checkout or blacklisted profile changes; retained artifacts remain local and untracked.


## Historical PR6 independently accepted scoped gate

[Exact independent evidence and repair ledger](docs/pr6-research-evidence.md): two actual actor-led autonomous code/instruction journeys, each baseline -> gain -> valid no-gain -> failed check -> further gain. Five evaluations and 10/30 invocations, four workers/lessons each; exact incumbent/evaluation/native attribution, operational/subject separation, installed role reload/revision, unchanged dirty source/index/refs and real owning-host RPC review preserved.

All five supplied fixes independently verified. Adjacent red/green repairs close late provider admission after descriptor awaits and unlinked legacy keep eligibility, including the owning-Pi command selector. The timestamp-dependent equal-OID fixture was repaired without changing production Git sanitation or weakening exact identity assertions.

**Accepted gates:** `npm run check` both no-emit checks and **227/227** (5 + unchanged 92 + 20 + 31 + 45 + 34); targeted regressions **7/7**; native PR2/3/4/5/6 **8/20/21/9/21**, no skips/exclusions. Audit **18 refs / 10 requirements / one unchanged public skill/registration / 60 packed files / 29 modules / 169 imports / 85 selected fresh exits**, comprising 82 clean plus three expected synthetic PR2 exit-guard failures. Exact two-window selection and failed intermediate checks are retained in evidence, not relabeled as passing.

**Scope:** A03/A04 deterministic journey portions; tested A23/A24/A25 role/collision/quiescent resume; serial within-run A13/A15/A21 lessons/stops/accounting. Broader acceptance remains partial. No PR7-PR13 work is accepted or started by this gate: branching/parallel, partial/ambiguous-worker continuation/apply, held-out, literature/cross-run learning, example packs, rich presentation/public-skill rewrite and legacy deletion remain pending. Budgets are admission limits, not containment; costs observational/unavailable; worker-scored feedback unexposed. Read-only CLI/browser, retained useful tests/fingerprint and public-API-only architecture remain intact. No Fabric, dependency/lock or protected-profile changes.

**Publication scope:** user-authorized reviewed-path-only PR6 commit/push and exact-evidence comment on draft PR #3. Final cached diff and remote equality checks remain mandatory. PR7 needs separate authorization; keep the PR draft.

## Historical PR6 five-finding reviewer repair checkpoint

Exact requested worktree/branch and HEAD `da20cd3c97445b101154a99e9657ac2273bc2378` preserved, including the existing uncommitted PR6 diff. No staging, commit, publication or PR7. [PR6 evidence](docs/pr6-research-evidence.md) records the concrete red/green ledger, final logs and native inventory.

All five findings are repaired: unresolved material/evaluator state survives every actor outcome; successful unchanged-binding research pause permits fresh actual RPC review while rejecting stale/forged inputs; cumulative native-effect admission includes post-baseline actor creation, every invocation and command check, owned files plus retained evaluator/log records; exact attempt/evaluation/compared-incumbent identity prevents equal-OID inheritance and historical outcome rewriting; authorized generation lineage admits original failed evidence without rewriting provenance. A real failed-worker reload also required distinguishing terminal operation failure from retirement/storage failure.

**Final executed gates:** normal **225/225** (5 + unchanged 92 + 20 + 31 + 44 + 33), both no-emit checks; native PR2/3/4/5/6 **8/20/21/9/21**, no skips/exclusions. Audit: **18 refs / 10 requirements / one unchanged public skill / 60 packed files / 29 modules / 169 imports / 85 exact fresh exits** (82 clean plus three expected synthetic exit-guard failures). Both original native five-stage journeys and role/fingerprint/read-only/exit assertions remain intact. The first normal run exposed timestamp-dependent equal-OID test metadata; a pinned isolated fixture and full normal rerun pass. Historical failing logs remain retained.

**Scope/limits:** the same serial PR6 journey and operational/lesson/stop scopes below, plus these five repairs. Budgets remain admission-based, not containment; in-flight work may overshoot, subsequent effects refuse. Shared DB page/WAL overhead and Fabric runtime storage are excluded; canonical retained evaluator/log records are charged even without a derived JSON export. Exact closed output schemas add nullable evaluation `attemptId` and optional bounded run `generationHistory`; action/input/risk/requirement surfaces are unchanged. Partial/ambiguous-worker recovery and source apply remain PR8, parallel branching remains PR7, cost aggregates remain observational/unavailable and worker-scored feedback remains unexposed.

**Next:** final Main verification only. Repair gates are not publication authorization.

## Historical PR6 pre-review implementation gate

Continued from exact HEAD `da20cd3c97445b101154a99e9657ac2273bc2378` in the requested worktree, preserving the prior uncommitted foundation. No commit/staging/push; PR7 not started. [PR6 evidence](docs/pr6-research-evidence.md) is the authoritative current status, exact artifact inventory and repair history.

Explicit `research` now enters through execute-risk `arbor.runResearch`. A valid baseline precedes one persistent proposal actor; production owner operations perform actual candidate dispatch/wait/freeze/evaluation/decision/distillation. Main does not choose subsequent operations. Code and clean-installed instruction journeys each complete **baseline -> gain -> valid no-gain -> failed check -> further gain**, using the correct current incumbent. They retain five evaluations and **10/30 accounted native invocations**, four workers/lessons apiece, unchanged original source/index/refs and no live owned participants at settlement. Native instructions/result sentinels and same-path operational-versus-subject loading are asserted, not inferred from discovery.

**Final gates:** `npm run check` passes both no-emit checks and **220/220** source tests (5 + unchanged 92 + 20 + 31 + 44 + 28). Native PR2/PR3/PR4/PR5/PR6 pass **8/20/21/9/16**, no skips. Audit passes **18 exact public refs, unchanged 10 owner requirements, one unchanged public skill/registration, 60 packed files, 29 active modules / 165 imports and 80 selected fresh exit records** (77 clean plus three expected synthetic exit-guard failures). Dependency/lock metadata, retained-source selection and fingerprint source/tests remain unchanged.

PR6 additionally proves native execute deny/ask for command and agent-suite research, invalid baseline blocking all actor/worker creation, no-gain/target/attempt/evaluator/active/artifact/repeated-native-failure stops, missing-phase refusal and actual installed reload/resume with both frozen roles and explicit quiescent role revision. Revised bootstrap reaches new workers while prior invocation provenance and immutable measurement/spec remain unchanged. Source regressions cover stale/forged role controls and receipt replay. Known failed workers remain unscored; ambiguous evaluator/native state is not hidden by actor cleanup.

**Scope:** A03's deterministic independently graded instruction journey and A04's code journey now have actual end-to-end evidence. Operational A23/A24/A25 and within-run lesson/stop/accounting portions of A13/A15/A21 are exercised in this serial mode. Broader A04 example packs, PR7 branching/parallel, PR8 partial/ambiguous-worker recovery/apply, held-out, literature/cross-run learning and rich presentation remain outstanding. No broad product acceptance is inferred from this gate. Active time/artifacts are admission limits; trusted writers can overshoot between boundaries. Cost/token telemetry is observational with unavailable aggregates. Worker-driven scored feedback is not exposed; diagnostics/reports never become scores.

The final run caught and repaired strict test shape errors, the new PR3 projection descriptor expectation and PR4's duplicate-schema environment-size failure (which caused an actual 420-second suite timeout). Exact assertions were preserved using deduplicated schema payloads; repaired full normal/PR3/PR4 lanes passed. Passing PR2/PR5/PR6 evidence was retained rather than rerun. Earlier actor-name, fixture-text/result and resume-receipt-ID failures remain in the evidence record.

**Next:** independent full-diff review only. No known repair remains from these executed gates; no commit or dependent PR7 work is authorized.

## Historical PR6 role-foundation checkpoint - full research was then pending

Starting branch/HEAD and remote PR5 `da20cd3c97445b101154a99e9657ac2273bc2378` were verified before edits. No commit/staging/push. The complete continuation checkpoint is [docs/pr6-research-evidence.md](docs/pr6-research-evidence.md); PR7 remains gated.

New inspect/material runs freeze package-resolved coordinator/executor and conditional strategy/evidence assets outside candidate material. Actual native create/ask/spawn requests use the saved bundle and persist append-only role/reference/request/native attribution. Missing or corrupt bootstrap blocks material reservation; required evidence procedures block resume before evaluator reconciliation. An installed package-change -> real component reload -> completed-baseline material resume retains the original worker bootstrap. A new run using incompatible changed package assets blocks. This extends the role foundation, **not the autonomous scored coordinator loop**.

Final `npm run check` passes both no-emit checks and **216/216** tests (5 + unchanged 92 + 20 + 28 + 44 + 27). Actual native PR2/PR3/PR4/PR5 pass **8/20/21/9**, with original assertions retained. The ninth PR5-lane case is the PR6 installed role/reload proof, not a second research driver. Audit verifies **64 distinct fresh exit records**, 16 public refs, 10 exact owner requirements, one unchanged public skill/registration, 59 packed files and 28 active modules / 157 static imports. Dependencies, lockfile, retained-source selection and minimal fingerprint source/tests are unchanged.

The first broad native PR3 run caught a new duplicate-dispatch regression. Role validation awaited after the final duplicate-receipt check; two native handles launched for one attempt. The existing test reproduced it in isolation. All awaits now precede the final check/transactional reservation; native identity guards were not weakened. The unchanged regression and complete final native suite pass. Red and green evidence remains under `.runtime/pr6-gates/`.

**Acceptance remains partial:** A23 has native bootstrap/phase/tools and admission-refusal evidence; A24 has preserved bundle and installed reload/material-resume evidence, but explicit owning-Pi role revision and stopped-coordinator resume are pending. A25's actual same-path subject/optimizer separation is strengthened in the existing native lane, not accepted throughout autonomous research. A03/A04's two five-stage autonomous journeys, within-run research policy/distillation and full A21 budgets remain pending. No overall PARTIAL item is upgraded to PASS. Prior PR4/PR5 material/evaluator/read-only/lifecycle scopes are preserved, not enlarged by counts alone.


## V2 deep-refactor checkpoint - PR5 independent scoped verification PASS

PR5 implements source-preserving dirty/non-Git capture, owned candidate worktrees, settled-writer freeze/restore, exact production evaluator loading, current-incumbent acceptance, descriptive noise policy, transactional integration intent/Git CAS and baseline-relative export through the existing managed owner/public Fabric adapter. `execution: material` is explicitly selected bounded work, **not PR6 role assembly or autonomous scored research**.

**Independent final gates:** both no-emit checks and **212/212** normal tests: 5 package/install + unchanged 92 retained source + 20 PR2 + 25 PR3 + 44 PR4 + **26 PR5**. Native PR2/PR3/PR4/PR5 pass **8/20/21/8**, without skips, after the adjacent repair. The audit verifies **63 distinct final host-exit records**: PR2 8 actual successes plus one synthetic success and three expected failing exit controls, PR3 22 successes, PR4 21 successes, PR5 8 successes. It also verifies **16 public refs, 10 owner requirements, one public skill, 57 packed files, 27 active modules / 148 static imports**. Dependencies, lockfile, retained test selection and minimal fingerprint source/test are unchanged. Exact evidence is retained locally in `.runtime/pr5-independent-gate/`, not committed.

**Independent findings:** all six supplied repairs verified: full tree-relative symlink escape/cycle refusal; unrounded BigInt task/repeat gain; unresolved evaluator cancellation retained as `cleanup_pending`; completed-baseline pause/resume with review/execute-policy guards; `S`/`s` skip-worktree refusal; actual attempts before baseline sentinels. The claimed 210 normal and 8/20/21/8 native counts were independently reproduced before adding two adjacent direct cases. Review then reproduced and repaired material control-state masking: terminal cancellation could report resume success/reopen through pause, and pause could hide unresolved cleanup. Direct and native agent/command/lost-reply assertions now prevent both; stale bindings and pending-CAS controls still refuse without mutation or redispatch. Red/green evidence and the corrected bounded-return/exit-selector harness failures remain retained.

**Accepted scope:** A07 passes for this bounded source-preserving journey. A05/A06/A08 pass their PR5 exact-validity/current-incumbent/owned rollback and changed-integration remeasurement portions. A04 non-Git capture, A10 durable intent/CAS/same-owner replay and A20/A21 owned snapshot evaluation have bounded evidence. A03/A04/A05/A06/A08/A09/A10/A11/A17/A20/A21/A22/A25/A28/A29/A30 remain PARTIAL overall where autonomous research, complete interactions/recovery, held-out/role work or later presentation are required. Preserved PR2-PR4/package/read-only scopes are not enlarged by fixture counts.

**Limits:** trusted worktrees are not confinement; arbitrary descendants/unknown handles may remain unresolved, with artifacts retained. Captures are bounded to 4096 files/16 MiB and UTF-8 filenames. Unsupported sparse/submodule/merge/special files and escaping/cyclic/non-UTF-8-target evaluation symlinks refuse. Missing native deadline evidence remains invalid and needs an explicitly charged fresh baseline. Pending integration after generation replacement is blocked; full PR8 recovery, source apply/undo and partial-worker continuation remain unavailable. Source review oracles and the retained native PR3 RPC gate are not complete interaction-mode acceptance.

See [docs/pr5-material-evidence.md](docs/pr5-material-evidence.md) for commands, per-finding proofs, exact exit inventory reconstruction and retained failures. **Independent review accepts only scoped PR5 publication; PR6-PR13 remain outstanding and PR #3 stays draft.** Historical implementation and six-finding repair evidence remain in `.runtime/pr5-gates/` and `.runtime/pr5-review-repair/`; earlier checkpoints below are superseded only within this scope.

## V2 deep-refactor checkpoint - PR4 independent scoped verification PASS

PR4 implements packaged command, Arbor-owned native agent-suite and finite configured-provider evaluation over an **explicit pair of committed material references**. Definitions/tasks/grades, snapshot/request identities, independent subject/judge roles and invocation purposes are frozen. Native completion is persisted before ingestion; explicit same-owner reconstruction reconciles known terminal handles without redispatch, while unknown/ambiguous handles block. Command effects retain execute-policy admission. CLI/browser and packaged examples remain read-only/data-only.

**Executed gates:** `npm run check` passes both no-emit checks and **186/186** tests (5 package/install + 92 retained-source + 20 PR2 + 25 PR3 + 44 PR4). Native gates pass **8/8 PR2**, **20/20 PR3**, and **21/21 PR4**, with **21 verified natural clean PR4 host exits**. Final public-contract/pack audit passes: **16 refs, 10 required owner refs, 51 packed files, 22 active modules / 115 static imports**. Exactly one public skill remains registered. Original fingerprint implementation/assertions, retained source selection, lock metadata and useful deferred review assertions are unchanged. Dependencies are app-local physical installs, with no profile-skill or private Fabric dependency.

**Review repair:** all five supplied findings have red regressions and passing product-path checks: judge infrastructure interruption/reconciliation, detached immutable trust bindings with per-await mutation detection, exact subdirectory blob selection, committed executable preservation/drift and unbounded internal derived BigInt deltas. Native poisoning tests wrap the public managed context, never replace the owner/evaluator driver. PR2/PR3 native gates were rerun after these changes. Follow-up independent review additionally reproduced and repaired omission of committed `__proto__` files, adding exact filename/persistence/coverage/tamper regression coverage. Whole staged-diff review also reproduced and repaired explicit null incorrectly inheriting a lower-precedence preset, with source and installed native coverage. All gates were independently rerun after both fixes. Current logs/audit use `.runtime/pr4-independent-final-*`; the audit records exact host paths, not a latest-directory heuristic. The action manifest regenerated byte-identically; no unsupported blocker assertion was weakened.

**Acceptance scope:** A20 and the PR4 portions of A16/A21 pass for exact-pair evaluation, terminal reconciliation, the finite optional catalog/preset contracts and invocation accounting. A03/A04/A05/A06/A09/A10/A11/A16/A17/A21/A22/A25/A29/A30 remain PARTIAL overall wherever they require full research journeys, incumbent adoption, held-out work, operational role assembly, partial-material recovery or later views. The new native role-path collision test separately proves actual subject snapshot loading and unchanged operational actor/executor instructions/tools. Existing bounded A01/A02/A26 and reviewed PR3 scopes remain preserved; other later acceptance items receive no blanket pass.

**Limits:** no PR5 dirty capture, automatic candidate generation/current-incumbent acceptance/source apply, PR6 autonomous research, or PR8 broader partial-material continuation. Analysis is paired descriptive evidence, not an inferential confidence claim; unsupported selected methods block. Usage/cost is observational, not a hard ceiling. Unknown/live native effects lacking terminal evidence remain blocked. The unknown-ID fault uses a deliberately corrupted journal ID and the actual public status lookup, not an invented handle-invalidation API. Shared-owner provider replacement can interrupt built-ins too; explicit maintenance and immutable-bound reconciliation are required. Schema enforce remains unavailable, not accepted compatibility.

Exact evidence, commands, red/green phase logs, native exit paths, architecture and item-by-item boundaries are in [docs/pr4-evaluator-evidence.md](docs/pr4-evaluator-evidence.md). **Independent review authorizes only this scoped PR4 checkpoint after whole staged-diff review. PR5-PR13 remain outstanding; PR5 has not started.** Earlier checkpoints below are historical and superseded only within this bounded PR4 scope.

## V2 deep-refactor checkpoint - PR3 final adversarial verification PASS

The scoped transactional interface and four prior independent-review repairs were independently verified. Native actor review survives successful quiescent settlement; intervening controls and old dialogs remain stale. Approved-direction dispatch, immutable evidence/export provenance and terminal-before-attach monotonicity pass. Final review found one additional stale-approval defect: renewed review/rejection retained an old direction approval. It now revokes prior admission on request and persists the actual response; old receipt replay cannot restore admission. Both guarded modes, reopen and real RPC approve/reject regressions pass after retained red runs.

**Final executed gates:** `npm run check` passes both no-emit checks and **142/142** tests (5 package/install + 92 retained-source + 20 PR2 + 25 PR3). `npm run test:pr3:e2e` passes **20/20**, with **22 clean exits (16 print, 6 RPC)**. Original exit guards, useful deferred review and fingerprint assertions remain intact. Source/manifest/pack audit passes: **16 public refs, 10 owner requirements, 38 assets, 11 active modules / 58 imports**. Lock metadata and retained source selection are unchanged.

**Scope remains partial:** no scored research, candidate snapshots, evaluator/apply implementation, PR8 native resume, or PR4+ production acceptance. Schema enforce remains unavailable; its exact host failure and zero-side-effect command probe are limitation tests, not compatibility. No Fabric runtime/API change. PR2 native was inspected, not rerun: final repairs affect only PR3 research-store admission, not PR2 execution/exit; earlier PR3 diagnostic route changes already had the historical 8/8 native gate. All 20 PR2 source tests passed again.

Exact commands, red/green logs, native traces and boundaries are in [docs/pr3-interface-evidence.md](docs/pr3-interface-evidence.md). Final verification authorizes the PR3 milestone checkpoint. PR4 is next; PR4-PR13 remain outstanding. Earlier checkpoints below are historical and superseded only within this bounded scope.

## V2 deep-refactor checkpoint - PR2 managed execution substrate

The production `src/managed/` owner now replaces the active source facade's PR1-only limitation. Registration remains passive. Setup/doctor, exact definition-time public dependencies, captured post-activation calls, native owning root/host/identity binding, proposal-only persistent actor, bounded worker dispatch/owned waits and retained provider storage are implemented. CLI/browser remain read-only and there is no legacy admission/certificate dependency.

**Scoped PR2 gates PASS:** normal `npm run check` passes both no-emit checks and 5 package/install + 92 retained-source + 20 managed tests (117 total); `npm run test:pr2:e2e` passes 8 tests (seven real Pi/Fabric host scenarios plus the subprocess exit-guard regression), with explicit successful print-host exit checks. The source-only package contains 31 assets including the new owner modules. Tests use app-local dependencies, isolated profiles and deterministic local inference, not paid models or a replacement execution driver.

**Acceptance boundaries:** A02 and A26 pass for the bounded PR2 execution substrate. Broader A11/A29 remain PARTIAL. Real cancellation settles held create/ask/spawn, while an unreturned create revoked during retirement is honestly `cleanup_pending`: its idle native actor remains until host exit, with dispatch provenance retained and no automatic redispatch. Retained storage/close faults and genuinely missing requirement diagnostics include explicitly labeled lifecycle-seam evidence. Disabled-agent doctor/setup and second-native-root journal-preserving denial are real-host evidence. Full stopped-actor replacement, partial resume/reconciliation and the broader lease/resource fault matrix remain later work.

A01 and the previously accepted read-only/package portions of A17/A22/A30 remain preserved; this does not accept scoring, complete product journeys, operational role assembly or later research views. Current actor commitments exclude Arbor mutations; workers are extension-free so those refs are absent, not merely exposed behind an auth guard.

Exact commands, retained trace locations, source links and limitations are in [docs/pr2-managed-owner-evidence.md](docs/pr2-managed-owner-evidence.md). PR3 is next; PR3-PR13 remain outstanding. The checkpoint records below are historical evidence, superseded only within this stated PR2 scope.

## V2 deep-refactor checkpoint - revised owner-local PR0 gate PASS

The 2026-09-05 generic owner-forwarding/host-callback blocker is **SUPERSEDED** by the approved owner-local architecture. On 2026-09-06, the revised source-loaded Pi/Fabric lane passed 11 host/seam tests on Pi 0.85.1 and Fabric 0.83.0. In addition to the earlier actor/worker ordering, cleanup, reload, role, evaluator, and interrupted-ingestion coverage, the lane now rejects every explicit contradictory or unknown `routed`/`local` discriminator for actor and agent stop results; authorizes the native run binding before entering cancellation drain; durably links actual returned actor/worker IDs to the native owner and snapshot/evaluation provenance; and proves owner-loss safety by exiting the original disposable Pi host, attempting recovery from a second native root, observing the recorded public handle absent, and blocking without redispatch or journal change.

**PR0 passes.** This is a PR0 falsification-gate verdict, not production completion and not acceptance of any complete A01-A30 item by PR0 alone. Same-host public-handle invalidation is unavailable in this harness and remains unverified; the executed proof is owner-host loss with second-root denial. Remote stop remains out of scope. Exact observed and unverified boundaries and commands are recorded in [docs/pr0-owner-local-evidence.md](docs/pr0-owner-local-evidence.md). `npm run test:pr0:plan` is text-only and makes no behavioral acceptance claim.

## V2 deep-refactor checkpoint - repaired PR1 source-only package PASS

The PR1 review repair replaced the missing emitted fingerprint-oracle digest path with the digest exported by the actually loaded oracle source. The original direct independent-oracle implementation is preserved without adding subprocess or IPC machinery; the declared source loader resolves its module, and 100 dirty-checkout equality trials, mutation quarantine, signatures, and digest assertions remain intact. Legacy release Web component characterization now derives its hashed bundle in memory from source assets rather than requiring emitted Web files.

Normal `npm test` and `npm run check` run 5/5 package/install tests plus the named 92/92 `test:source:retained` lane. The retained lane covers all model arithmetic/state/schema, Git fingerprint/workspace/promotion, persistence/artifacts, dispatch/outbox/report/cleanup/fault recovery, report completeness, command concurrency, evaluator parsing, fixture flow, and component/provider characterization directly from TypeScript source. Both no-emit checks pass. The clean install still packs 25 source/document assets, observes reload, one public skill, read-only CLI/assets, no research side effect, and no emitted fallback.

Every clean-install subprocess strips inherited `PI_*` values. The Pi fixture receives only isolated `PI_CODING_AGENT_DIR`, `PI_OFFLINE`, and `PI_SKIP_VERSION_CHECK` settings, so shared session, mesh, actor, owner, provider, model, and configuration identities cannot leak into it. Superseded v1 certificate/admission/containment/writable-Web suites are precisely listed as historical in [docs/pr1-source-install-evidence.md](docs/pr1-source-install-evidence.md); they are neither active gates nor claimed passes.

The 11-test PR0 host lane was not rerun: this repair did not touch its source, fixture, or integration tests, and the touched legacy fingerprint/Web paths are outside its execution path. Exact commands, counts, boundaries, and that coverage choice are recorded in the evidence document.

**Acceptance status:** A01 PASS for the repaired source extension/CLI/tests/assets/reload and normal retained-source lanes. A30 is PARTIAL: independent package assets and declared dependency resolution pass; PR12 research-view reuse remains pending. The PR1 portions of A17 and A22 prove only present read-only surfaces and one-skill packaging, not the later shared projection, operations, or complete journeys. At the PR1 checkpoint, PR2 was next and no PR2+ production behavior was accepted; the scoped PR2 checkpoint above supersedes that historical status.

The records below describe the legacy v1 Phase 7 implementation only. They are not evidence for any other A01-A30 v2 acceptance item.

Updated 2026-09-03 after the Phase 7 implementation and retained acceptance run. Claims apply only to the exact certified package, host, platform, tools, sources, and content-addressed evidence. Any verifier drift invalidates the claim.

> **Historical 2026-09-03 remediation state: ADMITTED for that exact host and source.** Separate complete B0/B1 matrices certified `pi-fabric@0.76.2 || pi-fabric@0.77.0`. On that exact `0.77.0` payload, all B0-B12 and Phase 4-7 artifacts verified, the signed Phase 7 verdict had `passed: true` with unresolved predicates `[]`, the final distribution inventory verified, and both `productionCertified` and `realAgentsEnabled` were true. Subsequent source-only work changed certified inputs, so this is no longer current admission and is not a v2 gate.

## Production gates B0-B12

| Gate | Status | Mechanical evidence |
|---|---|---|
| B0 upstream | PASS for exact `0.76.2 || 0.77.0` | Separate bounded payload, package-lock provenance, export, notice, and certifier-input inventories under `certification/upstream/pi-fabric/{0.76.2,0.77.0}/` |
| B1 runtime compatibility | PASS for exact `0.76.2 || 0.77.0` | Per-release direct six-action agent lifecycle/cancellation evidence, five approval modes, installed Fabric integration, and compatibility `supported: true` |
| B2 domain/schema | PASS | Closed bounded catalogs, Phase 7 public schemas, exact decimal arithmetic, state-machine and compatibility tests |
| B3 workspace/evaluation | PASS for retained host | Exact-OID dissociated private repository, isolated worktrees, strict finalization, confined evaluation |
| B4 persistence/recovery | PASS | WAL/replay/snapshot/outbox/recovery projections and migration 5 Phase 7 stores |
| B5 containment | PASS for retained Linux/Bubblewrap | Signed 38-observation direct matrix covering all 34 required names, pre-exec identity/cgroup handshake, and hard wall/output/process-tree/RSS termination |
| B6 non-interference | PASS for retained Git/platform | 100 signed primary/oracle fingerprint trials and 68 Phase 7 boundary certificates |
| B7 authorization | PASS for retained local policy | Owner-UID, owner-only Ed25519 keys, bounded TTY, expiry/revocation/one-use negative matrix |
| B8 held-out isolation | PASS | Worker denial, evaluator-only capability, sealed Unix service and exact candidate/input/policy receipts |
| B9 Web/security | PASS | Loopback-only intent inbox, Host/Origin/CSRF/schema/rate/body/SSE/artifact bounds, release UI and independent review |
| B10 licensing | PASS for retained active `0.77.0` lock/package | 267 reviewed packages, license/notice closure, zero unresolved obligations |
| B11 thresholds/distribution | PASS | Signed pre-execution policy seal and mechanically verified final 579-file dry-run npm inventory |
| B12 retention | PASS | Explicit outcome durations, holds, deletion rules, complete reports and protected retained evidence |

## Phase exits

| Phase | Exit status | Evidence |
|---|---|---|
| P0-P3 | PASS for declared retained scope | Domain, schemas, package boundary, B0/B1, Git, containment, evaluator and fingerprint artifacts |
| P4 | PASS | 19 boundaries x 20 deterministic crash injections, exact fence/identity observation, zero duplicate accepted effects |
| P5 | PASS | B7/B8, detached same-policy evaluation, exact winner-ref CAS, rollback and re-promotion certificates |
| P6 | PASS | Release-built detached Web UI, complete reports, cleanup/retention, Web/approval/license/browser/distribution artifacts |
| P7 | PASS | Signed seal, platform admission, 70 mandatory E2E steps, benchmark, soak, hard resources, browser UX/accessibility, independent reviews and signed graduation verdict |

## Mandatory E2E 1-35

Both `maximize` and `minimize` executions contain all numbered steps. Every step retains one nonempty, bounded, closed, step-discriminated concrete evidence receipt whose `evidenceDigest` is the canonical digest of that receipt. Steps 2-35 cross-bind the exact signed fingerprint certificate, effect, command, fence, containment, correlations, previous digest, before/after repository digests, and report generation; step 1 retains exact initial OID, dirty tracked/untracked, stash, sibling, user-ref, and bounded command/log facts.

| Steps | Retained observation |
|---|---|
| 1-4 | Presealed policy, dirty source/oracle fingerprint, content-addressed contract, exact-OID private Git import |
| 5-7 | Source/sibling write denial, complete direct containment matrix, canonical development plus sealed held-out baselines |
| 8-10 | Three reserved hypotheses/attempts, concurrent valid/protected/interrupted processes, exact digest-bound interrupted-workspace readiness before cancellation, completed-child crash gap |
| 11-15 | Disconnected durable Web intent, SSE catch-up, stale-fence rejection, completed-child recovery without rerun, retry with fresh identities |
| 16-20 | Exact candidate OID, direction/boundary/spread/tie arithmetic, detached merges, same-policy held-out comparison, correct trust labels |
| 21-27 | Web intent-only promotion, local authorization, independent Fabric approval evidence, exact CAS, crash observation, rollback, fresh re-promotion |
| 28-31 | Frozen report plan, partial-file/rename recovery, complete published report, manifest-only idempotent cleanup |
| 32-35 | Restart parity, guarded boundary coverage, independent oracle equality, deliberate mutation quarantine |

Retained acceptance IDs and digests:

- `acceptance_phase7_maximize_v1`: `896eeac0fee8cfe32e90804b70a688346726cf120c6db7b11faeb9edda42b893`
- `acceptance_phase7_minimize_v1`: `9c2fb34c4ef8dddf7a486d1ad66ed7ba2a5888a7e6886fe8c99e2656f479c24a`
- Total: 70/70 passed steps, 70 retained concrete receipt variants, 68 valid fingerprint certificates, 36 bounded command/log receipts, 6 distinct authorizations, 6 distinct Fabric write-policy approvals, 6 exact winner-ref CAS observations, and 22 complete report-file receipts.

## Phase 7 sealed results

| Predicate | Sealed threshold | Observed | Result |
|---|---:|---:|---|
| Normalized benchmark delta | >= 5, both directions | maximize 12; minimize 12 | PASS |
| Recovery success | 10,000 basis points | 10,000 | PASS |
| Duplicate effects | 0 | 0 | PASS |
| Soak | >= 10,000 cycles and >= 30,000 ms | 10,000 cycles; 30,000 ms | PASS |
| Step latency | p95 <= 750 ms; p99 <= 5,000 ms | p95 658 ms; p99 3,070 ms | PASS |
| Evaluator process | <= 10,000 ms, 65,536 output bytes, 16 processes, 536,870,912 RSS bytes | Hard cgroup-v2 enforcement and measured receipts; no acceptance breach | PASS |
| Agent aggregate | <= 3 concurrent, 4,096 tokens/agent, 12,288 tokens total, USD 1/agent, USD 3 total | Atomic pre-reservation and metered settlement; no acceptance breach | PASS |
| Accessibility | >= 8 checks, 3 viewports, no critical/serious findings | 11 boolean predicates, four contrast samples, named-control inventory, 3 viewports, no findings | PASS |
| Usability | >= 2 journeys, 100% success, median <= 120,000 ms | 2 journeys, 100% success | PASS |
| Security | >= 50 direct checks, zero critical/high | 71 checks, zero critical/high; npm audit 0 vulnerabilities | PASS |
| Licensing | zero unresolved obligations | 0 | PASS |

Artifact digests:

- Threshold seal `seal_phase7_graduation_v1`: `c1ca4aa826d37c87810ac828faf75e984f49b7f08b7354f4f3ba1d475772790d`
- Supported platform: `989be94c40ba6e67a43523dd028487f3692ac3d2fd1dde9b3661ba927ae7ebc1`
- Benchmark: `50d0b616adf4d321131497f0dc873c1dd95e85281a793ce859c7807a6e25b8a8`
- Soak: `5b76ef4b853ca6186b657ab736bab00a465e9e2ef25bc3b31387d3e1eedcd5bd`
- Browser evidence file: `962beb82c297621c76bfacd0af56e32648f51f6ce19f858d3c6ef6ad45a02e6d`
- Independent reviews: security `89ae7ec9b738f48ea58ec75f251007ee1309d6ef86cdd7391a9836905f9748b5`; accessibility `fae2004c5949ffabe7973476122352c2dc8d03ba1a2b8084a61fd5c62e6c3a77`; license `9f82463e0e91425f16cc09500952f505338144ad31449836df8a431d0e2deaeb`
- Graduation `phase7_graduation_v1`: `6686edae59dbfd7d03b68db2904350bb6b3a92b7f0e26c35e5afbeca09f3ae2a`, `passed: true`, unresolved predicates `[]`.

The complete hash-chained 10,000-cycle JSONL log, screenshots, review inputs, acceptance certificates, checksums and signatures are retained under `certification/phase7/`. Remote Web, resident mode and user-ref publication remain disabled.

## Final verification evidence

- `npm test`: PASS, 179 tests, 0 failures, including both retained release matrices, packaged-entrypoint admission, all 35 closed Phase 7 variants, readiness-digest tampering, all 34 consequential fingerprint mutations, containment fast exits, private-Git candidate verification, recovery, Web, promotion, report, cleanup, retention, and distribution checks.
- `npm run test:browser:phase7`: PASS, 33 ordinary-control route checks, 114 assertions, three screenshots/viewports, 11 boolean accessibility predicates plus structured contrast/control evidence, two journeys at 10,000 basis points, and no unexpected browser, HTTP, external-network, console, or leakage findings.
- `npm run verify:phase7`: PASS on active `pi-fabric@0.77.0`, certificate `6686edae59dbfd7d03b68db2904350bb6b3a92b7f0e26c35e5afbeca09f3ae2a`, no errors.
- `npm run lint:package-boundary`: PASS, no violations.
- `npm run verify:certificates`: PASS for both exact B0/B1 release matrices and every retained containment, fingerprint, recovery, authorization, held-out, promotion, Web, approval, licensing, retention, Phase 7, and distribution artifact.
- `npm run certify:distribution` and distribution verification: PASS with 579 certified inventory files and all required bins, exports, assets, Phase 0-7 evidence, runbooks, license/notice files, and prohibited-file checks.
- Direct startup aggregation on the current exact `pi-fabric@0.77.0` host reports `productionCertified: true`, `realAgentsEnabled: true`, and blockers `[]`. An unlisted future version or any digest drift remains blocked.

Production remains fail-closed. Use the opaque `admission` returned by `loadGraduatedProductionStatusV1()` or `prepareGraduatedProductionProviderV1()`. The package is not admitted if any active source, built/shipped byte, adapter identity, configuration, dependency, tool, platform, prior certificate, threshold, artifact, review, checksum, or signature binding drifts.
