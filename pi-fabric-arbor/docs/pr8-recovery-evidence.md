# PR8 controls, recovery and source-apply evidence

> Historical milestone evidence at or before PR12 `b509ae7ccb1db2da02272d9eb9d304267c59119f`, retained in the repository but not shipped. Commands, fixed counts and source paths below refer to that historical checkout, not current release gates. See [current cutover evidence](pr13-cutover-evidence.md) and the acceptance ledger. Existing runtime evidence remains untouched.

Status: **PR8 scoped gates and final publication reviews PASS**. Final independent terminal-source/pending-review/continuation guard review returned a genuine completed native child result (exit0), with no concrete blockers. Main reviewed the entire34-file staged diff in bounded reads, including all manifest changes, and verified both diff checks and exact PR8-only name/status scope. User-authorized publication requires ordinary push verification; PR #3 stays draft. PR9-PR13 remain unstarted.

Publication checks: `publication-target.log` naturally completes **23/23**, no failures/cancellations/skips/todo, exit0; `publication-audit.log` passes the same exact inventory and boundaries below, exit0. Unchanged full normal/native lanes were inspected for complete totals, not relabeled reruns. `publication-independent-review.md` retains the scoped read-only reviewer result; it did not rerun tests and does not claim full-product acceptance. No production edit was needed in this final publication continuation.

## Resolved final-review blockers

**Terminal source recovery after cancel/reload:** the prior generation guard ran before source routing, while terminal research resume correctly refused. `terminal-red.log` and `terminal-red-observation.json` reproduce all three actual-native cases: completed apply, final-journal gap and final-research-receipt gap. All three hosts exited cleanly, but undo/adoption rejected generation authorization and left applied bytes in source.

`ResearchService` now enters separate source-only authorization under the existing public write refs. `ResearchStore.authorizeSource` admits only the same intrinsic owner/component and immutable quiescent terminal material. It never changes the research generation/state or grants control/evaluator/dispatch authority. Terminal commands must select an original apply intent. Existing native observation, saved role/material verification, fresh owning-Pi source UI, revision/retirement checks and exact postimages remain mandatory. Source receipt CAS preserves the terminal research generation; the separate journal receipt records the current source-command generation. `terminal-green.log` passes3/3; expanded terminal6 and final native42 include newer bytes/modes, another actual Pi owner and Fabric write deny/ask refusals. Original intent identity, approval, spec/epoch, attempts, evaluations and measured target remain unchanged.

Continued full-diff review also found a dangling-symlink-parent preflight gap, first-attempt assumptions in continuation refinement/discard, and cancellation during continuation freeze/restore being overwritten by paused finalization. Real-FS/service red1, red2 and red1 probes now pass. Parent preflight uses `lstat`; refinement follows exact measured attempt/evaluation links; discard selects the latest reserved continuation with its native-digest guard intact; continuation finalization preserves terminal, interrupted, cleanup-pending and retired state. Actual-native restart/refine/discard and held-collection cancel journeys pass. Prior blocker reports are retained in `repair-staged-review.md`, `terminal-review-findings.md` and `final-implementation-review.md`; these are historical findings, not final approval.

## Verified prerequisite and scope

Exact root `/home/balauru/.pi-profiles/fabric/.worktrees/arbor-refactor`, branch `arbor/refactor-pr0-pr1`, repair-start HEAD `f1c816c5e54218066b99cf45fafce80b18afad11`. The supplied uncommitted PR8 diff was preserved. Original implementation started from clean pushed PR7; retained PR7 normal252, native8/20/21/9/21/5 and A12 ratio0.5567536889897844 remain historical, not relabeled reruns. Work is confined to this app and the authoritative plan. Dependencies/lock and physical app-local modules are unchanged.

## Implemented execution path

`commands.ts` emits only exact public policy-aware refs. `ResearchService` validates intrinsic native owner and closed schemas, owns genuine UI dialogs and exact single-flight source commands, then delegates the existing `OwnerExecution`, `MaterialJourney`, `EvaluationEngine` and `ResearchStore`. No callable owner pointer, provider transport, private Fabric import or second native participant registry was added.

- Modes: Auto continuous progress, Direction expansion review, Review promotion review, Collaborative reviewed direction and explicit continuation. Receipts bind exact material/epoch/revision/native owner. A real sixty-second dialog timeout preserves pending review; flags, wrong choices, stale dialogs and outside-owner calls cannot approve. Research review, source approval and Fabric permissions remain distinct.
- Resume: saved immutable spec and packaged role bundle, exact known native provenance and episode-unique stopped-actor replacement. Broken changed project defaults do not affect resume. Material recovery returns its exact committed binding; it never rebases a stale caller after awaited work. Unknown evaluator handles preflight before any material rebind. Uncertainty remains `cleanup_pending`, with original history/artifacts retained.
- Partial invocation: additive agent-risk `arbor.resumeAttempt`, emitted by `continue-partial` and `restart-parent`. Each admits a new bounded charged native invocation with prior/root attempt linkage, an explicit same-hypothesis summary and a distinct exact partial/parent OID. Prior native IDs/roles/spec/epoch and attempts remain immutable. Scoring/research uses its separate policy route. Cancelled terminal material cannot be reopened by continuation.
- Consequential material journal: existing reservation/native/evaluation records plus exact frozen/retained artifact receipts and owned-ref intent. A settled worker's reset parent is never mistaken for its original result. Independent restore still runs after freeze refusal. Only explicit proven recovery can supersede an already-settled operation rejection for close; original errors/history remain. Interruption/cleanup lifecycle facts can coexist with a pending Git intent; ordinary control/decision mutation remains blocked. Actual component reload now preserves that intent for exact same-host generation recovery.
- Source: `SourceApply.ts` records exact baseline-relative patch, owning UI binding and per-path pre/postimages before writes. It never invokes source Git index/ref mutation. Apply requires a quiescent current measured keep and separate Fabric write policy; undo requires the original exact apply record and verifies its postimage. Newer affected bytes/modes, unsafe parents and mixed partial outcomes conflict without overwriting them. Exact all-postimage replay adopts once. Journals/exports retain the patch for read-only retrieval even when ownership is lost.

## Final executed gates

All logs and detailed values are under `.runtime/pr8-gates/`, retained locally and not packed.

| Gate | Result | Exact log |
| --- | --- | --- |
| `npm run check`, both no-emit checks and all normal tests | **275/275**, no skips | `verified-final-normal.log` |
| Package / retained / managed / research / evaluator / material | **5 / 92 / 20 / 42 / 45 / 71** | same log; retained selection unchanged |
| `npm run test:pr8` | **23/23** | `verified-final-target.log` |
| Native PR2 / PR4, retained prior repair gates, not rerun in this continuation | **8 / 21** | `repair-pr2-native.log`, `repair-pr4-native.log` |
| Native PR3 / PR5 / PR6 / PR7, rerun after source and continuation-selection repairs | **20 / 9 / 21 / 5** | `final-pr{3,5,6,7}-native.log` |
| Full `npm run test:pr8:e2e`, rerun after the final continuation-only cancellation guard | **42/42**, no skips | `verified-final-pr8-native.log` |
| Exact local audit | **PASS** | `terminal-audit.log`, `audit.json` |

Every accepted suite naturally emitted tests/pass/fail/cancelled/skipped/todo, suites and duration summaries; `.exit` files record actual command status. The older `verified-pr8-native.log` ended without totals despite exit0 and **does not certify completion**. `terminal-final-pr8-native.log` naturally completed39/40 but failed the material agent-suite host's2-MiB stdout cap. Full native records are now compared before emitting a compact test summary; the cap and strict successful-exit assertions are unchanged. `terminal-output-green.log` passes1/1; the final42 suite passes naturally. Both failed/incomplete logs and all host artifacts remain retained, never relabeled passing gates.

PR8's42 cases are the prior34 controls/reload/crash/recovery journeys plus **6 terminal-source cases +1 native measured restart/refine/discard +1 native cancel during continuation collection**. Native clean exit and source/index/ref preservation assertions remain strict. Both PR6 code/instruction five-stage journeys and PR7 preparation/protected-write regressions pass. The final production change only guards `resumeAttempt` finalization; normal and full PR8 were rerun after it, with the unaffected prior native gates explicitly retained above.

A12 latest rerun: identical two-candidate atomic reservation/collection boundary, one discarded warmup and exactly three measured one-second waves per mode. Serial median **3506 ms**, parallel **1981 ms**, ratio **0.565031374786081 <=0.8**; all measured parallel waves overlap. Exact environment, roots and timing breakdown are in `a12.json`. Actor/evaluator turns outside that boundary are excluded. Controlled-wave evidence, not campaign speed or research quality. Earlier ratios remain historical.

Audit confirms **19 public refs, 10 unchanged owner native requirements, one unchanged public skill, 64 packed files, 31 reachable source modules, 180 static imports**. No private runtime/legacy-driver import or generated runtime file is packed. CLI rejects every added mutation, including attached/offline variants; browser remains static/read-only. Original fingerprint implementation/tests and retained92 selection are byte-identical to the PR7 base.

The inclusive audited window contains **711 exact exit records: 639 clean +6 expected synthetic PR2 exit-guard failures +65 actual SIGKILLs +1 explicitly identified failed output-overflow checkpoint**. These include historical/red/checkpoint runs and are inventory counts, not passing-test totals. Maximum event **56,000 bytes**, maximum trace **1,369,835 bytes**. `audit.json` lists every path and classifies the exact failed host `agent-suite-IvDj5P`; every other non-synthetic, non-crash exit must be clean. No retained artifacts are deleted.

## Nine consequential crash gaps

Every row below has an actual old Pi process killed with SIGKILL at the stated production boundary in a disposable app-source copy. Locked physical dependencies, public native bridge and ordinary guards are unchanged. The reopened actual Pi has a **different intrinsic native owner**: it publicly queries recorded native IDs, rejects resume and three separate apply/undo/review calls, and preserves the exact saved projection digest and create/spawn count. These are **nine owner-loss BLOCK proofs, not nine successful resumptions**. A read-only reopened projection retains its last saved state; it is not proof of live execution or cleanup success.

| Gap | Final host suffix under `.runtime/pr6-host/` | Observed boundary and complementary recovery |
| --- | --- | --- |
| 1 reservation before spawn | `command-JXTevi` | Reserved attempt has no native ID. Separate same-owner reservation recovery releases proven-unlaunched capacity, zero workers. |
| 2 spawn return before domain attachment | `command-p2Ybvb` | Actual spawn result exists before research attachment. Separate known-handle recovery attaches exact provenance, no duplicate worker. |
| 3 interrupted partial worker | `command-F8X5q5` | Partial worker bytes and native ID persist, but the attempt is still `running` with `nativeDigest: null`. This is not terminal settlement. Separate settled-failure native `continue-partial` and `restart-parent` tests each charge a new invocation and retain the original attempt. |
| 4 finished worker before freeze | `command-giqbdQ` | Candidate OID absent, native settlement retained. Separate same-owner freeze recovery reuses original retained worker edits and achieves measured keep. |
| 5 frozen before evaluation | `command-gXGQzP` | Exact candidate OID exists; only baseline evaluation is recorded. Reopened different owner blocks rather than evaluating/relaunching. |
| 6 evaluation before decision | `command-ROYxyi` | Two evaluations retained before decision. Separate same-owner recovery keeps original exact evidence with no duplicate evaluation or worker. |
| 7 owned ref before DB commit | `command-i6MlBn` | Durable pending intent survives actual ref change. Separate same-host **new component generation** recovery adopts exact target without duplicate worker; ordinary pending controls still reject. |
| 8 partially applied source | `command-vJtYuw` | Two-path journal is writing; one real source addition exists before death. No replay or blind inverse. Source probes also verify exact all-post adoption and newer-edit/mode conflict refusal. |
| 9 reload cleanup interrupted | `command-UHaxn7` | Real component reload reaches held native ask cleanup and is killed before stop. Reopened owner cannot assume cleanup or redispatch. Prior native late-create/ask/wait/stop and unknown-cleanup gates remain passing. |

`gap-1.json` through `gap-9.json` contain absolute roots, native observations, owner denials and projection digests. Each root retains `crash.json`, killed `exit.json`, clean `reopen-exit.json`, `crash-observation.json`, source copies and native logs. `recovered-reservation.json`, `recovered-attachment.json`, `recovered-freeze.json`, `recovered-evaluation.json`, `recovered-ref.json` contain the five successful same-owner cases. The ref case explicitly asserts a changed component generation with unchanged intrinsic owner/spec/epoch and original worker ID.

Additional exact RPC/actor evidence: `native-mode-{auto,direction,review,collaborative}.json`, `native-timeout.json`, `native-steer-reload.json`, `native-apply-undo.json`, `native-write-{deny,ask}.json`, `native-conflict-export.json`, `native-partial.json`, `native-restart-parent.json`. Each names its root and genuine UI request/response records. Apply/undo/partial/restart and research resume are exercised through actual public command emitters, not unchecked service calls.

## Independently reported reload defects: red, repair, native proof

`tests/integration/pr8-reload-regressions.test.ts` first failed **5/5** in `repair-red.log`; the first green loop is `repair-green.log`. The final42 suite reruns those exact regressions with stronger provenance assertions.

- **P1 pending review:** same-owner `/arbor resume RUN` now explicitly re-observes quiescence and renews only the pending decision's generation/revision binding. It returns `awaiting_review`, without approval, evaluation or dispatch. Saved choice/spec/epoch/owner/attempt/evaluation facts remain unchanged. Direction, Review and Collaborative actual native await-review -> component reload -> public resume -> fresh RPC review -> continued research all pass. Old bindings fail; a held old-generation dialog also fails in the fast service test. Evidence: `reload-review-{direction,review,collaborative}.json`.
- **P2 postwrite journal/receipt gaps:** after same-host reload and explicit resume, `/arbor apply RUN ORIGINAL_APPLY_COMMAND_ID` selects the original immutable intent under a fresh current command and genuine source dialog through the unchanged Fabric write-risk ref. Only complete exact postimages may be adopted; it never reapplies preimages or mixed outcomes. Original intent identity and approval remain byte-equivalent; a bounded separate adoption receipt records fresh owner/generation/revision/approval. `/arbor undo-apply RUN ORIGINAL_APPLY_COMMAND_ID` then verifies and restores that exact apply. Actual native tests fault after all source writes, separately before final journal save and before research receipt persistence. Evidence: `reload-source-{journal,receipt}.json`. Five fast outcome cases cover complete, preimage, mixed, newer bytes and newer mode; ownership, duplicate-command and undo guards remain active.
- The first full normal command caught a test-only unknown-projection TypeScript error (`repair-final-normal.log`). Typed run reads repaired it; `review-verified-normal.log` naturally completed both no-emit checks and271 tests. No failed command was counted as passing.

## Independent staged review: additional scoped repairs

The first independent full33-path staged-diff review blocked publication (`repair-staged-review-initial.md`) on two adjacent reproductions and a contradictory crash-table claim. No commit/push occurred. `review-blockers-red.log` then naturally failed3/3 native cases; `review-blockers-green.log` passed3/3. The final42 suite includes all of them:

- Pending `execution: material` reviews now share the inert rebind, reached via public `control` for native agent-suite evaluators and execute-risk `evaluate` for command evaluators. The returned receipt and pending-choice revision commit atomically; fresh review succeeds without intervening evaluation/dispatch. Both native routes pass (`reload-material-{command,agent-suite}.json`). Held old-generation dialog tests cover material and research.
- Original postwrite intent adoption is no longer conditioned on its target still being incumbent. An actual resume with remaining budget keeps a second, higher-scoring candidate before public recovery and undo of the first source apply. Admission verifies the original recorded measured target and complete original postimages; adoption/undo dialogs also show that original target. Newer source edits still block. Evidence: `reload-source-new-incumbent.json`.
- Gap3's table now correctly reports interrupted partial bytes, a persisted native ID, `running` state and **no native digest**, not terminal settlement. Separate continuation tests have settled-failure evidence.
- An old PR5 source test expected every pending-review resume to reject. `review-final-normal.log` caught it; the test now asserts inert resume, unchanged attempts/evaluations/native count, still-unapproved choice and stale-dialog refusal, while preserving its command execute-policy checks. `review-verified-normal.log` passes all271. The first held-dialog red test hung during cleanup after expected rebind failure; it now always releases the UI and `review-material-unit-red-complete.log` records the natural red result. The timeout is not counted as evidence of passing behavior.

Earlier PR2/4/7 and PR3/5/6 results in the preceding repair history are superseded only by the exact reruns in the final-gate table above. Full staged review remains mandatory for publication.

## Failures found and repaired, not hidden

- Initial missing SourceApply/new continuation red probes became passing real Git/FS and native journeys. Registration expectations were updated to the actual added ref: 16 research +3 substrate =19. The exact manifest was regenerated, not weakened. Continuation is correctly agent-risk native dispatch, not a command evaluator.
- The full material gate caught silent stale-resume rebasing. Original binding now survives unless explicit reconciliation returns its own exact revision; the original pending-CAS agent-control rejection still passes.
- Source single-flight and stale-dialog probes prevent duplicate UI calls from overwriting a completed journal and reject intervening control revisions. Seven SourceApply probes cover dirty baseline, unrelated newer edits, modes/symlinks, deletion/newline names, preimage conflicts, safe undo, partial reopen, exact postimage idempotence and unsafe symlink parents.
- Initial final-native PR5 gate exposed unknown-evaluator resume downgrading `cleanup_pending` via material rebind. `cleanup-red.log` minimized the exact projection mutation; `cleanup-green.log` and full repaired native PR5 **9/9** prove the preflight repair.
- Initial expanded nine-gap reopen probes were rejected by Fabric's TypeScript checker before execution because an inferred union was spread. No success was inferred. `crash-type-green.log` verifies the repaired program, followed by the expanded lane. The fresh naturally completed34 lane now certifies all nine outside-owner denials.
- `ref-reload-red.log` reproduced actual component cleanup failure with a pending ref intent. Only interrupted/cleanup lifecycle settlement may now retain and advance that intent binding; control/decision bypass remains forbidden. `ref-reload-green.log` and the full native lane prove clean reload into a new generation, then exact recovery.

## Acceptance and remaining limits

The passing tests provide scoped A09/A10 and PR8 portions of A11/A24/A27/A28/A29 evidence, preserving prior accepted scopes. Terminal-source and continued-review findings above are repaired with executed regressions. Final independent guard review and Main full staged-diff review pass; historical blocker reports do not certify the final tree. No broad lifecycle/product theorem or later milestone follows. Different intrinsic owner, unknown/mismatched native handles and uncertain effects remain blocked. Native private history is not required; native owner identity is not portable. Trusted worktrees and synchronous preimage checks are not arbitrary-descendant containment or filesystem locking. In-flight work may exceed admission-time artifact/time limits; aggregate costs remain observational/unavailable. Worker-scored feedback stays unavailable. No cleanup of retained artifacts is authorized.

README, consumer instructions, scoped AGENTS, exact manifest, plan and ledger describe this executed scope. The completed reviews permit only the user-authorized scoped commit and ordinary push with remote equality verification. No dependency/API/runtime replacement or PR9-PR13 work.
