# PR5 owned material evidence

Status: **independent scoped verification PASS, PR5 publication gate accepted**. PR6-PR13 remain pending; this is not full autonomous research or complete recovery acceptance.

## Independent publication gate (current)

Verified exact root `/home/balauru/.pi-profiles/fabric/.worktrees/arbor-refactor`, branch `arbor/refactor-pr0-pr1`, starting HEAD `e0cb919f20b5b2ae280404da53adafbfd1dfff62` and exclusively PR5 app/plan changes. Independent evidence is retained in `.runtime/pr5-independent-gate/`, excluded from the commit. Source/test inventory hashes and the original diff are retained there. Effective Fovea tools were unavailable; bounded source reads and the automatic change-impact notices guided review.

All six supplied repairs independently pass source inspection, direct regressions and the relevant actual native proofs:

| Repair | Decisive path and proof |
| --- | --- |
| Transitive symlink identity | `src/material/evaluation.ts` expands full tree-relative chains before `..` both on freeze and verification. `tests/material/pr5-workspace.test.ts` covers escape/cycle/internal alias; the production journey refuses the escaping baseline. No confinement claim. |
| Rounded means | `src/material/acceptance.ts` uses exact sums/counts and BigInt cross-products. Task/repeat threshold equality, relative/minimize, sub-nanounit and mixed directions pass; native `[1,0,0] -> [1,1,1]` remains below `0.666666667`. |
| Lost evaluator spawn | `MaterialJourney.cancel` includes durable unresolved invocations, not just candidate workers. Direct and native accepted-spawn reply loss retain `cleanup_pending`, exact input and artifacts; unknown-handle resume refuses without redispatch. |
| Completed baseline resume | `ResearchStore.rebindEvaluationRun` restores a quiescent paused material run. Direct and native agent/command paths dispatch afterward without baseline recharge; command resume still uses execute-risk `arbor.evaluate`, not agent-risk control. |
| Lowercase skip-worktree | `Workspace` rejects both `S` and `s`, including assume-unchanged plus missing indexed bytes, without source index/ref changes. |
| Sentinel attempt IDs | `MaterialJourney` resolves actual attempts before baseline/exact-material fallback; both legal IDs evaluate and keep their actual candidate OIDs. |

Adjacent review reproduced and repaired **material control-state masking**: a terminal cancelled run falsely reported resume success and could be changed to paused; pause could also hide unresolved cleanup. Material-only transaction guards now reject terminal pause/resume and unresolved pause without changing saved state. Explicit unknown-handle reconciliation remains available but blocked on missing proof. Added direct regressions cover agent/command terminal paths plus stale material/epoch/revision and pending-CAS controls. Native agent/command resume and lost-reply cases include the new guards while retaining all prior assertions. No evaluator/policy/stale-evidence guard was relaxed.

| Final command | Observed result | Retained evidence |
| --- | --- | --- |
| `npm run check` | Both no-emit checks; **212/212** (5 + unchanged 92 + 20 + 25 + 44 + **26 PR5**), no skips | `check-final.log` |
| Native PR2/PR3/PR4/PR5 | **8/20/21/8**, no skips; rerun after product repair | `pr2-native-final.log` through `pr5-native-final.log` |
| `node --import tsx tests/material/audit.ts --independent` | **63 distinct final exit records**, 16 public refs, 10 requirements, one public skill, 57 packed files, 27 active modules / 148 static imports | `audit-complete.log`, `audit-complete.json`, `native-final-paths.txt` |
| Adjacent before/after | Direct terminal and cleanup-pause failures reproduced; stale/pending-CAS controls already pass. Native terminal agent/command failures reproduce, then pass | `adjacent-red.log`, `adjacent-green.log`, `cleanup-pause-red.log`, `cleanup-pause-green.log`, `terminal-native-red-bounded.log`, `terminal-native-green.log` |

The supplied **210** normal and **8/20/21/8** native claims were first independently reproduced before the adjacent repair; initial rerun logs are retained separately. The final **212** count adds two direct cases, not exclusions. The final exit audit includes PR2 8 actual successes plus one successful synthetic guard and three expected failing synthetic guards, PR3 22 successes, PR4 21 successes and PR5 8 successes. Host markers never substitute for exits.

Verification-harness issues remain recorded, not hidden: the first expanded native return exceeded the existing result bound, so the probe now returns bounded summaries and fresh control IDs (`terminal-native-red.log`). The first exit selector omitted hyphenated `host-exit.json` names and failed **33 vs 63** (`audit-inventory-failed.log`). Complete final before/after lists were reconstructed using all `*exit.json` names and the preserved final-start marker; the final exact difference and all 63 exit contracts pass. No passing host contract was weakened and no runtime evidence was deleted. Whole staged review also reproduced the audit treating an empty untracked list as a path (`audit-staged-red.log`); NUL-delimited path parsing now handles empty staged/clean inventories while retaining exact scope checks (`audit-staged-green.log`). The final audit-only change passes `npm run typecheck:test`; no product code changed after the final normal/native gates.

Acceptance remains A07 and the PR5 portions of A05/A06/A08, with bounded A04/A10/A20/A21 and control evidence, not full journeys. All overall PARTIAL IDs below remain PARTIAL. Trusted worktrees are not confinement; arbitrary descendants and unknown handles can remain unresolved with artifacts retained. Pending integration after owner-generation replacement remains blocked; full PR8 recovery, source apply/undo and partial-worker continuation are not accepted. PR6 automatic research and frozen operational role assembly are pending/unstarted.

Whole scoped cached-diff review covers all 29 staged app/plan files via bounded reads, with exact-path staging and whitespace/name-status checks. The preservation probe records 350 initial app/plan files byte-identical, only ten explicitly allowed review changes and no removals (`preservation.json`). The publication receipt is Git/PR history, not a broader product-completion claim. No runtime/artifacts/credentials/dependencies, Fabric API/private runtime, blacklisted profile, original checkout, benchmark skill or paid research edits are included. Retained-source selection and minimal fingerprint source/test remain unchanged.

## Six-finding review repair checkpoint (historical)

Verified the exact worktree `/home/balauru/.pi-profiles/fabric/.worktrees/arbor-refactor`, branch `arbor/refactor-pr0-pr1`, HEAD `e0cb919f20b5b2ae280404da53adafbfd1dfff62` **with existing uncommitted PR5 changes**, before edits. Preserved the original implementation evidence below and all runtime artifacts. Repair-only evidence is in `.runtime/pr5-review-repair/`, including `initial-tracked.patch`, `initial-status.txt` and `initial-sha256.txt`. Fovea change-impact notices confirmed the Workspace/ResearchStore/OwnerExecution/EvaluationEngine seams; source and executed gates remained authoritative. Edits stay within app and plan. No commit, PR6, private Fabric/API change, dependency change, benchmark skill, paid research or dataset work.

| Finding | Repair and exercised boundary |
| --- | --- |
| P1 transitive symlink escape | Expand components against the full immutable tree before processing `..`; reject escapes and cycles both before materialization and during verification. Direct `a -> .`, `escape -> a/../outside`, cycle and valid internal-alias tests; actual ResearchService baseline refusal before evaluator dispatch. This is input identity consistency, not filesystem containment. |
| P2 rounded gain | Keep native task/repeat numerators and counts; BigInt cross-multiply practical thresholds and noise, and derive mixed-task direction without rounded means. Preserve declared command medians. Product task/repeat `2/3` boundary, exact equality, relative/minimize, sub-nanounit and hidden mixed-direction probes; actual native `[1,0,0] -> [1,1,1]` is blocked at `0.666666667`. |
| P2 evaluator cancellation gap | Await evaluator and candidate settlement and include durable launching/attached invocations lacking native completion. Direct and actual native accepted-spawn/lost-reply probes retain `cleanup_pending`, input snapshots and evaluation artifacts. Unknown-handle resume refuses without redispatch. The native fixture separately waits the public member for host hygiene; this is not product recovery or inferred ownership. |
| P2 completed-baseline resume | Explicitly transition a quiescent paused material run even with all evaluations completed. Keep pending review/integration guards. Command program selects an existing completed record with `resume:true` through execute-risk `arbor.evaluate`; agent-risk control remains blocked for commands. Direct and native agent/command paths dispatch afterward without baseline recharge. |
| P2 lowercase sparse flag | Refuse both `S` and `s` skip-worktree entries before capture. Regression combines skip-worktree and assume-unchanged with missing working bytes and verifies unchanged source index/refs. |
| P2 sentinel collision | Resolve persisted attempts before `baseline`/`exact-material` sentinel fallback. Both legal IDs complete actual candidate evaluation and keep; the candidate OID and score are checked rather than silently reselecting the baseline. |

### Current executed gates

| Command | Result | Repair evidence |
| --- | --- | --- |
| `npm run test:pr5` before fixes | 15 existing PASS, 8 new FAIL at the reported boundaries | `red-source.log` |
| Four new native probes before fixes | Threshold and both resume paths reproduce; first loss fixture registration missed the override, then explicit public registration/reload reproduces `cancelled` instead of `cleanup_pending` | `red-native.log`, `red-native-loss.log` |
| `npm run typecheck:test && npm run test:pr5` | 23/23 at first green checkpoint | `target-1.log` |
| Four targeted native repair probes | 4/4 | `native-target-1.log` |
| `npm run check` after supplementary exact-boundary test | Both no-emit checks, **210/210**: 5 + 92 + 20 + 25 + 44 + **24 PR5** | `check-final.log` |
| `npm run test:pr2:e2e` | **8/8** | `pr2-native-final.log` |
| `npm run test:pr3:e2e` | **20/20** | `pr3-native-final.log` |
| `npm run test:pr4:e2e` | **21/21** | `pr4-native-final.log` |
| `npm run test:pr5:e2e` | **8/8**, including all four original journeys | `pr5-native-final.log` |
| `node --import tsx tests/material/audit.ts --review-repair` | PASS: 16 refs, 10 requirements, 1 public skill, 57 packed files, 27 active modules / 148 static imports | `audit-complete.log`, `audit-complete.json` |
| `npm run typecheck:test` after audit extension | PASS | `typecheck-audit-final.log` |
| Repair preservation probe | PASS: 220 initial source/test/doc files byte-identical; only 12 explicitly allowed existing files in that inventory changed; branch/HEAD unchanged, no staging or commit | `preservation.log`, `final-checkpoint.json` |

The new before/after path-set difference contains **63 exact host-exit records**: PR2 8 actual successes + 1 successful synthetic guard + 3 expected failing synthetic guards; PR3 22 successes; PR4 21 successes; PR5 8 successes. All original exit assertions remain. The audit now asserts exact lane counts as well as each exit contract. Default audit mode still checks the original final3 evidence without overwriting it. Retained source selection, minimal fingerprint source/test, dependencies and lockfile are unchanged.

These repairs strengthen only the existing A07 and PR5 A05/A06/A08/A20/A21 portions; explicit pause/resume and unknown-handle cancellation add bounded A11/A29 evidence, not complete interaction/recovery acceptance. All overall PARTIAL IDs below remain PARTIAL. The reviewer's separate SIGKILL CAS recovery and foreign-ref refusal observations are reviewer-reported, not new native runs claimed here; the existing after-CAS source regression and all prior gates pass unchanged.

Limitations remain: trusted worktrees are not confinement; unknown evaluator handles stay blocked with retained artifacts; symlink resolution has a 4096-expansion bound per path; no autonomous research, generalized PR8 recovery, source apply/undo, or commit. Main independently reviews the whole uncommitted app/plan change before deciding any commit.

The remainder is retained original implementation history. Its final3 counts and timestamps are superseded only by the scoped repair checkpoint above.

## Original implementation identity and retained phase checkpoint (historical)

- Verified before edits: `/home/balauru/.pi-profiles/fabric/.worktrees/arbor-refactor`, clean branch `arbor/refactor-pr0-pr1`, HEAD `e0cb919f20b5b2ae280404da53adafbfd1dfff62`.
- Read root/scoped AGENTS, all 1070 lines of the authoritative plan in bounded continuations and latest PR4 ledger. Prior PR4 normal/native counts were 186 and 8/20/21, not PR5 evidence.
- App-local physical dependencies: Node 26.7.0, Pi 0.85.1, Fabric 0.83.0. No original-checkout symlinks, dependency changes or lockfile edits.
- Captured Fovea ref was unavailable in effective registry; targeted source tracing was used. Automatic Fovea change notices were available and considered. Shared-literal suggestions outside the app did not authorize reads/edits/dependencies.
- Read installed public Fabric components/providers/agents and linked component/provider calculus completely, and installed Pi packages documentation completely. Effective `agents.spawn`, `agents.wait`, `agents.stop` schemas were inspected. Existing post-activation captured context and exact requirements are unchanged; no Fabric API/private import/new runtime/transport/broker/benchmark skill is added.
- A phase checkpoint was persisted before implementation/native gates. Runtime logs, workspaces, repositories and native artifacts remain retained under `.runtime/pr5-*` and prior native lane roots.

## Production path

`ResearchService.start` resolves `execution: material`, captures into `runs/RUN/workspace/repository.git`, freezes definition/spec and evaluates the captured baseline. Command effects still require `arbor.evaluate` through normal execute policy. An owner-selected hypothesis enters `arbor.dispatch`: ResearchStore reserves it, Workspace creates a detached candidate, and OwnerExecution uses real captured `agents.spawn`/owned `agents.wait`/terminal cleanup. After settlement, MaterialJourney freezes the full tree and restores only that owned worktree. The production EvaluationEngine loads separate exact snapshots.

Original root/OID, owned repository/baseline, candidate OID, protected evaluation-input identity and evaluator/spec/epoch identities are separate. Indexed working bytes include staged/unstaged edits; index-deleted files left on disk are excluded unless selected. Ignored selections refuse. Non-Git capture never initializes source. Source index/refs are read only. Content/mode/stat/index inventories detect concurrent changes. Modes, binary content, symlinks, deletions and NUL-delimited paths survive; unsupported merge/submodule/sparse/special-file cases fail clearly.

A measured keep requires settled candidate provenance, successful exact evaluator/native/check evidence, immutable scope/inputs, current-incumbent comparison, positive direction-aware BigInt practical gain and the conservative noise policy. The oracle covers minimization, negative/zero incumbent, threshold equality, negative-threshold refusal and ties. Relative thresholds are fractions of incumbent magnitude; zero denominators are inconclusive. Command spread comparable to gain and mixed task directions are inconclusive, not scientific superiority. Rechecks have new accounted IDs. Actual owning-Pi review receipts remain distinct from Fabric permission.

SQLite records integration intent before Git CAS; incumbent/decision completion is atomic. Exact replay adopts a target already present, retries only the expected predecessor and refuses another ref. Combined branch material receives new exact evaluation, never the pre-integration score. Capture, candidate, worker-commit, partial and combined refs remain reachable. Export embeds `captured baseline -> selected owned incumbent`, excluding pre-existing dirty work. CLI/browser read existing artifacts and never generate exports.

This is one bounded production material journey, **not PR6's policy/role/autonomous loop**. Source apply/undo remains PR8.

## Final executed commands

Run from `pi-fabric-arbor/` with app-local dependencies. Logs are retained in `.runtime/pr5-gates/`.

| Command | Result | Exact evidence |
| --- | --- | --- |
| `npm run manifest:pr3` | 16 public refs, current closed schemas/effects | `manifest-final.log`, `docs/pr3-action-manifest.json` |
| `npm run check` | both no-emit checks; **201/201** tests | `check-final3.log` |
| `npm run test:pr2:e2e` | **8/8** | `pr2-native-final3.log` |
| `npm run test:pr3:e2e` | **20/20** | `pr3-native-final3.log` |
| `npm run test:pr4:e2e` | **21/21** | `pr4-native-final3.log` |
| `npm run test:pr5:e2e` | **4/4** | `pr5-native-final3.log` |
| `node --import tsx tests/material/audit.ts` | refs/config/import/package/exit audit PASS | `audit-complete.log`, `audit-complete.json` |

Normal counts: 5 package/install + 92 retained source + 20 PR2 + 25 PR3 + 44 PR4 + **15 PR5**. No test exclusions or removed assertions produced a pass. Retained source selection and minimal fingerprint implementation/test are byte-identical to HEAD. Dependencies and lockfile are unchanged. Audit walks 27 active modules / 148 static imports across package, extension and CLI, verifies all reachable modules in the 57-file package, exact 16 refs/10 owner requirements/component configuration and one public skill.

### Actual native PR5 journeys

1. Clean offline installed package: native Pi worker uses bash to edit, commit and stage the owned candidate; the production subject explicitly loads frozen instructions and is independently graded 0 -> 1. Keep/export preserve original dirty bytes/index/refs; patch contains worker changes, not original dirty work. No extra Main research inference.
2. Non-Git material: owned capture, real command baseline/candidate, parsed positive metric followed by exit **4** cannot win. No `.git` is created in source.
3. Late native spawn cancellation: owner awaits late handles, actual waits/stops and terminal proof; source/index/refs survive, artifacts remain, no live run-owned worker and no incumbent change.
4. Baseline interruption/reload: known public completion is re-observed without duplicate original dispatch. Missing deadline receipt stays invalid, requiring a separately charged fresh baseline; candidate work then completes. Uncertain recovery is not relabeled successful measurement.

### Native exit audit

Final native before/after path sets are `native-before-final3.txt` / `native-after-final3.txt`, with exact difference `native-final3-paths.txt`. Audit reads only those 59 new records:

- PR2: 8 actual successful hosts plus 1 synthetic successful exit control; 3 expected synthetic failures (crash, signal, timeout).
- PR3: 22 successful hosts. RPC records use the existing `{code:0, signal:null}` contract after stdin closes; print-host records also prove `killed:false,error:null`.
- PR4: 21 successful natural hosts.
- PR5: 4 successful natural hosts.

A completion marker never substitutes for process exit. Existing PR2-PR4 cancellation/resource/recovery/exit guards pass. Exact directories are listed in `audit-complete.json`; no giant event manifest is copied here.

## Acceptance IDs

| IDs | Executed PR5 scope | Remaining boundary |
| --- | --- | --- |
| A07 | PASS: dirty indexed/selected-untracked source bytes/index/refs preserved through capture, failure, cancel, discard, export | Trusted local filesystems, stated bounds |
| A05/A06 | PASS PR5 portion: failed native/check/parser evidence never wins; exact current incumbent, BigInt practical/noise oracle | Full autonomous/held-out journeys later |
| A08 | PASS PR5 portion: modes/symlinks/deletions/binary/NUL paths, worker commits/branches/staging rollback, actual combined-material reevaluation | Partial-worker continuation PR8 |
| A04 | Non-Git capture/evaluation, including native path | Full code/data recipe research pending |
| A10 | Durable integration intent, after-CAS fault, reopen and exact replay | Full spawn/partial-apply crash matrix PR8 |
| A20/A21 | Actual owned subject loading, accounted rechecks; prior PR4 reconstruction preserved | Worker-initiated feedback/held-out pending |
| A01/A02/A17/A22/A26/A28/A29/A30 | Prior package/read-only/owner policy/resource scopes preserved; exact manifest/source audit | Full role/interface/product acceptance not inferred |

A03/A04/A05/A06/A08/A09/A10/A11/A17/A20/A21/A22/A25/A28/A29/A30 remain PARTIAL overall where the full item needs later research, controls, roles or presentation. Review policy is tested through production source operations; the actual PR3 RPC dialog gate is rerun. This is not blanket PR8 interaction-mode acceptance.

## Red tests and diagnosed failures retained

- `red-workspace.log`, `red-acceptance.log`: fail before new modules exist.
- `workspace-1.log`: export assertion matched worker staging rather than original staged lines; corrected assertion. Nested unversioned material inherited enclosing Git root; fixed without initializing source.
- `journey-1.log` / `journey-2.log`: fixture omitted native root/local/stale fields; fixed fixture, not guard. Capture spread undeclared `kind`; fixed persisted contract.
- `native-1.log`: fake Main omitted subject model registration; fixed fixture, not product model validation.
- `check-1.log`: stale public manifest; regenerated without weakening assertions.
- `native-extended.log`: recovered completion lacked deadline receipt. Preserved PR4 invalid-grade policy and verified explicit charged fresh baseline.
- `red-staged-deletion.log`: `git rm --cached` left an unselected file; HEAD/index union wrongly recaptured it. Fixed to current index plus explicit untracked selection and retained regression.
- `red-subdirectory-deletion.log`: an index-empty tracked subdirectory incorrectly fell back to non-Git capture. HEAD is now consulted only for provenance, never to resurrect deleted content.
- `red-raw-symlink.log`: raw target bytes were UTF-8-normalized. Capture/verification now use buffers, and evaluation explicitly refuses unsupported target encoding.
- `audit-final.log`: audit expected print-host fields on RPC records; inspected existing RPC guard and used its exact contract. Pass is `audit-complete.log`.

All final required tests pass. These failures remain diagnosis evidence, not excluded tests.

## Limitations and next gate

- Trusted worktrees are isolation, not containment. Arbitrary background descendants are not guaranteed stopped. Ambiguous ownership/cleanup retains workspaces without freeze/restore/redispatch.
- 4096-file/16-MiB snapshots with UTF-8 filenames. Whole-tree evaluation refuses escaping or non-UTF-8-target symlinks, added/changed input files and unsupported special-file/submodule/sparse/unresolved-index cases. Evaluators that mutate their input tree are unsupported.
- Local Git subprocesses are bounded, not a new runtime. Repeated inventories detect observed changes; they are not an adversarial filesystem lock or write-confinement guarantee.
- Unknown deadlines remain invalid. Explicit reconciliation validates owner/spec/epoch/material; unknown handles are not guessed. Unreturned spawn/materialization gaps can remain blocked with artifacts instead of a general PR8 recovery engine.
- Descriptive inconclusive policy is not inferential statistics, a research-quality benchmark, paid inference or dataset acquisition.
- Source apply/undo and partial-worker continuation remain PR8. Frozen operational roles, actor-selected hypotheses and scored autonomous loops remain PR6; no dependent implementation is included.

Only documentation and the completed evidence audit changed after the final3 normal/native gates. `npm run typecheck:test` also passes for that final audit (`typecheck-audit-complete.log`).

That historical checkpoint ended without commit/push. The independent scoped publication gate above supersedes its pending-review status.
