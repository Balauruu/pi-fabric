# PR6 research implementation evidence

Status: **PR6 independently reviewed scoped gate PASS**. Accepted for the user-authorized scoped commit/push and draft PR #3 evidence comment, subject to explicit reviewed-path staging and final cached-diff checks. PR7 has not started; PR7-PR13 require separate work and acceptance. Historical checkpoints below do not override this gate.

## Independent Main acceptance ledger

Verified exact root `/home/balauru/.pi-profiles/fabric/.worktrees/arbor-refactor`, app `pi-fabric-arbor`, branch `arbor/refactor-pr0-pr1`, starting HEAD `da20cd3c97445b101154a99e9657ac2273bc2378`. Preserved the supplied diff and independently traced ResearchService -> OwnerExecution -> MaterialJourney -> EvaluationEngine/ResearchStore. Fovea tool discovery returned no callable action; bounded source reads and automatic impact notices were used. No Fabric changes, private APIs, alternate runtime, profile benchmarking skill, paid inference or research-dataset downloads. App-local declared dependencies only; no dependency/lock changes.

| Acceptance check | Independent result |
| --- | --- |
| Failed actor cannot hide uncertain worker/evaluator cleanup | PASS: settlement prioritizes cleanup/interruption/active reservations for every actor outcome; lost accepted worker reply retains `cleanup_pending` and active reservation. |
| Fresh autonomous review after settlement | PASS: actual native actor -> successful quiescent research pause -> real owning-host RPC approve and reject; stale/forged responses reject. |
| Pre-effect cumulative admission | PASS: actor create/ask, worker spawn, subject/retry/judge, command invocation/check and external provider. Owned files plus canonical retained evaluator/native records are charged, including pending output. Post-baseline active/artifact exhaustion admits no actor. |
| Exact attempt/evaluation attribution | PASS: identical OIDs do not inherit another attempt's evidence; decisions retain exact evidence/compared incumbent; completed outcomes and prior native facts are immutable. Both command selection and integration require the exact attempt link, including refusal of unlinked legacy evidence. |
| Authentic historical failure lineage | PASS: same native owner/component and explicit authorized generation history; attempt/artifact identities, native digest/status, material and epoch must still match. Real failed worker -> pause -> component reload -> discard/distill original evidence -> further research, with no duplicate dispatch. |

### Adjacent findings repaired before acceptance

- The first fresh normal check (`main-normal.log`) disproved the claimed timestamp repair: production `gitBytes` deliberately strips ambient `GIT_*`. Exact object inspection showed equal trees/parents but author/committer timestamps one second apart. The regression now calls real workspace freeze then canonicalizes only fixture commit metadata through the existing explicit Git environment seam. Production Git sanitation and the strict same-OID assertion remain intact.
- `main-adjacent-red.log` proved an external provider could dispatch after its awaited descriptor check exhausted active time (one call instead of zero). The real catalog now invokes the owner's final admission after descriptor validation, and only then records launching. Refusal leaves a reserved invocation, not invented launch uncertainty.
- The same red log proved nullable historical evaluation evidence could authorize a keep. Integration now requires exact attempt equality. `main-command-red.log` additionally caught the owning-Pi command selecting the latest equal-OID unlinked record; its selector now requires the exact attempt too. The regression exercises the generated public command through the real service and confirms the authentic evidence still keeps successfully.
- Two strict test projection-type errors are retained in `main-normal-repair.log` and `main-normal-final.log`; corrected before accepted gates. No failing run is counted as acceptance.

### Independent final gates

Logs and exact machine-readable inventories are retained locally under `.runtime/pr6-gates/`, excluded from the commit and package.

| Gate | Observed result | Log |
| --- | --- | --- |
| Seven targeted source regressions | **7/7**, no skips | `main-targeted-final.log` |
| `npm run check` | Both no-emit checks; **227/227** = 5 package + unchanged 92 retained + 20 managed + 31 research + 45 evaluator + 34 material | `main-normal-command-final.log` |
| `npm run test:pr2:e2e` | **8/8** | `main-pr2-accepted.log` |
| `npm run test:pr3:e2e` | **20/20** | `main-pr3-command-final.log` |
| `npm run test:pr4:e2e` | **21/21** | `main-pr4-accepted.log` |
| `npm run test:pr5:e2e` | **9/9** | `main-pr5-command-final.log` |
| `npm run test:pr6:e2e` | **21/21** | `main-pr6-accepted.log` |
| `npm run audit:pr6` | **18 refs / 10 exact requirements / one unchanged public skill / 60 packed files / 29 modules / 169 imports / 85 selected fresh exits** | `main-audit.log`, `audit-main-final.json` |

All accepted exits are zero, with no skips/exclusions. Native inventory selects PR2/PR4/PR6 from the exact `main-native-before-repair.txt` -> `main-native-after-repair.txt` difference and command-impacted PR3/PR5 from `main-command-native-before.txt` -> `main-command-native-after.txt`. Passing unaffected native lanes were not rerun after the keep selector change. The 85 records are **82 clean + three expected synthetic PR2 exit-guard failures**, not 85 successful product hosts.

Fresh journey summaries: `.runtime/pr6-host/command-LXbHH8/journey-summary.json` (**5 evaluations / 10 invocations**) and `.runtime/pr6-host/agent-suite-EnNbhN/journey-summary.json` (**5 / 30**). Each is actor-led baseline -> gain -> valid no-gain -> failed check -> further gain, four workers and lessons, with exact current-incumbent/material/native identities and unchanged dirty source/index/refs. Instruction optimization uses the clean-installed product and independently graded candidate role path, separate from operational bootstrap. `main-trace-summary.json` records maximum event **50,157 bytes**, maximum trace **930,860 bytes**, without trace dumps in Main.

### Accepted scope and remaining limits

Accept only the serial PR6 code/instruction journeys and tested operational-role/review/within-run lesson/stop/accounting scope. Preserve prior useful source tests, fingerprint oracle, read-only CLI/browser, public API-only native lifecycle and honest uncertainty. Public schema/manifest refs, caller classes, risks and commands are mechanically checked; no runtime/credentials/artifacts/dependencies are publishable payloads.

No full PR7+ or product acceptance: PR7 branching/parallel/pruning/convergence, PR8 partial/ambiguous-worker continuation and source apply/undo, PR9 held-out validation, PR10 literature/cross-run learning, PR11 example packs, PR12 rich presentation/public-skill rewrite, and PR13 legacy deletion/cut-over remain outstanding. Draft PR #3 stays draft. Active time/artifacts are cumulative admission limits, not hard caps or containment; in-flight work can overshoot. Shared DB page/WAL overhead and Fabric runtime storage are excluded. Cost/token aggregates are observational or unavailable; worker-scored feedback is unexposed. No paid-model, dataset, measured-speedup, statistical superiority or benchmark-quality claim.

## Historical reviewer repair checkpoint

The following reviewer repair claims preceded independent Main verification. Their 225-test count and timestamp-fix claim are superseded above; original logs remain retained.

## Five-finding reviewer repair ledger

Exact worktree `/home/balauru/.pi-profiles/fabric/.worktrees/arbor-refactor`, branch `arbor/refactor-pr0-pr1`, HEAD `da20cd3c97445b101154a99e9657ac2273bc2378` verified. Existing app/plan diff preserved in `.runtime/pr6-gates/reviewer-start.diff`; untracked foundation files retained. Exclusive app/plan editor, no staging/commit/publication or PR7. Effective Fovea action unavailable; bounded source traces plus automatic Fovea impact notices used.

| Finding | Repair and acceptance check | Current evidence |
| --- | --- | --- |
| P1 unresolved worker/evaluator state masked by actor error | Actor settlement preserves domain uncertainty for every actor outcome, not only completed. Lost accepted worker reply keeps `cleanup_pending` and active reservation. | Exact source red `failed != cleanup_pending`, then green |
| P1 autonomous research pause stales fresh review | Only successful, quiescent, unchanged-binding research pause advances the pending review revision. Stale/forged inputs remain rejected. | Actual native actor -> RPC approve and reject red/green |
| P1 cumulative budgets checked after native effects | Shared artifact/active admission before actor create/ask, worker spawn, each evaluator invocation/retry/judge and command check, including after baseline. | Actual local 60,000-byte output red: two invocations; green: one, no actor. Native post-baseline active exhaustion red: actor created; green: none. |
| P2 OID-based evaluation inheritance and rewritten historical fact | Evaluations persist exact `attemptId` alongside immutable pair; summaries expose nullable `attemptId`. Decisions pin exact evaluation evidence, not latest tree. Terminal evaluation outcomes cannot be rewritten. | Equal-tree source red/green, immutable decided evidence and compared-incumbent assertions |
| P2 historical failed evidence rejected after authorized rebind | Optional bounded run `generationHistory` records only authorized rebind lineage. Original attempt/artifact and completed evaluation provenance remain unchanged. Known terminal worker failure is not a retirement/storage error. | Source red/green; actual failed native worker -> pause -> reload -> discard/distill original evidence -> further research, no duplicate |

Logs: `reviewer-red-source.log`, `reviewer-red-loss-exact.log`, `reviewer-red-native.log`, `reviewer-green-source-first.log`, `reviewer-green-native.log`, `reviewer-lineage-native-first.log` (real cleanup error), `reviewer-lineage-native-green.log`, `reviewer-targeted-first.log` (manifest drift), `reviewer-targeted-second.log` (107/107). Two intermediate fixture/type errors remain recorded; no failing gate is relabeled as passing.

Artifact accounting charges owned disk files (roles, Git/workspaces, evaluation inputs and derived artifacts) plus canonical retained SQLite evaluator/log and native-evidence records. A derived JSON copy is an additional owned copy; refusing it does not erase mandatory native evidence from accounting. Shared SQLite page/WAL overhead and Fabric's own runtime storage are excluded. These are cumulative dispatch-admission limits, not retrospective hard caps or containment: in-flight output/time can overshoot, but subsequent native effects are refused. Cleanup/wait/observation remain allowed. No cost-total, paid-inference, dataset or benchmark claim.

Public action refs, requirements, risks and input contracts are unchanged. Closed output schemas add `evaluations[].attemptId` (nullable for baseline/legacy unlinked records) and optional `run.generationHistory` (maximum 128); `docs/pr3-action-manifest.json` is regenerated from the authoritative schemas. Historical unlinked evidence is not inferred from an OID.

## Final reviewer repair gates

All final exits are zero; no skips or exclusions. Logs are under `.runtime/pr6-gates/`.

| Gate | Observed final result | Log |
| --- | --- | --- |
| Source reviewer regressions | **5/5**, then all covered by final normal check | `reviewer-source-final.log` |
| `npm run check` | Both no-emit checks; **225/225** = 5 package + unchanged 92 retained + 20 managed + 31 research + 44 evaluator + 33 material | `reviewer-normal-repair-final.log` |
| `npm run test:pr2:e2e` | **8/8** | `reviewer-pr2-native-final.log` |
| `npm run test:pr3:e2e` | **20/20** | `reviewer-pr3-native-final.log` |
| `npm run test:pr4:e2e` | **21/21** | `reviewer-pr4-native-final.log` |
| `npm run test:pr5:e2e` | **9/9** | `reviewer-pr5-native-final.log` |
| `npm run test:pr6:e2e` | **21/21**, original 16 plus two RPC review, large-output admission, post-baseline active admission and failed-evidence reload/continue cases | `reviewer-pr6-native-final.log` |
| `npm run audit:pr6` | **18 refs, unchanged 10 requirements, one unchanged public skill/registration, 60 packed files, 29 modules / 169 imports; 85 fresh exit records** | `reviewer-audit-final.log`, `audit-reviewer-final.json` |

Fresh PR6 trace bounds: maximum event **50,157 bytes**, maximum whole trace **930,860 bytes**, summarized without event dumps in `reviewer-trace-summary.json`. Final journey artifacts: `.runtime/pr6-host/command-tUjftf/journey-summary.json` (**5 evaluations / 10 invocations**) and `.runtime/pr6-host/agent-suite-wk957P/journey-summary.json` (**5 / 30**).

The exact native inventory is the difference between `reviewer-native-before-final.txt` and `reviewer-native-after-final.txt`: PR2 nine clean plus three expected synthetic exit-guard failures, PR3 22 clean, PR4 21 clean, PR5 nine clean, PR6 21 clean. Total **82 clean + three expected synthetic failures**. PR6 contains both original five-stage journeys, role separation/fingerprint/read-only/policy assertions and native exit assertions unchanged.

The first full normal gate (`reviewer-normal-final.log`) caught a test-only equal-OID assumption: identical trees frozen across a wall-clock second can have different commit metadata. The regression now pins author/committer dates in its isolated test process, restores the environment afterward, and asserts exact same OID plus different evaluation IDs. The repaired **full** normal gate passes; passing native lanes were not rerun unchanged. The prior 220 and 8/20/21/9/16 gates remain historical below, not the repair result.

Final Main verification of the complete preserved diff is next. No commit/publication or PR7 is authorized or performed. These gates accept only the supplied serial PR6 repair scope; broader PR7-PR13/product acceptance is not inferred.

## Prior implementation checkpoint (superseded by reviewer repairs)

The exact worktree, branch `arbor/refactor-pr0-pr1` and HEAD `da20cd3c97445b101154a99e9657ac2273bc2378` were reverified before continuation edits. The entire existing PR6 diff was preserved. Public installed managed-component/provider/actor contracts and effective create/ask/spawn/wait/stop schemas were consulted. Fovea's effective invocation was unavailable; source tracing and automatic impact notices supplied navigation evidence. No other editor, profile-skill invocation, paid inference, download, private Fabric import, new runtime/broker or lifecycle callback was used.

Explicit `execution: research` freezes roles and source through `ResearchService.start` without research effects. The owning-Pi start command composes execute-risk **`arbor.runResearch`**. That route runs a valid baseline, then reuses `OwnerExecution`'s tracked native create/ask/stop/remove lifecycle. The same persistent actor proposes every next hypothesis/operation. Closed proposals bind exact run/material/epoch/revision/evidence and full evaluation capacity. Production `MaterialJourney` owns native worker waits, settled freeze/restore, `EvaluationEngine` measurement and transactional incumbent decisions. Main never chooses the next research operation. The fixture provider produces inference proposals/tool requests only, never research transitions or authoritative grades.

`policy.ts` supplies bounded current-incumbent/frontier/recent-fact/ancestor-lesson/control/budget observations and deterministic stop facts, not a hypothesis engine. Valid no-gain, failed checks and known native infrastructure failure are separate; keeps reset no-gain. Known failed workers can be discarded/distilled without a score. Ambiguous cleanup/evaluator work stays unresolved, including at successful actor cleanup. Every candidate is fixed-hypothesis and its native structured result must carry the exact attempt and `ARBOR_WORKER_RESULT_V1` sentinel. Worker-initiated scored feedback is **not exposed**. No informal diagnostic invocation beyond the required identity check is admitted; worker text is never imported as a grade. This is a declared trusted-worker contract, not shell interception.

`/arbor revise-roles RUN` is an explicit paused/quiescent owning-Pi control. It appends a new role binding/revision without rewriting the frozen measurement spec, epoch or earlier invocation provenance. Native reload/resume tests prove both ordinary package-change preservation and actual explicit revision loading. New actor episodes use fresh proposal command IDs. Missing phases refuse before baseline/actor effects. Exact public requirements remain the original ten managed refs; command execution cannot enter through agent-risk start/control, and each actual actor/worker/subject effect also traverses its declared managed `agents.*` policy route.

## Historical pre-review executed gates

All listed final exits are zero. No skips/exclusions. Logs and historical failures remain under `.runtime/pr6-gates/`.

| Gate | Observed result | Final log |
| --- | --- | --- |
| `npm run check` | Both no-emit checks; **220/220**: 5 package + unchanged 92 retained + 20 managed + 31 research + 44 evaluator + 28 material | `normal-loop-repair.log` |
| `npm run test:pr2:e2e` | **8/8** | `pr2-native-loop-final.log` |
| `npm run test:pr3:e2e` | **20/20** | `pr3-native-loop-repair.log` |
| `npm run test:pr4:e2e` | **21/21** | `pr4-native-loop-repair.log` |
| `npm run test:pr5:e2e` | **9/9** | `pr5-native-loop-final.log` |
| `npm run test:pr6:e2e` | **16/16** actual native cases | `research-native-loop-final.log` |
| `npm run audit:pr6` | PASS: **18 public refs, 10 exact requirements, one unchanged public skill, 60 packed files, 29 active modules / 165 imports, 80 selected fresh exit records** | `audit-loop-final.log`, `audit-loop-final.json` |

The 80 exit records include 77 clean exits and three expected synthetic PR2 exit-guard failures. PR2 has nine success records (eight hosts plus the success control), PR3 22, PR4 21, PR5 nine, and PR6 sixteen. The audit selects passing PR2/PR5/PR6 records from the first final window and passing repaired PR3/PR4 records from the second window, rather than counting failed attempts as acceptance. The unchanged retained-source selection, dependency/lock metadata, minimal fingerprint source/tests, public skill/registration and empty staging area are mechanically checked.

The PR6 native matrix is two five-stage journeys; two installed package-change/reload/resume variants (frozen and explicit revised roles); six stop probes (no-gain, target, evaluator, artifact, active time, repeated known-native failures); one missing-phase refusal; four command/agent-suite execute deny/ask cases; and one invalid actual command baseline that creates neither actor nor workers. Attempt limits stop both full journeys. Quiescent public role revision also has source regressions for stale/forged requests, receipt idempotence and immutable old provenance.

## Actual five-stage journeys

| Material/evaluator | Stages and independent outcomes | Retained artifact |
| --- | --- | --- |
| Executable `program.cjs` / command plus required command check | Baseline score 1; keep 2; valid no-gain 2; apparent 9 fails required check; further keep 3. Five exact evaluations, **10 reserved invocations**, four workers/lessons. | `.runtime/pr6-host/command-Qm5eej/journey-summary.json` |
| Colliding `skills/fabric-arbor/roles/executor.md` / fixed three-task agent suite, clean installed product | Baseline 1/3; keep 2/3; valid no-gain 2/3; candidate loses a baseline-correct task and fails the declared independent `preserve-baseline-correct` veto; further keep 3/3. Five evaluations, **30 reserved invocations**, four workers/lessons. | `.runtime/pr6-host/agent-suite-VS0pVz/journey-summary.json` |

Both have `valid, valid, valid, invalid, valid` evaluation validity and `measured-keep, discard, discard, measured-keep` decisions. Candidates two, three and four compare against the first kept incumbent, not the original baseline. Each evaluation/native/snapshot/material/spec identity and final incumbent is asserted from the production store. Every invocation is ingested with its native handle/evidence. Exact original dirty contents, staged index, refs, selected and ignored files are unchanged. Subject tasks actually load the candidate collision path; optimizer children actually receive the separate preserved role sentinel and expected tools/result schema. Only two Main inference calls occur (initial fixed program and final response). The ordinary journeys use one actor apiece and verify no live owned participants before host exit.

The resume variants run two autonomous attempts before a bounded pause, change only a disposable installed package asset, reload the real component, then autonomously finish the remaining two attempts with a replacement actor. Without revision the old bootstrap remains; with revision the new native worker sentinel is observed and old invocation bindings/measurement spec remain unchanged.

Maximum observed final PR6 trace event is **50,011 bytes**; maximum whole trace is **925,405 bytes**. Full research evidence stays in SQLite/artifacts, not large Main return events or a transcript mirror.

## Repairs found and retained

- `loop-red.log`: absent autonomous contract/policy before implementation.
- `command-journey-first.log`: native actor name exceeded the supported bound; fixed with a bounded unique research label.
- `command-journey-second.log`: local inference incorrectly JSON-stringified native text blocks, corrupting identity/result sentinel extraction; fixed by reading actual text blocks. The owner rejected the malformed native output correctly.
- `resume-stops-first.log`: stopped-actor resume reused `actor-0` receipt IDs; fixed with episode-specific proposal IDs. Large fixture return truncation was separately repaired by returning compact projections, not weakening native/store assertions.
- `resume-stops-second.log`: production resume completed correctly, but the final projection still exceeded the 50KB result limit; compact return repair is included in the passing 16-case gate.
- The first final normal check found strict test `BoundCommand` object-shape errors; fixed with detached object spreads. PR3's exhaustive descriptor test needed the new projection action included. PR4 embedded repeated full output schemas in one environment variable, hit the OS argument-size limit and left its local test server alive; the gate hit an actual **420-second tool timeout**. Its exact-schema assertion now deduplicates identical schema payloads while still checking every descriptor. Full repaired normal/PR3/PR4 gates pass; unchanged passing PR2/PR5/PR6 lanes were not rerun.

## Acceptance scope and precise continuation

**Historical pre-review claim, superseded by the five findings and repair gates above.** Main's independent full-diff review is next; no commit or PR7 implementation is authorized here. A03's independently graded instruction-improvement journey and the code journey portion of A04 are proven on deterministic local fixtures. A23/A24/A25 operational bootstrap, result contracts, collision and quiescent package/revision/resume portions have actual native evidence. Within-run lesson references, transparent research stops and invocation accounting extend A13/A15/A21 in this serial lane. This does not accept PR7 branching/parallel policy, PR8 ambiguous/partial-worker continuation or source apply, PR9 held-out behavior, PR10 literature/cross-run learning, PR11 example packs or PR12 rich presentation. Prior PR2-PR5 source-preservation, rational thresholds/noise, evidence/symlink/current-incumbent/policy/review/lifecycle and read-only CLI/browser gates remain preserved.

Active time and cumulative owned artifacts are **admission** limits, not hard containment: trusted in-flight work can overshoot between boundaries. Pauses exclude elapsed paused time. Tokens/cost are observational and aggregate totals can be unavailable; no hard monetary cap, measured speedup, scientific transfer or benchmark-quality claim is made. Evaluation infrastructure interruption blocks/reconciles honestly rather than becoming a failed scientific grade. Worker-driven authoritative feedback remains unavailable, not claimed complete.

## Historical role-foundation checkpoint

The following retained foundation evidence describes the earlier **PR6 INCOMPLETE** checkpoint, not the final gate above. Its precise continuation tasks were implemented and verified in this continuation. No historical log or runtime artifact was deleted.

## Verified starting boundary

- Exact worktree `/home/balauru/.pi-profiles/fabric/.worktrees/arbor-refactor`, branch `arbor/refactor-pr0-pr1`, clean HEAD `da20cd3c97445b101154a99e9657ac2273bc2378` verified before edits. Remote `refs/heads/arbor/refactor-pr0-pr1` returned the same PR5 SHA.
- Root/scoped AGENTS, all 1078 authoritative plan lines, full ledger and latest PR5 evidence read. Prior accepted normal/native counts were 212 and 8/20/21/8.
- Dependencies physically app-local, including public Pi/Fabric exports; no install, lockfile or dependency change.
- Effective Fovea invocation unavailable; bounded source tracing used. Automatic Fovea change-impact notices were available and considered as navigation evidence.
- Installed public Fabric components, component/provider calculus, providers, skills, agents and agent reference read completely; installed Pi skill/package docs read completely. Effective create/ask/spawn/wait/stop schemas inspected. No private Fabric imports/API changes.

## Implemented production path

`ResearchService.start` now freezes package-resolved operational assets for new `inspect` and `material` runs before candidate/evaluator effects. `ResolvedSpec.roleBundle` points to a content-addressed bundle under `runs/RUN/roles/`, outside candidate material. Its manifest binds package origin, exact file identities and byte counts. `RoleBundle` rejects missing/version-incompatible bootstrap, invalid content/path identities, over-budget files and symlinked bundle write destinations. It reads only the four explicit packaged coordinator/executor/strategy/evidence assets, never candidate-relative discovery or profile skills.

`OwnerExecution` loads the preserved role into actual supported actor `instructions`, phase-specific `agents.ask.message` and worker `task`. Strategy is loaded for selection; evidence interpretation is added after native observations and required for resume. Every operational create/ask/spawn records its bundle, role binding, loaded references, instruction/request identities, explicit model/tools/requirements/extensions/runner/thinking/result contract before the native call, then attaches native IDs. `BindingStore` rejects removal or rewriting of prior invocation attribution. This journals native request configuration; actual child tools/model/bootstrap are separately verified in native tests, not inferred solely from journal fields. No duplicate transcript is stored.

`MaterialJourney` checks the saved bootstrap before reservation/workspace dispatch. Completed-baseline material resume validates the preserved role/reference bundle before evaluator reconciliation. Package changes do not substitute optimizer instructions. The four existing internal Markdown assets now include fixed-hypothesis strategy, diagnostic/scored-feedback distinctions and adapted merge/evaluation/resume interpretation. Exactly one public `fabric-arbor` skill and its registration are byte-identical to PR5. Literature is still PR10, not another runtime or registered skill.

This is implemented through the existing owner/service/store/material/evaluator path. It does **not** implement the autonomous scored coordinator/material loop. `inspect` remains the bounded unscored PR3 actor cycle; `material` remains the explicit owner-selected PR5 journey.

## Executed gates

All logs below are retained under `.runtime/pr6-gates/`. Source tests run with app-local dependencies and no emitted directories. Native tests use actual Pi/Fabric process, Git and filesystem boundaries with local fake inference only.

| Command | Final observed result | Evidence |
| --- | --- | --- |
| Targeted PR6 source tests in `tests/research/pr6-roles.test.ts` and `tests/material/pr5-journey.test.ts` | 4/4 | `roles-final-targeted.log` |
| `npm run check` | Both no-emit checks; **216/216**, no exclusions/skips: 5 package + unchanged 92 retained + 20 PR2 + 28 research + 44 evaluator + 27 material | `normal-final.log`, `.exit` |
| `npm run test:pr2:e2e` | **8/8** | `pr2-native-final.log`, `.exit` |
| `npm run test:pr3:e2e` | **20/20** after duplicate-admission repair | `pr3-native-final.log`, `.exit` |
| `npm run test:pr4:e2e` | **21/21** | `pr4-native-final.log`, `.exit` |
| `npm run test:pr5:e2e` | **9/9**, original eight plus installed role-change/reload/resume | `pr5-native-final.log`, `.exit` |
| `node --import tsx tests/research/pr6-audit.ts` | **64 distinct fresh exit records**, 16 public refs, 10 exact requirements, one public skill, 59 packed files, 28 active modules / 157 static imports | `audit.log`, `audit.json`, `.exit` |

The audit checks the exact difference between `native-before-final.txt` and `native-after-final.txt`, not a latest-directory heuristic. It covers PR2 nine clean records (eight actual hosts and one synthetic success) plus three expected failing exit guards, PR3 22 clean print/RPC hosts, PR4 21 clean hosts and PR5 nine clean hosts. Exit assertions are unchanged. Completion markers do not substitute for exits. Final audit also verifies unchanged dependencies/lockfile, retained-source selection, fingerprint source/tests, public skill/registration, clean index/unchanged HEAD and app/plan-only edits. No evidence/runtime/dependencies are staged.

### Direct and actual native role proofs

- Filesystem tests: bundle survives package edits and resolver reconstruction; a same-path candidate cannot choose optimizer assets; missing/incompatible bootstrap and missing selected phase refuse; new content yields a distinct bundle without changing earlier bytes; budget/integrity/output-symlink cases refuse.
- Production material test: damaged executor bootstrap prevents reservation/native launch with unchanged research projection; damaged evidence procedure prevents resume/evaluator reconciliation without recharge; restored exact bytes permit dispatch; native invocation ID/model/tools/bundle are linked, and attempted attribution rewrite fails.
- Existing actual PR4 source collision test now verifies coordinator bootstrap/strategy/evidence sentinels in the fake provider's received context. The actor still attempts forbidden spawn/review and cannot resolve them. The same candidate role path is separately loaded by actual subject invocations and independently graded. The read-only operational executor retains its intended tools/instructions.
- Existing installed PR5 journey now verifies executor bootstrap sentinel, actual tools and external saved bundle while keeping all exact material, grade, source/index/ref and host-exit assertions.
- New installed PR6 role test changes only a disposable installed executor asset after initial capture/baseline, pauses, reloads the real component, resumes and dispatches a native worker. The worker receives the preserved original bootstrap, exact candidate evaluation grades 0 -> 1, and keep succeeds. A new run with the incompatible changed package is refused before run creation/evaluator work. Main performs only its fixed invocation and terminal response, not research reasoning. This remains one owner-selected candidate, not an autonomous PR6 journey.

## Failure and repair retained

`roles-red.log` fails before the module exists. Targeted role source/native proofs then passed. The first broad check passed normal/PR2/PR4/PR5 but PR3's unchanged concurrent duplicate-dispatch test failed (`pr3-native.log`). A minimized native rerun reproduced it (`duplicate-red.log`): the real trace in `.runtime/pr3-host/duplicate-dispatch-Ds7WIB/trace.jsonl` contains two distinct `agents.spawn` handles for the same attempt.

Cause: an awaited role-validation read was inserted between the final duplicate-receipt check and synchronous transactional reservation. Both callers crossed the check; the second received an idempotent receipt but still launched. The fix moves **all awaited admission work before the final receipt check**, leaving check/reservation contiguous. Native ingestion/provenance guards and the existing test were not weakened. `duplicate-green.log` and the final full 20-test PR3 gate pass. No debug instrumentation remains in product code.

## Acceptance scopes

| IDs | Executed checkpoint scope | Still pending |
| --- | --- | --- |
| A23 | Actual package bootstrap, phase sentinel/tools observation and pre-dispatch/pre-resume refusal | Full autonomous actor/executor journey and structured worker result sentinel |
| A24 | Immutable bundles, missing/incompatible refusal, actual installed package-change/reload/material resume | Explicit owning-Pi role-change control/new attributable revision; stopped-coordinator research resume |
| A25 | Existing actual same-path operational/subject collision and separate exact subject loading preserved and strengthened | Collision throughout autonomous instruction optimization; literature role PR10 |
| A22/A26/A27/A29/A30 | Exact existing refs, one skill, captured native calls/owned waits, installed assets and impacted lifecycle gates preserved | Full product/research acceptance unchanged |
| A03/A04/A05/A06/A13/A15/A20/A21 | Prior PR4/PR5 measurement/material scopes preserved | Both autonomous five-stage journeys, within-run relevance/distillation/stop policy and full budgets |

No overall PARTIAL acceptance ID is promoted to PASS by this checkpoint. In particular A03/A04 are **not** accepted autonomous code/instruction journeys.

## Historical foundation continuation checklist (superseded by the executed gate above)

1. Reverify exact branch/HEAD and preserve this uncommitted app/plan diff. Read this evidence and the authoritative PR6/role/research/budget plan sections. No commit; Main reviews the complete change.
2. Integrate **one persistent coordinator** with real `MaterialJourney` operations, not fixture sequencing. Existing seams:
   - `ResearchService.#start` resolves/captures/freezes, then agent-suite baseline calls `material.invoke("evaluate", ...)`. Command start deliberately does not execute; `src/research/commands.ts` composes the execute-risk `arbor.evaluate` route.
   - `OwnerExecution.#cycle/#researchCycle` currently creates one actor, asks bounded proposals and dispatches only read-only research workers. It drains/removes the actor on exit. Preserve tracked create/ask/wait/late-stop settlement.
   - `MaterialJourney.invoke` already owns dispatch -> native settlement -> freeze/restore, exact evaluation and transactional keep/CAS. `OwnerExecution.dispatchMaterial` owns a single hypothesis worker and its wait, not strategy.
   - `ResearchStore.validateProposal/research`, exact `acceptance` and evaluator reservations remain authoritative. Do not copy the PR5 fixture's manual sequence into another owner/coordinator/evaluator driver.
3. Add an explicit autonomous execution selection/closed contract while preserving earlier lanes. Choose an owner policy route that covers **actual command evaluation effects**, not an agent-risk start/control that later executes commands. Bind any new exact captured requirements before activation; no self-dependency cycle, generic broker, private import or Fabric change. Inspect relevant installed docs/schemas completely before using new surfaces. Native deny/ask tests must cover actual command and subject evaluation effects.
4. Require valid baseline before candidate-producing workers. Owner asks/validates/reserves/dispatches/owns waits/collects/evaluates/decides, then asks the same actor with bounded incumbent/frontier/recent-evidence/ancestor-insight/control/budget observations. No Main selecting the next operation. Add bounded within-run distillation/relevance and transparent no-gain/failure/target/budget stops. Count valid non-improvements separately from failed checks/infrastructure. Keep full branching/parallel/pruning PR7 gated.
5. Implement explicit **quiescent owner-authorized role revision** as a new binding/revision without rewriting immutable saved measurement or prior attempt/invocation provenance. `RoleBundle.freeze` already creates a distinct immutable bundle, but is not a public revision mechanism. Current `#role` loads only `spec.roleBundle`; a new role history/current binding must preserve earlier spec/evaluation identities. Test missing/incompatible bundle refusal before actions and actual reload/resume across package changes. Pre-foundation inspect/material runs lacking a bundle must not silently acquire new defaults.
6. Preserve bounded fixed-hypothesis executor behavior. Current worker result contract is native unscored text, no shared Arbor mutations/spawn and **no worker-initiated scored feedback capability**. If feedback is supported, every invocation must snapshot exact current material, reserve/charge a stable evaluator ID and retain native result, never accept a scalar or bypass grades. Diagnostic instructions currently admit no informal diagnostic runs, apart from identity checking; do not claim shell interception or hard cost enforcement.
7. Add two actual source/installed production-path native fake-provider research journeys, code and instruction improvement, **each baseline -> gain -> valid no-gain -> failed check -> further gain**. Coordinator fake inference chooses structured hypotheses from observations; product owner/evaluator executes everything. Assert exact all material/evaluation/invocation/incumbent IDs, independent grades, unchanged dirty source/index/refs, budget accounting, sentinel/result contracts and separate subject candidate loading. Existing PR4/PR5 fixtures are regression evidence, not those journeys.
8. Enforce attempts/evaluator calls/active/artifact budgets end-to-end; bundle freeze currently enforces only its own byte ceiling, not cumulative run artifact accounting. Usage/cost remain observational/unknown. Keep source rational/noise/symlink/provenance guards and readonly CLI/browser unchanged. No paid inference/datasets, profile benchmarking skill, private APIs, new runtime, lifecycle callback, original checkout or blacklisted Pi profile.
9. Targeted regressions first, then source normal checks and impacted native lanes. Keep logs and exact exit inventory under `.runtime/pr6-gates/` with new names rather than overwriting this checkpoint. Update this document, ledger/README/scoped guidance/plan only to observed scope. The current audit is intentionally a **role-foundation** audit, not the final PR6 gate.
