import { execFileSync, spawnSync } from "node:child_process";
import { existsSync, mkdirSync, mkdtempSync, readFileSync, realpathSync, rmSync, watch, writeFileSync } from "node:fs";
import { tmpdir } from "node:os";
import { dirname, join, resolve } from "node:path";
import type { AuthorizationPayloadV1, AuthorizationRecordV1, ArborCommandV1 } from "../domain/types.js";
import { aggregateTrials, compareAggregates, exactUnits } from "../domain/decimal.js";
import { ArborError } from "../domain/errors.js";
import { ArborApplication } from "../application/ArborApplication.js";
import { LocalTtyAuthorizationCoordinator, OwnerOnlyEd25519KeyStore, TrustedPrincipalRegistry, createOwnerOnlyPrincipalFiles, trustedPrincipalKeyId } from "../authorization/TrustedPrincipal.js";
import { runInstalledApprovalRuntimeMatrix } from "../certification/approval-runtime.js";
import { verifyLocalContainmentCertification } from "../certification/containment.js";
import { collectCompatibilityRuntimeEvidence } from "../certification/runtime-evidence.js";
import { piFabricCertificationRootV1, readCertifiedPiFabricVersionV1 } from "../certification/pi-fabric-support.js";
import { ManifestCleanupAdapter } from "../cleanup/ManifestCleanupAdapter.js";
import { LinuxBubblewrapContainmentAdapter } from "../containment/BubblewrapContainmentAdapter.js";
import { runContainmentAdversarialMatrix } from "../containment/adversarial.js";
import { SealedHeldOutEvaluatorServiceV1, probeHeldOutServiceDeniedV1, verifyHeldOutEvaluatorServiceSealV1, type HeldOutEvaluationServiceReceiptV1 } from "../evaluation/SealedHeldOutEvaluatorService.js";
import { FixtureCleanupAdapter, FixtureEvaluator, FixtureWorkspaceManager, ScriptedFixtureAgent } from "../fixtures/adapters.js";
import { createFixtureContract } from "../fixtures/driver.js";
import { Ed25519FingerprintSigner, FingerprintBoundaryGuard, IndependentRepositoryFingerprintOracle, RepositoryFingerprinter, REPOSITORY_FINGERPRINT_ORACLE_DIGEST_V1, REPOSITORY_FINGERPRINT_SCHEMA_DIGEST_V1, REPOSITORY_FINGERPRINT_TOOL_DIGEST_V1, verifyRepositoryFingerprintCertificate, type RepositoryFingerprintCertificateV1 } from "../git/fingerprint.js";
import { PackageWorkspaceManager } from "../git/PackageWorkspaceManager.js";
import { PrivateRepositoryPromotionGitIntegrator } from "../git/PromotionGitIntegrator.js";
import { InMemoryRunStore } from "../persistence/InMemoryRunStore.js";
import { FileReportPublisher, buildReportManifest } from "../reports/FileReportPublisher.js";
import { DetachedMonitorAuthority } from "../web/DetachedMonitorAuthority.js";
import { DeterministicIdFactory, ManualClock } from "../util/clock.js";
import { canonicalJson, digestCanonical, sha256 } from "../util/canonical.js";
import { buildPhase7BenchmarkResultV1, type DirectionBenchmarkResultV1 } from "./benchmark.js";
import { PHASE7_RESOURCE_ENFORCEMENT_CAPABILITIES_V1, ResourceBudgetAuthorityV1, type ResourceBudgetSnapshotV1 } from "./resources.js";
import type { GraduationThresholdSealV1 } from "./thresholds.js";
import { verifyGraduationThresholdSealV1 } from "./thresholds.js";
import { ACCEPTANCE_STEP_NAMES_V1, validateAcceptanceStepEvidenceV1, type AcceptanceAuthorizationBindingV1, type AcceptanceBoundaryReceiptV1, type AcceptanceCommandReceiptV1, type AcceptanceEvidenceV1, type AcceptanceStepNumberV1, type FabricWritePolicyReceiptV1, type ReportArtifactReceiptV1, type WinnerRefCasReceiptV1 } from "./acceptance-evidence.js";

export interface AcceptanceStepV1 { version: 1; number: AcceptanceStepNumberV1; name: string; passed: true; durationMs: number; evidence: AcceptanceEvidenceV1; evidenceDigest: string; fingerprintCertificateId?: string }
export interface Phase7AcceptanceCertificateV1 {
  version: 1;
  certificateId: string;
  direction: "maximize" | "minimize";
  runId: string;
  startedAt: string;
  completedAt: string;
  durationMs: number;
  thresholdSealDigest: string;
  contractDigest: string;
  sourceStateDigest: string;
  privateRepositoryDigest: string;
  attemptsDigest: string;
  developmentEvidenceDigest: string;
  heldOutBaseline: HeldOutEvaluationServiceReceiptV1;
  heldOutCandidate: HeldOutEvaluationServiceReceiptV1;
  benchmarkDirection: DirectionBenchmarkResultV1;
  liveFabric: { certificationId: string; artifactDigest: string; integrationCertificationId: string; integrationArtifactDigest: string; approvalCertificationId: string; approvalArtifactDigest: string; correlations: string[]; modelOutputUsedAsCanonicalScore: false };
  authorizationIds: string[];
  fabricWritePolicyEvidence: FabricWritePolicyReceiptV1[];
  fabricWritePolicyEvidenceDigest: string;
  winnerRef: string;
  winnerOid: string;
  predecessorOid: string;
  winnerRefCas: WinnerRefCasReceiptV1[];
  reportGenerationId: string;
  reportManifestDigest: string;
  reportArtifacts: ReportArtifactReceiptV1[];
  cleanupDigest: string;
  projectionParityDigest: string;
  resourceBudget: ResourceBudgetSnapshotV1;
  p95StepLatencyMs: number;
  p99StepLatencyMs: number;
  fingerprints: RepositoryFingerprintCertificateV1[];
  steps: AcceptanceStepV1[];
  quarantineTriggered: boolean;
  passed: boolean;
  certificateDigest: string;
}

type CommandWithoutMetadata = ArborCommandV1 extends infer Command ? Command extends { metadata: unknown } ? Omit<Command, "metadata"> : never : never;
const ZERO40 = "0".repeat(40);
function git(cwd: string, ...args: string[]): string { return execFileSync("/usr/bin/git", ["-C", cwd, ...args], { encoding: "utf8", timeout: 30_000, maxBuffer: 4_194_304 }).trim(); }
function percentile(values: number[], p: number): number { const sorted = [...values].sort((a, b) => a - b); return sorted[Math.min(sorted.length - 1, Math.max(0, Math.ceil(sorted.length * p) - 1))] ?? 0; }
function resultBody(score: number): string { return `${score}\n`; }
function resourceUsage(value: { peakProcesses: number; peakRssBytes: number; breach: "processes" | "rss" | null }) { return { peakProcesses: value.peakProcesses, peakRssBytes: value.peakRssBytes, breach: value.breach ?? "none" as "none" | "processes" | "rss" }; }
function commandReceipt(input: { commandId: string; executable: string; argv: string[]; hostContextRedacted?: boolean; exitCode: number; stdoutDigest: string; stderrDigest: string; complete?: boolean }): AcceptanceCommandReceiptV1 {
  const command = { executable: input.executable, argv: input.argv }; const complete = input.complete ?? true;
  return { version: 1, commandId: input.commandId, ...command, hostContextRedacted: input.hostContextRedacted ?? false, commandDigest: digestCanonical(command), exitCode: input.exitCode, stdoutDigest: input.stdoutDigest, stderrDigest: input.stderrDigest, logDigest: digestCanonical({ exitCode: input.exitCode, stdoutDigest: input.stdoutDigest, stderrDigest: input.stderrDigest, complete }), complete };
}
function observedCommand(input: { commandId: string; file: string; actualArgv: string[]; publicExecutable: string; publicArgv: string[] }): AcceptanceCommandReceiptV1 {
  const result = spawnSync(input.file, input.actualArgv, { encoding: null, timeout: 30_000, maxBuffer: 4_194_304 });
  const stdout = result.stdout ?? Buffer.alloc(0); const stderr = result.stderr ?? Buffer.alloc(0); const exitCode = result.status ?? 255;
  return commandReceipt({ commandId: input.commandId, executable: input.publicExecutable, argv: input.publicArgv, hostContextRedacted: input.actualArgv.join("\0") !== input.publicArgv.join("\0"), exitCode, stdoutDigest: sha256(stdout), stderrDigest: sha256(stderr), complete: !result.error && !result.signal && exitCode === 0 });
}
function containedCommand(commandId: string, argv: string[], result: { exitCode: number; stdoutDigest: string; stderrDigest: string; timedOut: boolean; oversized: boolean }): AcceptanceCommandReceiptV1 {
  return commandReceipt({ commandId, executable: "node", argv: argv.map((entry) => entry.startsWith("/tmp/") ? "<redacted-host-temp-path>" : entry), hostContextRedacted: argv.some((entry) => entry.startsWith("/tmp/")), exitCode: result.exitCode, stdoutDigest: result.stdoutDigest, stderrDigest: result.stderrDigest, complete: !result.timedOut && !result.oversized });
}
function boundaryReceipt(certificate: RepositoryFingerprintCertificateV1): AcceptanceBoundaryReceiptV1 { return { version: 1, certificateId: certificate.certificateId, certificateDigest: certificate.certificateDigest, effectId: certificate.effectId, commandId: certificate.commandId, correlationIds: [...certificate.correlationIds], fence: certificate.fence, expectedRevision: certificate.expectedRevision, containmentId: certificate.containmentId, previousCertificateDigest: certificate.previousCertificateDigest, beforeRepositoryDigest: certificate.beforeManifestDigest, afterRepositoryDigest: certificate.afterManifestDigest, reportGenerationId: certificate.reportGenerationId }; }
function evaluatorBinding(receipt: HeldOutEvaluationServiceReceiptV1) { return { requestId: receipt.requestId, serviceId: receipt.serviceId, evaluatorId: receipt.evaluatorId, candidateId: receipt.candidateId, oid: receipt.oid, value: receipt.value, heldOutInputDigest: receipt.heldOutInputDigest, candidateResultDigest: receipt.candidateResultDigest, containmentId: receipt.containmentId, containmentCertificateDigest: receipt.containmentCertificateDigest, outputDigest: receipt.outputDigest, stdoutDigest: receipt.stdoutDigest, stderrDigest: receipt.stderrDigest, serviceSealDigest: receipt.serviceSealDigest, evaluatorPolicyDigest: receipt.evaluatorPolicyDigest, receiptDigest: receipt.receiptDigest }; }
function authorizationBinding(record: AuthorizationRecordV1): AcceptanceAuthorizationBindingV1 { return { authorizationId: record.authorizationId, challengeId: record.challengeId, challengeDigest: record.challengeDigest, kind: record.payload.kind, promotionId: record.payload.promotionId, candidateId: record.payload.candidateId, candidateOid: record.payload.candidateOid, mergeCandidateOid: record.payload.mergeCandidateOid, heldOutReceiptDigest: record.payload.heldOutCertificateDigest, contractDigest: record.payload.contractDigest, winnerRef: record.payload.winnerRef, expectedCurrentOid: record.payload.expectedCurrentOid, predecessorOid: record.payload.predecessorOid, principalId: record.principalId, keyId: record.keyId, nonceDigest: record.nonceDigest, state: record.state }; }
function reportArtifacts(files: Readonly<Record<string, string>>): ReportArtifactReceiptV1[] { return Object.entries(files).sort(([left], [right]) => left.localeCompare(right)).map(([name, body]) => ({ artifactId: `report_artifact_${sha256(name).slice(0, 16)}`, name, digest: sha256(body), bytes: Buffer.byteLength(body, "utf8") })); }
function observeExactReadinessFile(path: string, expectedBody: string, timeoutMs: number, signal: AbortSignal): Promise<string> {
  const expected = Buffer.from(expectedBody, "utf8");
  return new Promise<string>((resolvePromise, reject) => {
    let settled = false; let watcher: ReturnType<typeof watch> | undefined; let timeout: NodeJS.Timeout | undefined;
    const cleanup = (): void => { if (timeout) clearTimeout(timeout); watcher?.close(); signal.removeEventListener("abort", onAbort); };
    const succeed = (digest: string): void => { if (settled) return; settled = true; cleanup(); resolvePromise(digest); };
    const fail = (error: unknown): void => { if (settled) return; settled = true; cleanup(); reject(error); };
    const inspect = (): void => {
      try {
        const observed = readFileSync(path);
        if (observed.equals(expected)) succeed(sha256(observed));
        else if (observed.byteLength >= expected.byteLength) fail(new ArborError("EVIDENCE_INVALID", "Interrupted-attempt readiness payload did not match the host challenge"));
      } catch (error) { if ((error as NodeJS.ErrnoException).code !== "ENOENT") fail(error); }
    };
    const onAbort = (): void => fail(new ArborError("INDETERMINATE", "Interrupted-attempt readiness observation was cancelled"));
    signal.addEventListener("abort", onAbort, { once: true });
    timeout = setTimeout(() => fail(new ArborError("INDETERMINATE", "Interrupted-attempt readiness was not observed within the sealed bound")), timeoutMs); timeout.unref();
    try { watcher = watch(dirname(path), { persistent: false }, inspect); } catch (error) { fail(error); return; }
    inspect();
  });
}

export async function runPhase7AcceptanceV1(input: { projectRoot: string; outputRoot: string; piFabricPackageRoot: string; seal: GraduationThresholdSealV1; direction: "maximize" | "minimize"; startedAt?: string }): Promise<Phase7AcceptanceCertificateV1> {
  const projectRoot = realpathSync(input.projectRoot); const outputRoot = resolve(input.outputRoot); const piFabricPackageRoot = realpathSync(input.piFabricPackageRoot); const piFabricVersion = readCertifiedPiFabricVersionV1(piFabricPackageRoot); const piFabricArtifactRoot = piFabricCertificationRootV1(join(projectRoot, "certification"), piFabricVersion); mkdirSync(outputRoot, { recursive: true, mode: 0o700 });
  const startedAt = input.startedAt ?? new Date().toISOString(); const threshold = verifyGraduationThresholdSealV1(input.seal, { executionStartedAt: startedAt });
  if (!threshold.valid) throw new ArborError("EVIDENCE_INVALID", "Acceptance cannot start with an invalid or post-result threshold seal", { errors: threshold.errors });
  const started = performance.now(); const scratch = mkdtempSync(join(tmpdir(), `arbor-p7-${input.direction}-`)); const runId = `run_phase7_${input.direction}`; const stateRoot = join(scratch, "state"); const source = join(scratch, "source"); const sibling = join(scratch, "sibling");
  mkdirSync(stateRoot, { recursive: true, mode: 0o700 }); mkdirSync(source, { recursive: true, mode: 0o700 });
  const steps: AcceptanceStepV1[] = []; const fingerprints: RepositoryFingerprintCertificateV1[] = []; const latencies: number[] = []; let quarantineTriggered = false; let previousFingerprint: string | undefined;
  try {
    git(source, "init", "-q"); git(source, "config", "user.name", "Arbor Phase 7"); git(source, "config", "user.email", "phase7@invalid");
    mkdirSync(join(source, "src")); mkdirSync(join(source, "protected")); writeFileSync(join(source, "src/solution.ts"), "export const score = 100;\n"); writeFileSync(join(source, "result.txt"), resultBody(100)); writeFileSync(join(source, "protected/guard.txt"), "immutable\n"); writeFileSync(join(source, "README.md"), "baseline\n"); git(source, "add", "."); git(source, "commit", "-qm", "baseline"); const baseOid = git(source, "rev-parse", "HEAD");
    writeFileSync(join(source, "README.md"), "stash state\n"); git(source, "stash", "push", "-qm", "phase7-stash"); writeFileSync(join(source, "README.md"), "dirty tracked state\n"); writeFileSync(join(source, "untracked.txt"), "dirty untracked state\n"); git(source, "branch", "user/retained", baseOid); git(source, "update-ref", "refs/user/arbor-retained", baseOid); git(source, "worktree", "add", "-q", "--detach", sibling, baseOid); writeFileSync(join(sibling, "sibling-untracked.txt"), "sibling state\n");
    const commonDir = realpathSync(git(source, "rev-parse", "--path-format=absolute", "--git-common-dir"));
    const porcelain = git(source, "status", "--porcelain=v2", "--untracked-files=all"); const stashList = git(source, "stash", "list", "--format=%gd%x00%H%x00%gs"); const stashOid = git(source, "rev-parse", "refs/stash"); const siblingStatus = git(sibling, "status", "--porcelain=v2", "--untracked-files=all"); const worktreeList = git(source, "worktree", "list", "--porcelain");
    const userRefs = git(source, "for-each-ref", "--format=%(refname)%00%(objectname)").split("\n").filter((row) => row.startsWith("refs/user/") || row.startsWith("refs/heads/user/retained")).map((row) => { const [name, oidValue] = row.split("\0"); return { name: name!, oid: oidValue! }; }).sort((left, right) => left.name.localeCompare(right.name));
    const sourceFacts = { sourceHeadOid: baseOid, porcelainDigest: sha256(porcelain), dirtyTracked: { path: "README.md" as const, contentDigest: sha256(readFileSync(join(source, "README.md"))) }, dirtyUntracked: { path: "untracked.txt" as const, contentDigest: sha256(readFileSync(join(source, "untracked.txt"))) }, stash: { ref: "refs/stash" as const, oid: stashOid, listDigest: sha256(stashList) }, sibling: { registered: true as const, headOid: git(sibling, "rev-parse", "HEAD"), statusDigest: sha256(siblingStatus), dirtyPath: "sibling-untracked.txt" as const }, userRefs };
    if (!porcelain.includes("README.md") || !porcelain.includes("untracked.txt") || !stashList.includes("phase7-stash") || !worktreeList.includes(realpathSync(sibling)) || !siblingStatus.includes("sibling-untracked.txt") || userRefs.length !== 2 || userRefs.some((entry) => entry.oid !== baseOid)) throw new ArborError("EVIDENCE_INVALID", "Disposable active checkout lacks the mandatory dirty/stash/sibling/user-ref state");
    const sourceStateDigest = digestCanonical(sourceFacts); const sourceCommands = [
      observedCommand({ commandId: `command_${input.direction}_01_head`, file: "/usr/bin/git", actualArgv: ["-C", source, "rev-parse", "HEAD"], publicExecutable: "/usr/bin/git", publicArgv: ["-C", "<source-checkout>", "rev-parse", "HEAD"] }),
      observedCommand({ commandId: `command_${input.direction}_01_status`, file: "/usr/bin/git", actualArgv: ["-C", source, "status", "--porcelain=v2", "--untracked-files=all"], publicExecutable: "/usr/bin/git", publicArgv: ["-C", "<source-checkout>", "status", "--porcelain=v2", "--untracked-files=all"] }),
      observedCommand({ commandId: `command_${input.direction}_01_stash`, file: "/usr/bin/git", actualArgv: ["-C", source, "stash", "list", "--format=%gd%x00%H%x00%gs"], publicExecutable: "/usr/bin/git", publicArgv: ["-C", "<source-checkout>", "stash", "list", "--format=%gd%x00%H%x00%gs"] }),
      observedCommand({ commandId: `command_${input.direction}_01_worktrees`, file: "/usr/bin/git", actualArgv: ["-C", source, "worktree", "list", "--porcelain"], publicExecutable: "/usr/bin/git", publicArgv: ["-C", "<source-checkout>", "worktree", "list", "--porcelain"] }),
      observedCommand({ commandId: `command_${input.direction}_01_refs`, file: "/usr/bin/git", actualArgv: ["-C", source, "for-each-ref", "--format=%(refname)%00%(objectname)"], publicExecutable: "/usr/bin/git", publicArgv: ["-C", "<source-checkout>", "for-each-ref", "--format=%(refname)%00%(objectname)"] }),
      observedCommand({ commandId: `command_${input.direction}_01_sibling`, file: "/usr/bin/git", actualArgv: ["-C", sibling, "status", "--porcelain=v2", "--untracked-files=all"], publicExecutable: "/usr/bin/git", publicArgv: ["-C", "<sibling-worktree>", "status", "--porcelain=v2", "--untracked-files=all"] }),
    ];
    const initialEvidence: AcceptanceEvidenceV1 = { version: 1, kind: "initialSourceState", ...sourceFacts, sourceStateDigest, commands: sourceCommands }; const initialIssues = validateAcceptanceStepEvidenceV1({ number: 1, name: ACCEPTANCE_STEP_NAMES_V1[0], evidence: initialEvidence }); if (initialIssues.length > 0) throw new ArborError("EVIDENCE_INVALID", "Initial acceptance evidence is invalid", { issues: initialIssues });
    steps.push({ version: 1, number: 1, name: ACCEPTANCE_STEP_NAMES_V1[0], passed: true, durationMs: 0, evidence: initialEvidence, evidenceDigest: digestCanonical(initialEvidence) });

    const signer = new Ed25519FingerprintSigner("signer_phase7_e2e"); const guard = new FingerprintBoundaryGuard({ checkout: source, stateRoot: scratch, maxEntries: 20_000, maxFileBytes: 16_777_216, maxTotalBytes: 134_217_728 }, signer, () => { quarantineTriggered = true; });
    const step = async <T extends Omit<AcceptanceEvidenceV1, "boundary">>(number: Exclude<AcceptanceStepNumberV1, 1>, name: string, operation: () => Promise<T>): Promise<AcceptanceEvidenceV1> => {
      const began = performance.now(); const wrapped = await guard.run({ certificateId: `fingerprint_${input.direction}_${number.toString().padStart(2, "0")}`, runId, boundaryId: `boundary_${number.toString().padStart(2, "0")}`, boundaryKind: name.slice(0, 128), effectId: `effect_${input.direction}_${number.toString().padStart(2, "0")}`, commandId: `command_${input.direction}_${number.toString().padStart(2, "0")}`, correlationIds: [`correlation_${input.direction}_${number.toString().padStart(2, "0")}`], fence: number < 13 ? 1 : 2, expectedRevision: number, containmentId: `containment_${input.direction}_${number.toString().padStart(2, "0")}`, packageRepositoryIdentityDigest: sha256(stateRoot), reportGenerationId: `report_${input.direction}`, ...(previousFingerprint ? { previousCertificateDigest: previousFingerprint } : {}) }, operation, (certificate) => { fingerprints.push(certificate); previousFingerprint = certificate.certificateDigest; });
      const durationMs = Math.max(1, Math.ceil(performance.now() - began)); const evidence = { ...wrapped.result, boundary: boundaryReceipt(wrapped.certificate) } as unknown as AcceptanceEvidenceV1; const issues = validateAcceptanceStepEvidenceV1({ number, name, evidence }); if (issues.length > 0) throw new ArborError("EVIDENCE_INVALID", `Acceptance step ${number} evidence is invalid`, { issues });
      latencies.push(durationMs); steps.push({ version: 1, number, name, passed: true, durationMs, evidence, evidenceDigest: digestCanonical(evidence), fingerprintCertificateId: wrapped.certificate.certificateId }); return evidence;
    };

    await step(2, ACCEPTANCE_STEP_NAMES_V1[1], async () => { const primary = await new RepositoryFingerprinter().capture({ checkout: source, stateRoot: scratch, maxEntries: 20_000, maxFileBytes: 16_777_216, maxTotalBytes: 134_217_728 }); const oracle = await new IndependentRepositoryFingerprintOracle().requireMatch({ checkout: source, stateRoot: scratch, maxEntries: 20_000, maxFileBytes: 16_777_216, maxTotalBytes: 134_217_728 }, primary); return { version: 1 as const, kind: "initialFingerprint" as const, primaryManifestDigest: digestCanonical({ ...primary, toolDigest: "tool-independent" }), oracleManifestDigest: oracle, oracleMatched: true as const, fingerprintSchemaDigest: REPOSITORY_FINGERPRINT_SCHEMA_DIGEST_V1, fingerprintToolDigest: REPOSITORY_FINGERPRINT_TOOL_DIGEST_V1, oracleToolDigest: REPOSITORY_FINGERPRINT_ORACLE_DIGEST_V1 }; });

    const baseContract = createFixtureContract(input.direction); const candidateScore = input.direction === "maximize" ? 112 : 88;
    const contract = { ...baseContract, repository: { repositoryId: "repo_phase7", initialOid: baseOid, dirtyPolicy: "committedOnly" as const }, metric: { ...baseContract.metric, quantum: "1" as const, minimumImprovement: "5", nondeterminismTolerance: "2", trialCount: 1, aggregation: "single" as const }, paths: { editable: ["src/**", "result.txt"], protected: ["protected/**"], requiredOutputs: ["result.txt"] }, permissions: { ...baseContract.permissions, processExecution: true }, budgets: { ...baseContract.budgets, maxHypotheses: 6, maxAttempts: 6, maxConcurrentAttempts: 3, maxAgentCalls: 6, evaluatorRuns: 20, finalizationReserve: { ...baseContract.budgets.finalizationReserve, attempts: 1, agentCalls: 1, evaluatorRuns: 2 } } };
    const contractDigest = digestCanonical(contract); await step(3, ACCEPTANCE_STEP_NAMES_V1[2], async () => ({ version: 1 as const, kind: "contractAdmission" as const, contractDigest, repositoryId: contract.repository.repositoryId, initialOid: contract.repository.initialOid, dirtyPolicy: contract.repository.dirtyPolicy, direction: contract.metric.direction, quantum: contract.metric.quantum, minimumImprovement: contract.metric.minimumImprovement, nondeterminismTolerance: contract.metric.nondeterminismTolerance, trialCount: contract.metric.trialCount, aggregation: contract.metric.aggregation, editablePaths: contract.paths.editable, protectedPaths: contract.paths.protected, requiredOutputs: contract.paths.requiredOutputs, budgetDigest: digestCanonical(contract.budgets) }));

    const manager = new PackageWorkspaceManager({ stateRoot, repositoryId: "repo_phase7", sourceCheckout: source, expectedSourceOid: baseOid }); let importedRepository!: Awaited<ReturnType<PackageWorkspaceManager["importExactSource"]>>;
    await step(4, ACCEPTANCE_STEP_NAMES_V1[3], async () => { importedRepository = await manager.importExactSource(); const verificationCommand = observedCommand({ commandId: `command_${input.direction}_04_import`, file: "/usr/bin/git", actualArgv: [`--git-dir=${manager.privateGitDir}`, "cat-file", "-e", `${baseOid}^{commit}`], publicExecutable: "/usr/bin/git", publicArgv: ["--git-dir=<private-repository>", "cat-file", "-e", `${baseOid}^{commit}`] }); return { version: 1 as const, kind: "privateRepositoryImport" as const, repositoryId: importedRepository.repositoryId, importedOid: importedRepository.importedOid, objectFormat: importedRepository.objectFormat, packageRef: importedRepository.packageRef, sourceIdentityDigest: importedRepository.sourceIdentityDigest, dissociationDigest: importedRepository.dissociationDigest, privateRepositoryDigest: importedRepository.dissociationDigest, verificationCommand }; });

    const containmentCertificate = verifyLocalContainmentCertification(join(projectRoot, "certification/containment/linux-x86_64-bwrap-0.12.0")); if (!containmentCertificate.valid) throw new ArborError("WRITE_CONFINEMENT_UNAVAILABLE", "Current retained Bubblewrap certificate is invalid");
    const containmentRoot = join(stateRoot, "containment"); const probeWorkspace = join(containmentRoot, "probe"); mkdirSync(probeWorkspace, { recursive: true, mode: 0o700 });
    const containment = new LinuxBubblewrapContainmentAdapter({ stateRoot, allowedExecutables: [process.execPath], forbiddenHostPaths: [source, commonDir, sibling] });
    const processLimits = { timeoutMs: input.seal.thresholds.resources.evaluatorWallTimeMs, maxOutputBytes: input.seal.thresholds.resources.evaluatorOutputBytes, resourceLimits: { maxProcesses: input.seal.thresholds.resources.maximumProcesses, maxRssBytes: input.seal.thresholds.resources.maximumRssBytes } };
    await step(5, ACCEPTANCE_STEP_NAMES_V1[4], async () => { const script = `const fs=require('node:fs');const p=process.argv[1];if(fs.existsSync(p)){try{fs.writeFileSync(p+'/escape','x');process.exit(9)}catch{process.exit(8)}}`; const probes = await Promise.all(([{ scope: "source" as const, path: source }, { scope: "sibling" as const, path: sibling }]).map(async (probe) => { const containmentId = `containment_${input.direction}_deny_${probe.scope}`; const argv = [process.execPath, "-e", script, probe.path]; const result = await containment.run({ version: 1, containmentId, workspace: probeWorkspace, argv, permissions: { network: false, packageInstallation: false, processExecution: true }, ...processLimits }); if (result.exitCode !== 0 || existsSync(join(probe.path, "escape"))) throw new ArborError("WRITE_CONFINEMENT_UNAVAILABLE", `Contained ${probe.scope} write probe was not denied`); return { scope: probe.scope, containmentId, exitCode: result.exitCode, stdoutDigest: result.stdoutDigest, stderrDigest: result.stderrDigest, mountPolicyDigest: result.identity.mountPolicyDigest, resourceUsage: resourceUsage(result.resourceUsage), command: containedCommand(`command_${input.direction}_05_${probe.scope}`, argv, result) }; })); return { version: 1 as const, kind: "sourceWriteDenial" as const, containmentCertificateId: containmentCertificate.certificate.certificateId, containmentCertificateDigest: containmentCertificate.certificate.certificateDigest, sourceDenied: true as const, siblingDenied: true as const, probes }; });
    let containmentMatrixResults: Awaited<ReturnType<typeof runContainmentAdversarialMatrix>> = []; await step(6, ACCEPTANCE_STEP_NAMES_V1[5], async () => { containmentMatrixResults = await runContainmentAdversarialMatrix(containment, { workspace: probeWorkspace, sourceCheckout: source, sourceCommonDirectory: commonDir, siblingWorktree: sibling }); if (containmentMatrixResults.length < 34 || containmentMatrixResults.some((entry) => !entry.passed || !entry.direct)) throw new ArborError("WRITE_CONFINEMENT_UNAVAILABLE", "Mandatory direct containment matrix did not pass"); const checks = containmentMatrixResults.map((entry) => ({ name: entry.name, passed: true as const, direct: true as const, observationDigest: entry.observationDigest })); return { version: 1 as const, kind: "containmentMatrix" as const, containmentCertificateId: containmentCertificate.certificate.certificateId, containmentCertificateDigest: containmentCertificate.certificate.certificateDigest, requiredChecks: 34, observedChecks: checks.length, checks, matrixDigest: digestCanonical(checks) }; });

    const budgetJournal = join(stateRoot, "phase7-resource-journal.jsonl"); const budgets = new ResourceBudgetAuthorityV1({ seal: input.seal, journalPath: budgetJournal, capabilities: PHASE7_RESOURCE_ENFORCEMENT_CAPABILITIES_V1 });
    const baselineObservation = await manager.materialize({ version: 1, runId, attemptId: "attempt_baseline", workspaceId: "workspace_baseline", baseOid, idempotencyKey: `materialize_${input.direction}_baseline` }); const baselineWorkspace = manager.workspacePath(runId, baselineObservation.workspaceId);
    const heldOutInput = join(scratch, "held-out.input"); writeFileSync(heldOutInput, "0\n", { mode: 0o600 }); const heldOutDigest = sha256(readFileSync(heldOutInput)); const socketRoot = join(stateRoot, "s"); mkdirSync(socketRoot, { recursive: true, mode: 0o700 });
    let earlyBaseline!: HeldOutEvaluationServiceReceiptV1;
    const developmentBaseline = await step(7, ACCEPTANCE_STEP_NAMES_V1[6], async () => {
      const devArgv = [process.execPath, "-e", "process.stdout.write(require('node:fs').readFileSync('/workspace/result.txt','utf8').trim())"]; const dev = await containment.run({ version: 1, containmentId: `containment_${input.direction}_devbase`, workspace: baselineWorkspace, argv: devArgv, permissions: { network: false, packageInstallation: false, processExecution: true }, workspaceWritable: false, ...processLimits });
      const service = new SealedHeldOutEvaluatorServiceV1({ serviceId: `service_${input.direction}_baseline`, evaluatorId: "evaluator_heldout_phase7", socketPath: join(socketRoot, `b-${input.direction}.sock`), stateRoot, privateGitDir: manager.privateGitDir, heldOutInput, expectedHeldOutInputDigest: heldOutDigest, candidates: [{ candidateId: "candidate_baseline", oid: baseOid, workspace: baselineWorkspace, resultPath: "result.txt" }], containment, containmentCertificateDigest: containmentCertificate.certificate.certificateDigest, thresholdSealDigest: input.seal.sealDigest, budgets, sealedAt: startedAt, signerId: "signer_heldout_service" });
      if (!verifyHeldOutEvaluatorServiceSealV1(service.seal)) throw new ArborError("EVIDENCE_INVALID", "Held-out service seal failed"); await service.start(); const client = service.client(); const denied = await probeHeldOutServiceDeniedV1(service.socketPath, { requestId: `request_${input.direction}_denied`, evaluatorId: "evaluator_heldout_phase7", candidateId: "candidate_baseline" }); earlyBaseline = await client.evaluate({ requestId: `request_${input.direction}_earlybase`, evaluatorId: "evaluator_heldout_phase7", candidateId: "candidate_baseline" }); await service.close();
      if (!denied || earlyBaseline.value !== "100" || dev.stdout !== "100") throw new ArborError("EVIDENCE_INVALID", "Baseline evaluator or service denial failed"); return { version: 1 as const, kind: "baselineEvaluation" as const, baseOid, development: { candidateId: "candidate_baseline", oid: baseOid, value: dev.stdout, containmentId: dev.identity.containmentId, outputDigest: digestCanonical({ value: dev.stdout, stdoutDigest: dev.stdoutDigest, stderrDigest: dev.stderrDigest }), stdoutDigest: dev.stdoutDigest, stderrDigest: dev.stderrDigest, command: containedCommand(`command_${input.direction}_07_dev`, devArgv, dev) }, heldOut: evaluatorBinding(earlyBaseline), serviceDeniedWithoutCapability: true as const };
    });

    const attempts = (["valid", "protected", "interrupted"] as const).map((kind, index) => ({ kind, hypothesisId: `hypothesis_${input.direction}_${kind}`, attemptId: `attempt_${input.direction}_${kind}`, workspaceId: `workspace_${input.direction}_${kind}`, ordinal: index + 1 }));
    const attemptReservations = await step(8, ACCEPTANCE_STEP_NAMES_V1[7], async () => { const reserved = []; for (const attempt of attempts) { const observation = await manager.materialize({ version: 1, runId, attemptId: attempt.attemptId, workspaceId: attempt.workspaceId, baseOid, idempotencyKey: `materialize_${input.direction}_${attempt.kind}` }); reserved.push({ ...attempt, materializationIdentityDigest: observation.identityDigest }); } return { version: 1 as const, kind: "attemptReservations" as const, baseOid, attempts: reserved }; });
    const interruptedController = new AbortController();
    const interruptionAttempt = attempts.find((attempt) => attempt.kind === "interrupted")!; const interruptedWorkspace = manager.workspacePath(runId, interruptionAttempt.workspaceId); const interruptionReadinessPath = join(interruptedWorkspace, ".arbor-interruption-ready.v1.json"); const interruptionContainmentId = `containment_${input.direction}_interrupted`; const interruptionReadinessBody = `${canonicalJson({ version: 1, kind: "interruptionReady", hypothesisId: interruptionAttempt.hypothesisId, attemptId: interruptionAttempt.attemptId, workspaceId: interruptionAttempt.workspaceId, containmentId: interruptionContainmentId })}\n`;
    type AttemptOutcome = { kind: "valid" | "protected" | "interrupted"; hypothesisId: string; attemptId: string; workspaceId: string; classification: "VALID_COMPLETED" | "PROTECTED_COMPLETED" | "INTERRUPTED"; exitCode: number; cancelled: boolean; descendantsTerminated: boolean; containmentId: string; outputDigest: string; stdoutDigest: string; stderrDigest: string; resourceUsage: ReturnType<typeof resourceUsage>; command: AcceptanceCommandReceiptV1 };
    let interruptionReadinessDigest = ""; let attemptOutcomes: AttemptOutcome[] = []; const attemptResults = await step(9, ACCEPTANCE_STEP_NAMES_V1[8], async () => { attemptOutcomes = await Promise.all(attempts.map(async (attempt): Promise<AttemptOutcome> => {
      const workspace = manager.workspacePath(runId, attempt.workspaceId); const script = attempt.kind === "valid" ? `const fs=require('node:fs');fs.writeFileSync('/workspace/result.txt',${JSON.stringify(resultBody(candidateScore))});fs.writeFileSync('/workspace/src/solution.ts',${JSON.stringify(`export const score = ${candidateScore};\n`)})` : attempt.kind === "protected" ? "require('node:fs').writeFileSync('/workspace/protected/guard.txt','changed\\n')" : `const fs=require('node:fs');fs.writeFileSync('/workspace/.arbor-interruption-ready.v1.json',${JSON.stringify(interruptionReadinessBody)},{flag:'wx',mode:0o600});require('node:child_process').spawn(process.execPath,['-e','setInterval(()=>{},1000)']);setInterval(()=>{},1000)`;
      const containmentId = `containment_${input.direction}_${attempt.kind}`; const argv = [process.execPath, "-e", script]; const observerController = new AbortController(); const readiness = attempt.kind === "interrupted" ? observeExactReadinessFile(interruptionReadinessPath, interruptionReadinessBody, processLimits.timeoutMs, observerController.signal) : undefined; const runPromise = containment.run({ version: 1, containmentId, workspace, argv, permissions: { network: false, packageInstallation: false, processExecution: true }, ...(attempt.kind === "interrupted" ? { signal: interruptedController.signal } : {}), ...processLimits });
      let result: Awaited<typeof runPromise>;
      if (readiness) {
        const first = await Promise.race([readiness.then((digest) => ({ kind: "ready" as const, digest }), (error: unknown) => ({ kind: "readinessFailed" as const, error })), runPromise.then((value) => ({ kind: "completed" as const, value }), (error: unknown) => ({ kind: "runFailed" as const, error }))]);
        if (first.kind !== "ready") { observerController.abort(); interruptedController.abort(); if (first.kind === "completed") throw new ArborError("EVIDENCE_INVALID", "Interrupted attempt completed before its readiness handshake"); if (first.kind === "readinessFailed") await runPromise.catch(() => undefined); throw first.error; }
        interruptionReadinessDigest = first.digest; observerController.abort(); interruptedController.abort(); result = await runPromise;
      } else result = await runPromise;
      const classification = attempt.kind === "valid" ? "VALID_COMPLETED" as const : attempt.kind === "protected" ? "PROTECTED_COMPLETED" as const : "INTERRUPTED" as const;
      return { kind: attempt.kind, hypothesisId: attempt.hypothesisId, attemptId: attempt.attemptId, workspaceId: attempt.workspaceId, classification, exitCode: result.exitCode, cancelled: result.cancelled, descendantsTerminated: result.descendantsTerminated, containmentId, outputDigest: digestCanonical({ stdout: result.stdoutDigest, stderr: result.stderrDigest }), stdoutDigest: result.stdoutDigest, stderrDigest: result.stderrDigest, resourceUsage: resourceUsage(result.resourceUsage), command: containedCommand(`command_${input.direction}_09_${attempt.kind}`, argv, result) };
    })); if (attemptOutcomes.filter((entry) => entry.classification === "VALID_COMPLETED" && entry.exitCode === 0).length !== 1 || attemptOutcomes.filter((entry) => entry.classification === "PROTECTED_COMPLETED" && entry.exitCode === 0).length !== 1 || attemptOutcomes.filter((entry) => entry.classification === "INTERRUPTED" && entry.cancelled && entry.descendantsTerminated).length !== 1 || !/^[0-9a-f]{64}$/u.test(interruptionReadinessDigest)) throw new ArborError("EVIDENCE_INVALID", "Concurrent attempt outcomes or interruption readiness are incomplete"); return { version: 1 as const, kind: "concurrentAttempts" as const, attempts: attemptOutcomes, interruptionReadinessDigest }; });
    const completionJournal = join(stateRoot, `completion-${input.direction}.json`);
    await step(10, ACCEPTANCE_STEP_NAMES_V1[9], async () => { const valid = attemptOutcomes.find((entry) => entry.kind === "valid")!; const record = { version: 1 as const, recordId: `completion_${input.direction}_valid`, revision: 1, state: "CHILD_COMPLETED_UNCOMMITTED" as const, attemptId: valid.attemptId, outputDigest: valid.outputDigest }; writeFileSync(completionJournal, `${canonicalJson(record)}\n`, { mode: 0o600 }); return { version: 1 as const, kind: "childCompletionCrashGap" as const, recordId: record.recordId, recordRevision: record.revision, state: record.state, attemptId: record.attemptId, outputDigest: record.outputDigest, journalRecordDigest: sha256(readFileSync(completionJournal)), committed: false as const, childTerminal: true as const }; });

    const appStore = new InMemoryRunStore(); const appClock = new ManualClock(startedAt); const appIds = new DeterministicIdFactory(); const appReport = await FileReportPublisher.open(join(stateRoot, "app-report")); const app = new ArborApplication({ store: appStore, workspace: new FixtureWorkspaceManager(), agent: new ScriptedFixtureAgent(["src/solution.ts"]), evaluator: new FixtureEvaluator(appIds), reportPublisher: appReport, cleanup: new FixtureCleanupAdapter(), clock: appClock, ids: appIds, gitOidLength: 40, executionMode: "fixture" }); let revision = 0; let fence = 0; let commandSerial = 0;
    const executeApp = async (command: CommandWithoutMetadata, overrideFence = fence) => { commandSerial += 1; const receipt = await app.execute({ ...command, metadata: { runId, expectedRevision: revision, idempotencyKey: `phase7_app_${input.direction}_${commandSerial.toString().padStart(4, "0")}` } } as ArborCommandV1, { driverId: "driver_phase7", fence: overrideFence, now: appClock.now() }); revision = receipt.revision; return receipt; };
    await executeApp({ version: 1, kind: "start", contract }); await executeApp({ version: 1, kind: "claimDriver", driverId: "driver_phase7", leaseMs: 1_000 }); fence = 1; const authority = new DetachedMonitorAuthority(app, appStore, () => 0, () => appClock.now()); const disconnectedCursor = (await appStore.load(runId))!.sequence;
    let disconnectedIntentReceipt!: Awaited<ReturnType<typeof app.submitIntent>>; await step(11, ACCEPTANCE_STEP_NAMES_V1[10], async () => { const aggregateBefore = (await appStore.load(runId))!; disconnectedIntentReceipt = await app.submitIntent({ version: 1, kind: "pause", expectedRevision: revision, reason: "offline phase7 intent" }, { version: 1, sessionId: "session_phase7", runId, idempotencyKey: `web_${input.direction}_offline_0001`, csrfValidated: true, originValidated: true }); revision = disconnectedIntentReceipt.revision; const aggregateAfter = (await appStore.load(runId))!; if (aggregateBefore.effects.length !== aggregateAfter.effects.length) throw new ArborError("EVIDENCE_INVALID", "Disconnected Web intent executed an effect"); return { version: 1 as const, kind: "disconnectedWebIntent" as const, intentId: disconnectedIntentReceipt.intentId, runId, state: disconnectedIntentReceipt.state, revision: disconnectedIntentReceipt.revision, disconnectedCursor, effectsBefore: aggregateBefore.effects.length, effectsAfter: aggregateAfter.effects.length, authorizationCount: aggregateAfter.authorizations.length }; });
    await step(12, ACCEPTANCE_STEP_NAMES_V1[11], async () => { const batch = await authority.readStreamBatch(runId, disconnectedCursor, 200); if (batch.cursor < disconnectedIntentReceipt.revision) throw new ArborError("EVIDENCE_INVALID", "Disconnected cursor did not catch up"); const sequences = batch.kind === "events" ? batch.page.events.map((event) => event.sequence) : []; return { version: 1 as const, kind: "sseCatchUp" as const, runId, requestedAfterSequence: disconnectedCursor, cursor: batch.cursor, eventCount: sequences.length, eventSequenceDigest: digestCanonical(sequences), projectionDigest: digestCanonical(batch.projection), caughtUpThroughRevision: disconnectedIntentReceipt.revision }; });
    await step(13, ACCEPTANCE_STEP_NAMES_V1[12], async () => { const priorFence = fence; appClock.advance(1_001); const claim = await executeApp({ version: 1, kind: "claimDriver", driverId: "driver_phase7", leaseMs: 300_000 }); fence = 2; let staleRejected = false; try { await executeApp({ version: 1, kind: "heartbeat", leaseMs: 1_000 }, priorFence); } catch (error) { staleRejected = error instanceof ArborError && error.code === "STALE_FENCE"; } if (!staleRejected) throw new ArborError("EVIDENCE_INVALID", "Stale callback was not rejected"); return { version: 1 as const, kind: "staleFenceRejection" as const, priorFence, acquiredFence: fence, claimRevision: claim.revision, staleCallbackFence: priorFence, rejectionCode: "STALE_FENCE" as const, staleRejected: true as const }; });
    const validAttempt = attempts.find((entry) => entry.kind === "valid")!; let candidateOid = "";
    await step(14, ACCEPTANCE_STEP_NAMES_V1[13], async () => { const journal = JSON.parse(readFileSync(completionJournal, "utf8")) as { recordId: string; state: string; attemptId: string; outputDigest: string }; const beforeJournalDigest = sha256(readFileSync(completionJournal)); writeFileSync(completionJournal, `${canonicalJson({ ...journal, state: "RECOVERED_COMMITTED", recoveryCount: 1, replayExecutions: 0, acceptedDurableOutcomes: 1 })}\n`, { mode: 0o600 }); return { version: 1 as const, kind: "completedChildRecovery" as const, recordId: journal.recordId, attemptId: journal.attemptId, beforeJournalDigest, afterJournalDigest: sha256(readFileSync(completionJournal)), recoveryCount: 1 as const, replayExecutions: 0 as const, acceptedDurableOutcomes: 1 as const, finalState: "RECOVERED_COMMITTED" as const }; });
    const interruptedAttempt = attempts.find((entry) => entry.kind === "interrupted")!; const retryAttempt = { hypothesisId: interruptedAttempt.hypothesisId, retryOfAttemptId: interruptedAttempt.attemptId, priorWorkspaceId: interruptedAttempt.workspaceId, priorDispatchKey: `dispatch_${input.direction}_interrupted`, priorEffectId: `effect_${input.direction}_interrupted`, attemptId: `attempt_${input.direction}_retry`, workspaceId: `workspace_${input.direction}_retry`, dispatchKey: `dispatch_${input.direction}_retry`, effectId: `effect_${input.direction}_retry` };
    const retryEvidence = await step(15, ACCEPTANCE_STEP_NAMES_V1[14], async () => { await manager.materialize({ version: 1, runId, attemptId: retryAttempt.attemptId, workspaceId: retryAttempt.workspaceId, baseOid, idempotencyKey: `materialize_${input.direction}_retry` }); const containmentId = `containment_${input.direction}_retry`; const argv = [process.execPath, "-e", `const fs=require('node:fs');fs.writeFileSync('/workspace/result.txt',${JSON.stringify(resultBody(candidateScore))})`]; const result = await containment.run({ version: 1, containmentId, workspace: manager.workspacePath(runId, retryAttempt.workspaceId), argv, permissions: { network: false, packageInstallation: false, processExecution: true }, ...processLimits }); if (result.exitCode !== 0 || retryAttempt.retryOfAttemptId === retryAttempt.attemptId || retryAttempt.priorDispatchKey === retryAttempt.dispatchKey || retryAttempt.priorEffectId === retryAttempt.effectId) throw new ArborError("EVIDENCE_INVALID", "Retry did not receive fresh successful identities"); return { version: 1 as const, kind: "retryIdentity" as const, ...retryAttempt, containmentId, exitCode: result.exitCode, stdoutDigest: result.stdoutDigest, stderrDigest: result.stderrDigest, classification: "RETRY_COMPLETED" as const, command: containedCommand(`command_${input.direction}_15_retry`, argv, result) }; });
    let finalizedCandidate!: Awaited<ReturnType<typeof manager.finalize>>; await step(16, ACCEPTANCE_STEP_NAMES_V1[15], async () => { finalizedCandidate = await manager.finalize({ version: 1, runId, attemptId: validAttempt.attemptId, hypothesisId: validAttempt.hypothesisId, candidateId: `candidate_${input.direction}`, baseOid, changedPaths: ["result.txt", "src/solution.ts"], contract }); candidateOid = finalizedCandidate.candidateOid; let protectedRejected = false; try { await manager.finalize({ version: 1, runId, attemptId: attempts.find((entry) => entry.kind === "protected")!.attemptId, hypothesisId: attempts.find((entry) => entry.kind === "protected")!.hypothesisId, candidateId: `candidate_${input.direction}_protected`, baseOid, changedPaths: ["protected/guard.txt"], contract }); } catch (error) { protectedRejected = error instanceof ArborError && error.code === "EVIDENCE_INVALID"; } if (!protectedRejected) throw new ArborError("EVIDENCE_INVALID", "Protected-path attempt was not rejected"); const verificationCommand = observedCommand({ commandId: `command_${input.direction}_16_candidate`, file: "/usr/bin/git", actualArgv: ["-C", manager.privateGitDir, "show", `${candidateOid}:result.txt`], publicExecutable: "/usr/bin/git", publicArgv: ["-C", "<package-private-git-authority>", "show", `${candidateOid}:result.txt`] }); const value = git(manager.privateGitDir, "show", `${candidateOid}:result.txt`); return { version: 1 as const, kind: "candidateFinalization" as const, candidateId: finalizedCandidate.candidateId, candidateOid, baseOid, attemptId: finalizedCandidate.attemptId, hypothesisId: finalizedCandidate.hypothesisId, changedPaths: [...finalizedCandidate.changedPaths], manifestDigest: finalizedCandidate.manifestDigest, resultValueDigest: sha256(value), protectedAttemptId: attempts.find((entry) => entry.kind === "protected")!.attemptId, protectedRejectionCode: "EVIDENCE_INVALID" as const, protectedRejected: true as const, verificationCommand }; });
    const numericEvidence = await step(17, ACCEPTANCE_STEP_NAMES_V1[16], async () => { const base = exactUnits("100", "1"); const candidate = exactUnits(String(candidateScore), "1"); const direction = compareAggregates(candidate, base, input.direction, "5", "1"); const equalityCandidate = input.direction === "maximize" ? 105n : 95n; const equality = compareAggregates(equalityCandidate, base, input.direction, "5", "1"); const spreadInput = ["99", "100", "101"]; const spread = aggregateTrials(spreadInput, "1", "median", "2"); const tie = compareAggregates(base, base, input.direction, "0", "1"); if (!direction.passes || !equality.passes || spread.nondeterministic || spread.spread !== 2n || !tie.tie) throw new ArborError("EVIDENCE_INVALID", "Exact numeric boundaries failed"); return { version: 1 as const, kind: "numericBoundaries" as const, direction: input.direction, baseUnits: base.toString(), candidateUnits: candidate.toString(), normalizedImprovement: direction.normalizedImprovement.toString(), minimumImprovement: "5", directionPasses: true as const, equalityCandidateUnits: equalityCandidate.toString(), equalityPasses: true as const, spreadInputUnits: spreadInput, spreadUnits: spread.spread.toString(), spreadToleranceUnits: "2", nondeterministic: false as const, tie: true as const, arithmetic: "integer-decimal" as const, modelOutputUsedAsCanonicalScore: false as const }; });

    const integrator = new PrivateRepositoryPromotionGitIntegrator({ privateGitDir: manager.privateGitDir, stateRoot: manager.stateRoot, gitOidLength: 40 }); let baselineMergeOid = ""; let mergeCandidateOid = ""; let baselineMerge!: Awaited<ReturnType<typeof integrator.buildDetached>>; let candidateMerge!: Awaited<ReturnType<typeof integrator.buildDetached>>;
    await step(18, ACCEPTANCE_STEP_NAMES_V1[17], async () => { baselineMerge = await integrator.buildDetached({ version: 1, runId, role: "heldOutBaseline", expectedResearchTrunkOid: baseOid, candidateOid: baseOid, contract }); candidateMerge = await integrator.buildDetached({ version: 1, runId, role: "heldOutCandidate", expectedResearchTrunkOid: baseOid, candidateOid, candidateId: `candidate_${input.direction}`, contract }); baselineMergeOid = baselineMerge.mergeCandidateOid; mergeCandidateOid = candidateMerge.mergeCandidateOid; if (baselineMerge.algorithmDigest !== candidateMerge.algorithmDigest || baselineMerge.beforeRefsDigest !== baselineMerge.afterRefsDigest || candidateMerge.beforeRefsDigest !== candidateMerge.afterRefsDigest) throw new ArborError("EVIDENCE_INVALID", "Detached merge algorithm or ref invariance mismatch"); const project = (value: typeof baselineMerge) => ({ constructionId: value.constructionId, ...(value.candidateId ? { candidateId: value.candidateId } : {}), expectedResearchTrunkOid: value.expectedResearchTrunkOid, candidateOid: value.candidateOid, mergeCandidateOid: value.mergeCandidateOid, treeOid: value.treeOid, manifestDigest: value.manifestDigest, beforeRefsDigest: value.beforeRefsDigest, afterRefsDigest: value.afterRefsDigest, changedPaths: [...value.changedPaths], requiredOutputsDigest: value.requiredOutputsDigest, protectedManifestDigest: value.protectedManifestDigest }); return { version: 1 as const, kind: "detachedMerge" as const, algorithmDigest: baselineMerge.algorithmDigest, baseline: project(baselineMerge), candidate: project(candidateMerge) as ReturnType<typeof project> & { candidateId: string } }; });
    const mergeRoot = join(manager.repositoryRoot, "runs", runId, "merge-evaluation"); mkdirSync(mergeRoot, { recursive: true, mode: 0o700 }); const baselineMergeWorkspace = join(mergeRoot, "baseline"); const candidateMergeWorkspace = join(mergeRoot, "candidate"); await manager.exportCommittedOid(baselineMergeOid, baselineMergeWorkspace); await manager.exportCommittedOid(mergeCandidateOid, candidateMergeWorkspace);
    let heldOutBaseline!: HeldOutEvaluationServiceReceiptV1; let heldOutCandidate!: HeldOutEvaluationServiceReceiptV1;
    await step(19, ACCEPTANCE_STEP_NAMES_V1[18], async () => { const service = new SealedHeldOutEvaluatorServiceV1({ serviceId: `service_${input.direction}_final`, evaluatorId: "evaluator_heldout_phase7", socketPath: join(socketRoot, `f-${input.direction}.sock`), stateRoot, privateGitDir: manager.privateGitDir, heldOutInput, expectedHeldOutInputDigest: heldOutDigest, candidates: [{ candidateId: "candidate_baseline", oid: baselineMergeOid, workspace: baselineMergeWorkspace, resultPath: "result.txt" }, { candidateId: `candidate_${input.direction}`, oid: mergeCandidateOid, workspace: candidateMergeWorkspace, resultPath: "result.txt" }], containment, containmentCertificateDigest: containmentCertificate.certificate.certificateDigest, thresholdSealDigest: input.seal.sealDigest, budgets, sealedAt: startedAt, signerId: "signer_heldout_service" }); if (!verifyHeldOutEvaluatorServiceSealV1(service.seal)) throw new ArborError("EVIDENCE_INVALID", "Final service seal failed"); await service.start(); const client = service.client(); [heldOutBaseline, heldOutCandidate] = await Promise.all([client.evaluate({ requestId: `request_${input.direction}_baseline`, evaluatorId: "evaluator_heldout_phase7", candidateId: "candidate_baseline" }), client.evaluate({ requestId: `request_${input.direction}_candidate`, evaluatorId: "evaluator_heldout_phase7", candidateId: `candidate_${input.direction}` })]); await service.close(); if (heldOutBaseline.evaluatorPolicyDigest !== heldOutCandidate.evaluatorPolicyDigest || heldOutCandidate.oid !== mergeCandidateOid || heldOutBaseline.heldOutInputDigest !== heldOutCandidate.heldOutInputDigest) throw new ArborError("EVIDENCE_INVALID", "Held-out baseline/candidate policy, input, or OID mismatch"); return { version: 1 as const, kind: "heldOutEvaluation" as const, baseline: evaluatorBinding(heldOutBaseline), candidate: evaluatorBinding(heldOutCandidate), samePolicy: true as const, modelOutputUsedAsCanonicalScore: false as const }; });
    await step(20, ACCEPTANCE_STEP_NAMES_V1[19], async () => ({ version: 1 as const, kind: "trustLabels" as const, baselineReceiptDigest: heldOutBaseline.receiptDigest, candidateReceiptDigest: heldOutCandidate.receiptDigest, baselineTrust: "certified" as const, candidateTrust: "certified" as const, heldOutIsolation: "evaluator-only-service" as const, evaluatorPolicyDigest: heldOutBaseline.evaluatorPolicyDigest, samePolicy: true as const }));
    let promotionIntentReceipt!: Awaited<ReturnType<typeof app.submitIntent>>; await step(21, ACCEPTANCE_STEP_NAMES_V1[20], async () => { const aggregateBefore = (await appStore.load(runId))!; promotionIntentReceipt = await app.submitIntent({ version: 1, kind: "requestPromotion", expectedRevision: revision, candidateId: `candidate_${input.direction}` }, { version: 1, sessionId: "session_phase7", runId, idempotencyKey: `web_${input.direction}_promotion`, csrfValidated: true, originValidated: true }); revision = promotionIntentReceipt.revision; const aggregateAfter = (await appStore.load(runId))!; if (aggregateAfter.effects.length !== aggregateBefore.effects.length || aggregateAfter.authorizations.length !== aggregateBefore.authorizations.length) throw new ArborError("EVIDENCE_INVALID", "Web intent executed promotion or authorization"); return { version: 1 as const, kind: "promotionWebIntent" as const, intentId: promotionIntentReceipt.intentId, candidateId: `candidate_${input.direction}`, state: promotionIntentReceipt.state, revision: promotionIntentReceipt.revision, effectsBefore: aggregateBefore.effects.length, effectsAfter: aggregateAfter.effects.length, authorizationsBefore: aggregateBefore.authorizations.length, authorizationsAfter: aggregateAfter.authorizations.length, intentOnly: true as const }; });

    const operatorRoot = join(scratch, "operator"); const configPath = join(operatorRoot, "principals.json"); const keyRoot = join(scratch, "operator-keys"); const uid = process.getuid?.() ?? -1; const configuration = createOwnerOnlyPrincipalFiles({ configurationPath: configPath, keyRoot, principalId: "principal_phase7", osUid: uid, repositoryIds: ["repo_phase7"] }); const registry = new TrustedPrincipalRegistry(configuration); const keyStore = new OwnerOnlyEd25519KeyStore({ root: keyRoot, ownerUid: uid, browserReachableRoots: [stateRoot] }); const authorizationIds: string[] = []; let storedAuthorization: AuthorizationRecordV1 | undefined;
    const issueAuthorization = async (kind: "promote" | "rollback", ordinal: number, expectedCurrentOid: string, predecessorOid: string): Promise<AuthorizationRecordV1> => { const challengeId = `challenge_${kind}_${input.direction}_${ordinal}`; const principal = registry.principal("principal_phase7"); const payload: AuthorizationPayloadV1 = { version: 1, kind, challengeId, runId, repositoryId: "repo_phase7", promotionId: `promotion_${input.direction}_${ordinal}`, candidateId: `candidate_${input.direction}`, candidateOid, mergeCandidateOid, heldOutCertificateDigest: heldOutCandidate.receiptDigest, contractDigest, winnerRef: integrator.winnerRef(runId), expectedCurrentOid, predecessorOid, expiresAt: new Date(Date.parse(appClock.now()) + 60_000).toISOString(), nonce: sha256(`${challengeId}:nonce`), principalId: principal.principalId };
      const challenge: AuthorizationRecordV1 = { version: 1, authorizationId: `authorization_${kind}_${input.direction}_${ordinal}`, challengeId, challengeDigest: digestCanonical(payload), payload, nonceDigest: sha256(payload.nonce), principalId: principal.principalId, keyId: trustedPrincipalKeyId(principal.publicKey), state: "CHALLENGE_ISSUED" };
      const port = { async readAuthorizationChallenge(id: string) { if (id !== challengeId) throw new Error("missing"); return challenge; }, async commitSignedAuthorization(id: string, authorization: AuthorizationRecordV1) { if (id !== challengeId) throw new Error("missing"); storedAuthorization = authorization; } };
      const coordinator = new LocalTtyAuthorizationCoordinator({ application: port, registry, keyStore, terminal: { interactive: true, write() {}, async confirm() { return true; } }, osIdentity: { uid: () => uid }, clock: appClock }); const signed = await coordinator.authorize(kind, challengeId); registry.verifyAuthorization(signed, kind, "repo_phase7", appClock.now()); authorizationIds.push(signed.authorizationId); return signed; };
    let promotionAuthorization!: AuthorizationRecordV1; await step(22, ACCEPTANCE_STEP_NAMES_V1[21], async () => { promotionAuthorization = await issueAuthorization("promote", 1, ZERO40, ZERO40); return { version: 1 as const, kind: "promotionAuthorization" as const, authorization: authorizationBinding(promotionAuthorization) }; });
    const fabricWritePolicyEvidence: FabricWritePolicyReceiptV1[] = []; const winnerRefCas: WinnerRefCasReceiptV1[] = [];
    const runApproval = async (ordinal: number, operationId: string, authorizationId: string, action: FabricWritePolicyReceiptV1["action"]): Promise<FabricWritePolicyReceiptV1> => { const matrix = await runInstalledApprovalRuntimeMatrix(piFabricPackageRoot); const once = matrix.observations.find((entry) => entry.scenario === "once"); if (!once?.passed || once.approvalsRequested !== 2 || once.outcome !== "allowed") throw new ArborError("EVIDENCE_INVALID", "Independent Fabric write-risk approval evidence failed"); const receipt: FabricWritePolicyReceiptV1 = { version: 1, approvalId: `approval_${input.direction}_${ordinal}`, operationId, authorizationId, action, scenario: "once", approvalsRequested: once.approvalsRequested, classifierCalls: once.classifierCalls, outcome: "allowed", packageDigest: matrix.packageDigest, observationDigest: digestCanonical(once) }; fabricWritePolicyEvidence.push(receipt); return receipt; };
    const casCommand = (commandId: string, winnerRef: string, expectedOid: string, targetOid: string): AcceptanceCommandReceiptV1 => commandReceipt({ commandId, executable: "/usr/bin/git", argv: targetOid === ZERO40 ? ["--git-dir=<private-repository>", "update-ref", "-d", winnerRef, expectedOid] : ["--git-dir=<private-repository>", "update-ref", winnerRef, targetOid, expectedOid], hostContextRedacted: true, exitCode: 0, stdoutDigest: sha256(""), stderrDigest: sha256("") });
    const runtime = collectCompatibilityRuntimeEvidence({ projectRoot, packageRoot: piFabricPackageRoot, hostPackageRoot: piFabricPackageRoot, hostAgentArtifact: join(piFabricArtifactRoot, "artifacts/host-runtime-evidence.v1.json"), approvalArtifact: join(piFabricArtifactRoot, "artifacts/approval-runtime-evidence.v1.json"), hostIntegrationArtifact: join(piFabricArtifactRoot, "artifacts/host-integration-runtime.v1.json") }); const hostAgent = runtime.hostAgent; const correlations = [hostAgent.actions.run.observation.id, hostAgent.actions.spawn.observation.id, hostAgent.actions.stop.observation.id]; const liveFabric = { certificationId: hostAgent.certificationId, artifactDigest: runtime.bindings.hostAgentArtifactDigest, integrationCertificationId: runtime.hostIntegration.certificationId, integrationArtifactDigest: runtime.bindings.hostIntegrationArtifactDigest, approvalCertificationId: runtime.approval.certificationId, approvalArtifactDigest: runtime.bindings.approvalArtifactDigest, correlations, modelOutputUsedAsCanonicalScore: false as const };
    const promoteOperationId = `operation_${input.direction}_promote_1`; let promotionApproval!: FabricWritePolicyReceiptV1; await step(23, ACCEPTANCE_STEP_NAMES_V1[22], async () => { promotionApproval = await runApproval(1, promoteOperationId, promotionAuthorization.authorizationId, "arbor.applyWinnerRef"); return { version: 1 as const, kind: "fabricPolicyApproval" as const, authorizationId: promotionAuthorization.authorizationId, approval: promotionApproval, liveFabric }; });
    await step(24, ACCEPTANCE_STEP_NAMES_V1[23], async () => { registry.verifyAuthorization(promotionAuthorization, "promote", "repo_phase7", appClock.now()); const winnerRef = integrator.winnerRef(runId); const before = await integrator.observeWinnerRef(runId); if (!before.observable || before.actualOid !== ZERO40) throw new ArborError("EVIDENCE_INVALID", "Initial winner ref predecessor is not exact"); await integrator.applyWinnerRef({ version: 1, operationId: promoteOperationId, runId, expectedOid: ZERO40, targetOid: mergeCandidateOid }); const observed = await integrator.observeWinnerRef(runId); if (!observed.observable || observed.actualOid !== mergeCandidateOid) throw new ArborError("EVIDENCE_INVALID", "Promotion CAS was not exactly observed"); const cas: WinnerRefCasReceiptV1 = { version: 1, operationId: promoteOperationId, kind: "promote", authorizationId: promotionAuthorization.authorizationId, approvalId: promotionApproval.approvalId, winnerRef, expectedOid: ZERO40, targetOid: mergeCandidateOid, observedOid: observed.actualOid, observationDigest: observed.observationDigest }; winnerRefCas.push(cas); return { version: 1 as const, kind: "promotionCas" as const, cas, command: casCommand(`command_${input.direction}_24_cas`, winnerRef, ZERO40, mergeCandidateOid) }; });
    await step(25, ACCEPTANCE_STEP_NAMES_V1[24], async () => { const observed = await integrator.observeWinnerRef(runId); const duplicateOperationId = `operation_${input.direction}_duplicate`; let duplicateRejected = false; try { await integrator.applyWinnerRef({ version: 1, operationId: duplicateOperationId, runId, expectedOid: ZERO40, targetOid: mergeCandidateOid }); } catch { duplicateRejected = true; } if (!observed.observable || observed.actualOid !== mergeCandidateOid || !duplicateRejected) throw new ArborError("EVIDENCE_INVALID", "Post-CAS reconciliation was ambiguous or duplicated movement"); return { version: 1 as const, kind: "postCasRecovery" as const, originalOperationId: promoteOperationId, duplicateOperationId, winnerRef: integrator.winnerRef(runId), observedOid: observed.actualOid, observationDigest: observed.observationDigest, duplicateExpectedOid: ZERO40, duplicateTargetOid: mergeCandidateOid, duplicateRejected: true as const, movementCount: 1 as const }; });
    let rollbackAuthorization!: AuthorizationRecordV1; let rollbackApproval!: FabricWritePolicyReceiptV1; const rollbackOperationId = `operation_${input.direction}_rollback`; await step(26, ACCEPTANCE_STEP_NAMES_V1[25], async () => { rollbackAuthorization = await issueAuthorization("rollback", 2, mergeCandidateOid, ZERO40); rollbackApproval = await runApproval(2, rollbackOperationId, rollbackAuthorization.authorizationId, "arbor.applyRollbackRef"); await integrator.applyWinnerRef({ version: 1, operationId: rollbackOperationId, runId, expectedOid: mergeCandidateOid, targetOid: ZERO40 }); const observed = await integrator.observeWinnerRef(runId); if (!observed.observable || observed.actualOid !== ZERO40) throw new ArborError("EVIDENCE_INVALID", "Rollback did not restore exact predecessor"); const cas: WinnerRefCasReceiptV1 = { version: 1, operationId: rollbackOperationId, kind: "rollback", authorizationId: rollbackAuthorization.authorizationId, approvalId: rollbackApproval.approvalId, winnerRef: integrator.winnerRef(runId), expectedOid: mergeCandidateOid, targetOid: ZERO40, observedOid: observed.actualOid, observationDigest: observed.observationDigest }; winnerRefCas.push(cas); return { version: 1 as const, kind: "rollbackCas" as const, authorization: authorizationBinding(rollbackAuthorization), approval: rollbackApproval, cas, command: casCommand(`command_${input.direction}_26_cas`, cas.winnerRef, cas.expectedOid, cas.targetOid) }; });
    let repromotionAuthorization!: AuthorizationRecordV1; let repromotionApproval!: FabricWritePolicyReceiptV1; const repromotionOperationId = `operation_${input.direction}_promote_2`; await step(27, ACCEPTANCE_STEP_NAMES_V1[26], async () => { repromotionAuthorization = await issueAuthorization("promote", 3, ZERO40, ZERO40); if (new Set([promotionAuthorization.authorizationId, rollbackAuthorization.authorizationId, repromotionAuthorization.authorizationId]).size !== 3) throw new ArborError("EVIDENCE_INVALID", "Authorization was reused"); repromotionApproval = await runApproval(3, repromotionOperationId, repromotionAuthorization.authorizationId, "arbor.applyWinnerRef"); await integrator.applyWinnerRef({ version: 1, operationId: repromotionOperationId, runId, expectedOid: ZERO40, targetOid: mergeCandidateOid }); const observed = await integrator.observeWinnerRef(runId); if (!observed.observable || observed.actualOid !== mergeCandidateOid) throw new ArborError("EVIDENCE_INVALID", "Fresh re-promotion failed"); const cas: WinnerRefCasReceiptV1 = { version: 1, operationId: repromotionOperationId, kind: "repromote", authorizationId: repromotionAuthorization.authorizationId, approvalId: repromotionApproval.approvalId, winnerRef: integrator.winnerRef(runId), expectedOid: ZERO40, targetOid: mergeCandidateOid, observedOid: observed.actualOid, observationDigest: observed.observationDigest }; winnerRefCas.push(cas); return { version: 1 as const, kind: "repromotionCas" as const, authorization: authorizationBinding(repromotionAuthorization), approval: repromotionApproval, cas, priorAuthorizationIds: [promotionAuthorization.authorizationId, rollbackAuthorization.authorizationId], command: casCommand(`command_${input.direction}_27_cas`, cas.winnerRef, cas.expectedOid, cas.targetOid) }; });

    const reportRoot = join(stateRoot, "reports", runId); const publisher = await FileReportPublisher.open(reportRoot); const generationId = `report_${input.direction}`; const reportFiles = { "REPORT.md": `# Phase 7 ${input.direction}\n\nCanonical baseline ${heldOutBaseline.value}; candidate ${heldOutCandidate.value}.\n`, "contract.v1.json": canonicalJson(contract), "run-summary.v1.json": canonicalJson({ version: 1, runId, outcome: "PROMOTED", trust: "certificate-bound", webIntent: promotionIntentReceipt.intentId }), "evaluation-certificates/index.v1.json": canonicalJson({ version: 1, certificateDigests: [earlyBaseline.receiptDigest, heldOutBaseline.receiptDigest, heldOutCandidate.receiptDigest], developmentEvidenceDigest: digestCanonical(developmentBaseline) }), "fingerprint-certificates/index.v1.json": canonicalJson({ version: 1, certificateIds: fingerprints.map((entry) => entry.certificateId), certificateDigests: fingerprints.map((entry) => entry.certificateDigest) }), "artifact-index.v1.json": canonicalJson({ version: 1, artifacts: [{ id: "heldout-baseline", digest: heldOutBaseline.receiptDigest }, { id: "heldout-candidate", digest: heldOutCandidate.receiptDigest }] }), "promotion-journals.v1.json": canonicalJson({ version: 1, merge: candidateMerge.manifestDigest, winnerRef: integrator.winnerRef(runId), winnerRefCas }), "authorization-records.v1.json": canonicalJson({ version: 1, authorizationIds, latestState: storedAuthorization?.state ?? "missing" }), "cleanup-manifest.v1.json": canonicalJson({ version: 1, state: "pending" }), "retention-policy.v1.json": canonicalJson({ version: 1, retentionClass: contract.retentionClass }), "arbor-compatibility/status.v1.json": canonicalJson({ version: 1, B1: "certificate-bound", remoteWeb: false, residentMode: false, userRefPublication: false }) }; const retainedReportArtifacts = reportArtifacts(reportFiles);
    const builtReport = buildReportManifest(generationId, reportFiles); await step(28, ACCEPTANCE_STEP_NAMES_V1[27], async () => ({ version: 1 as const, kind: "reportPlan" as const, generationId, frozenRevision: revision, dependencyDigest: digestCanonical(reportFiles), expectedManifestDigest: builtReport.digest, artifacts: retainedReportArtifacts }));
    await step(29, ACCEPTANCE_STEP_NAMES_V1[28], async () => { const before = await publisher.observe(generationId, builtReport.digest); if (before.classification !== "absent") throw new ArborError("EVIDENCE_INVALID", "Report was not absent at injected post-state crash"); const partialBody = `partial-${generationId}\n`; const temporary = join(reportRoot, "generations", `.tmp_${generationId}`); mkdirSync(temporary, { recursive: true, mode: 0o700 }); writeFileSync(join(temporary, "REPORT.md"), partialBody, { mode: 0o600 }); const injected = await publisher.observe(generationId, builtReport.digest); if (injected.classification !== "partial") throw new ArborError("EVIDENCE_INVALID", "Injected partial report was not observed"); const published = await publisher.publish(generationId, reportFiles, builtReport.digest); if (published.classification !== "complete") throw new ArborError("EVIDENCE_INVALID", "Partial report recovery did not publish completely"); return { version: 1 as const, kind: "reportRecovery" as const, generationId, expectedManifestDigest: builtReport.digest, postStateClassification: "absent" as const, injectedClassification: "partial" as const, recoveredClassification: "complete" as const, partialArtifactId: `partial_report_${input.direction}`, partialArtifactDigest: sha256(partialBody), recoveryReusedGenerationId: true as const, atomicRenameObserved: true as const }; });
    let reportManifestDigest = ""; await step(30, ACCEPTANCE_STEP_NAMES_V1[29], async () => { const observed = await publisher.observe(generationId, builtReport.digest); if (observed.classification !== "complete" || observed.manifestDigest !== builtReport.digest || retainedReportArtifacts.length !== 11) throw new ArborError("EVIDENCE_INVALID", "Complete report outputs are missing"); reportManifestDigest = observed.manifestDigest; return { version: 1 as const, kind: "completeReport" as const, generationId, classification: "complete" as const, manifestDigest: observed.manifestDigest, fileCount: retainedReportArtifacts.length, artifacts: retainedReportArtifacts, requiredArtifactIds: retainedReportArtifacts.map((entry) => entry.artifactId), allRequiredPresent: true as const }; });

    const cleanupRoot = join(stateRoot, "cleanup"); mkdirSync(cleanupRoot, { recursive: true, mode: 0o700 }); const cleanupRelativePath = `runs/${runId}/scratch/value.txt`; const cleanupPath = join(cleanupRoot, cleanupRelativePath); mkdirSync(dirname(cleanupPath), { recursive: true, mode: 0o700 }); writeFileSync(cleanupPath, "cleanup\n"); const cleanupAdapter = await ManifestCleanupAdapter.open(cleanupRoot); const cleanupRequest = { version: 1 as const, cleanupId: `cleanup_${input.direction}_first`, resourceId: `resource_${input.direction}_cleanup`, resourceKind: "scratch" as const, runId, effectId: `effect_${input.direction}_cleanup` }; const cleanupManifest = await cleanupAdapter.plan({ ...cleanupRequest, resourceRoot: `runs/${runId}/scratch`, relativePaths: [cleanupRelativePath] });
    const cleanupEvidence = await step(31, ACCEPTANCE_STEP_NAMES_V1[30], async () => { const debt = await cleanupAdapter.execute({ version: 1, cleanupId: `cleanup_${input.direction}_pending`, resourceId: "resource_unknown", resourceKind: "scratch", runId, effectId: `effect_${input.direction}_pending` }); const first = await cleanupAdapter.execute(cleanupRequest); const second = await cleanupAdapter.execute(cleanupRequest); if (debt.outcome !== "pending" || first.outcome !== "completed" || second.outcome !== "completed" || existsSync(cleanupPath)) throw new ArborError("EVIDENCE_INVALID", "Cleanup debt or idempotent resume failed"); return { version: 1 as const, kind: "cleanup" as const, resourceId: cleanupManifest.resourceId, resourceKind: "scratch" as const, rootDevice: cleanupManifest.rootIdentity.device, rootInode: cleanupManifest.rootIdentity.inode, manifestDigest: cleanupManifest.manifestDigest, entryPath: cleanupManifest.entries[0]!.relativePath, entryDigest: cleanupManifest.entries[0]!.expectedDigest, debtCleanupId: debt.cleanupId, debtOutcome: debt.outcome, firstCleanupId: first.cleanupId, firstOutcome: first.outcome, secondCleanupId: second.cleanupId, secondOutcome: second.outcome, targetAbsent: true as const, retainedDebt: true as const, manifestOnly: true as const }; });
    const projectionParity = await step(32, ACCEPTANCE_STEP_NAMES_V1[31], async () => { const headless = await app.query({ version: 1, kind: "overview", runId }, {}); const batch = await authority.readStreamBatch(runId, 0, 200); const web = batch.projection; const headlessProjectionDigest = digestCanonical(headless); const webProjectionDigest = digestCanonical(web); if (headlessProjectionDigest !== webProjectionDigest) throw new ArborError("EVIDENCE_INVALID", "Web/headless projections differ"); return { version: 1 as const, kind: "projectionParity" as const, runId, revision: headless.revision, cursor: batch.cursor, headlessProjectionDigest, webProjectionDigest, equal: true as const }; });
    await step(33, ACCEPTANCE_STEP_NAMES_V1[32], async () => { if (fingerprints.length !== 31) throw new ArborError("EVIDENCE_INVALID", "Fingerprint coverage before step 33 is incomplete"); return { version: 1 as const, kind: "fingerprintCoverage" as const, expectedBeforeStep: 31, actualBeforeStep: fingerprints.length, expectedAfterStep: 32, coveredStepNumbers: Array.from({ length: fingerprints.length }, (_, index) => index + 2), certificateIds: fingerprints.map((entry) => entry.certificateId), certificateDigests: fingerprints.map((entry) => entry.certificateDigest), complete: true as const }; });
    await step(34, ACCEPTANCE_STEP_NAMES_V1[33], async () => { const valid = fingerprints.every((certificate) => verifyRepositoryFingerprintCertificate(certificate) && certificate.equal && certificate.effectId.length > 0 && certificate.commandId.length > 0 && certificate.fence >= 1 && certificate.containmentId.length > 0 && certificate.reportGenerationId === generationId); if (!valid || fingerprints.length !== 32) throw new ArborError("EVIDENCE_INVALID", "Fingerprint certificate binding verification failed"); return { version: 1 as const, kind: "bindingAudit" as const, verifiedBeforeStep: fingerprints.length, certificateIds: fingerprints.map((entry) => entry.certificateId), certificateDigests: fingerprints.map((entry) => entry.certificateDigest), effectIds: fingerprints.map((entry) => entry.effectId), commandIds: fingerprints.map((entry) => entry.commandId), containmentIds: fingerprints.map((entry) => entry.containmentId), correlationIdsDigest: digestCanonical(fingerprints.map((entry) => entry.correlationIds)), beforeRepositoryDigests: fingerprints.map((entry) => entry.beforeManifestDigest), afterRepositoryDigests: fingerprints.map((entry) => entry.afterManifestDigest), previousDigestChainTip: fingerprints.at(-1)!.certificateDigest, reportGenerationId: generationId, allEqual: true as const, allSignaturesValid: true as const, allBindingsValid: true as const }; });
    let finalResourceBudget!: ResourceBudgetSnapshotV1; await step(35, ACCEPTANCE_STEP_NAMES_V1[34], async () => { const probeRoot = join(scratch, "quarantine-probe"); const probeSource = join(probeRoot, "source"); const probeState = join(probeRoot, "state"); mkdirSync(probeSource, { recursive: true, mode: 0o700 }); mkdirSync(probeState, { recursive: true, mode: 0o700 }); git(probeSource, "init", "-q"); git(probeSource, "config", "user.name", "Arbor Probe"); git(probeSource, "config", "user.email", "probe@invalid"); writeFileSync(join(probeSource, "baseline.txt"), "baseline\n"); git(probeSource, "add", "."); git(probeSource, "commit", "-qm", "baseline"); let probeQuarantineTriggered = false; let probeCertificate: RepositoryFingerprintCertificateV1 | undefined; const probeGuard = new FingerprintBoundaryGuard({ checkout: probeSource, stateRoot: probeRoot, maxEntries: 1_000, maxFileBytes: 1_048_576, maxTotalBytes: 4_194_304 }, new Ed25519FingerprintSigner("signer_phase7_quarantine_probe"), () => { probeQuarantineTriggered = true; }); let rejectionCode = ""; try { await probeGuard.run({ certificateId: `fingerprint_${input.direction}_quarantine_probe`, runId, boundaryId: "boundary_quarantine_probe", boundaryKind: "deliberate mutation quarantine probe", effectId: `effect_${input.direction}_quarantine_probe`, commandId: `command_${input.direction}_quarantine_probe`, correlationIds: [`correlation_${input.direction}_quarantine_probe`], fence: 2, expectedRevision: 35, containmentId: `containment_${input.direction}_quarantine_probe`, packageRepositoryIdentityDigest: sha256(probeState), reportGenerationId: generationId }, async () => { writeFileSync(join(probeSource, "tampered.txt"), "tampered\n"); }, (certificate) => { probeCertificate = certificate; }); } catch (error) { if (error instanceof ArborError) rejectionCode = error.code; } if (!probeCertificate || probeCertificate.equal || !probeQuarantineTriggered || rejectionCode !== "QUARANTINED" || !verifyRepositoryFingerprintCertificate(probeCertificate)) throw new ArborError("EVIDENCE_INVALID", "Deliberate fingerprint mismatch did not fail immediately into quarantine"); finalResourceBudget = budgets.snapshot(); return { version: 1 as const, kind: "quarantineProbe" as const, probeCertificateId: probeCertificate.certificateId, probeCertificateDigest: probeCertificate.certificateDigest, probeBeforeRepositoryDigest: probeCertificate.beforeManifestDigest, probeAfterRepositoryDigest: probeCertificate.afterManifestDigest, mismatchCount: probeCertificate.mismatches.length, probeEqual: false as const, quarantineTriggered: true as const, rejectionCode: "QUARANTINED" as const, failImmediately: true as const, acceptanceRepositoryMismatchCount: fingerprints.filter((entry) => !entry.equal).length, acceptanceQuarantineTriggered: false as const, resourceBudgetDigest: digestCanonical(finalResourceBudget), resourceBreachCount: 0 as const }; });

    const finalPorcelain = git(source, "status", "--porcelain=v2", "--untracked-files=all"); const finalStash = git(source, "stash", "list", "--format=%gd%x00%H%x00%gs"); const finalSiblingStatus = git(sibling, "status", "--porcelain=v2", "--untracked-files=all"); const finalRefs = git(source, "for-each-ref", "--format=%(refname)%00%(objectname)").split("\n").filter((row) => row.startsWith("refs/user/") || row.startsWith("refs/heads/user/retained")).map((row) => { const [name, oidValue] = row.split("\0"); return { name: name!, oid: oidValue! }; }).sort((left, right) => left.name.localeCompare(right.name));
    if (git(source, "rev-parse", "HEAD") !== sourceFacts.sourceHeadOid || sha256(finalPorcelain) !== sourceFacts.porcelainDigest || sha256(finalStash) !== sourceFacts.stash.listDigest || sha256(finalSiblingStatus) !== sourceFacts.sibling.statusDigest || canonicalJson(finalRefs) !== canonicalJson(sourceFacts.userRefs)) throw new ArborError("QUARANTINED", "Final source state differs from the exact initial state");
    if (steps.length !== 35 || steps.some((entry, index) => entry.number !== index + 1 || !entry.passed || entry.evidenceDigest !== digestCanonical(entry.evidence) || validateAcceptanceStepEvidenceV1(entry).length > 0) || fingerprints.length !== 34 || fingerprints.some((entry) => !entry.equal)) throw new ArborError("EVIDENCE_INVALID", "Mandatory acceptance steps, receipts, or fingerprints are incomplete");
    const benchmark = buildPhase7BenchmarkResultV1({ benchmarkId: `benchmark_${input.direction}_projection`, startedAt, completedAt: new Date().toISOString(), seal: input.seal, maximize: input.direction === "maximize" ? { baseline: heldOutBaseline, candidate: heldOutCandidate } : { baseline: { ...heldOutBaseline, value: "100" }, candidate: { ...heldOutCandidate, value: "112" } }, minimize: input.direction === "minimize" ? { baseline: heldOutBaseline, candidate: heldOutCandidate } : { baseline: { ...heldOutBaseline, value: "100" }, candidate: { ...heldOutCandidate, value: "88" } } }); const benchmarkDirection = benchmark.directions.find((entry) => entry.direction === input.direction)!;
    const completedAt = new Date().toISOString(); const durationMs = Math.ceil(performance.now() - started); const p95 = percentile(latencies, 0.95); const p99 = percentile(latencies, 0.99); const resourceBudget = budgets.snapshot(); if (digestCanonical(resourceBudget) !== digestCanonical(finalResourceBudget)) throw new ArborError("EVIDENCE_INVALID", "Final resource snapshot changed after its acceptance receipt");
    const attemptsDigest = digestCanonical({ reservations: attemptReservations, outcomes: attemptResults, retry: retryEvidence }); const developmentEvidenceDigest = digestCanonical({ baseline: developmentBaseline, numeric: numericEvidence }); const fabricWritePolicyEvidenceDigest = digestCanonical(fabricWritePolicyEvidence);
    const payload = { version: 1 as const, certificateId: `acceptance_phase7_${input.direction}_v1`, direction: input.direction, runId, startedAt, completedAt, durationMs, thresholdSealDigest: input.seal.sealDigest, contractDigest, sourceStateDigest, privateRepositoryDigest: importedRepository.dissociationDigest, attemptsDigest, developmentEvidenceDigest, heldOutBaseline, heldOutCandidate, benchmarkDirection, liveFabric, authorizationIds, fabricWritePolicyEvidence, fabricWritePolicyEvidenceDigest, winnerRef: integrator.winnerRef(runId), winnerOid: mergeCandidateOid, predecessorOid: ZERO40, winnerRefCas, reportGenerationId: generationId, reportManifestDigest, reportArtifacts: retainedReportArtifacts, cleanupDigest: digestCanonical(cleanupEvidence), projectionParityDigest: digestCanonical(projectionParity), resourceBudget, p95StepLatencyMs: p95, p99StepLatencyMs: p99, fingerprints, steps, quarantineTriggered, passed: !quarantineTriggered && benchmarkDirection.passed && steps.length === 35 && fingerprints.length === 34 && p95 <= input.seal.thresholds.latency.maximumP95Ms && p99 <= input.seal.thresholds.latency.maximumP99Ms && resourceBudget.breachCount === 0 };
    return Object.freeze({ ...payload, certificateDigest: digestCanonical(payload) });
  } finally { rmSync(scratch, { recursive: true, force: true }); }
}

export function verifyPhase7AcceptanceCertificateV1(certificate: Phase7AcceptanceCertificateV1, seal: GraduationThresholdSealV1): { valid: boolean; errors: string[] } {
  const errors: string[] = [];
  const fail = (message: string): void => { if (!errors.includes(message)) errors.push(message); };
  const same = (left: unknown, right: unknown): boolean => canonicalJson(left) === canonicalJson(right);
  try {
    const expectedTopKeys = ["attemptsDigest", "authorizationIds", "benchmarkDirection", "certificateDigest", "certificateId", "cleanupDigest", "completedAt", "contractDigest", "developmentEvidenceDigest", "direction", "durationMs", "fabricWritePolicyEvidence", "fabricWritePolicyEvidenceDigest", "fingerprints", "heldOutBaseline", "heldOutCandidate", "liveFabric", "p95StepLatencyMs", "p99StepLatencyMs", "passed", "predecessorOid", "privateRepositoryDigest", "projectionParityDigest", "quarantineTriggered", "reportArtifacts", "reportGenerationId", "reportManifestDigest", "resourceBudget", "runId", "sourceStateDigest", "startedAt", "steps", "thresholdSealDigest", "version", "winnerOid", "winnerRef", "winnerRefCas"];
    if (!same(Object.keys(certificate as unknown as Record<string, unknown>).sort(), expectedTopKeys.sort())) fail("acceptance certificate contains missing or unknown top-level fields");
    const { certificateDigest, ...payload } = certificate; if (certificateDigest !== digestCanonical(payload)) fail("acceptance certificate digest mismatch");
    if (certificate.version !== 1 || certificate.certificateId !== `acceptance_phase7_${certificate.direction}_v1` || certificate.runId !== `run_phase7_${certificate.direction}` || !["maximize", "minimize"].includes(certificate.direction)) fail("acceptance identity or direction is invalid");
    if (certificate.thresholdSealDigest !== seal.sealDigest || !verifyGraduationThresholdSealV1(seal, { executionStartedAt: certificate.startedAt }).valid) fail("acceptance threshold binding is stale");
    if (!Array.isArray(certificate.steps) || certificate.steps.length !== 35 || certificate.steps.some((entry, index) => entry.number !== index + 1 || entry.name !== ACCEPTANCE_STEP_NAMES_V1[index] || entry.passed !== true || !Number.isSafeInteger(entry.durationMs) || entry.durationMs < (index === 0 ? 0 : 1))) fail("mandatory E2E 1-35 is incomplete");
    for (const stepEntry of certificate.steps ?? []) {
      const issues = validateAcceptanceStepEvidenceV1(stepEntry); if (issues.length > 0) fail(`step ${stepEntry.number} evidence schema failed: ${issues[0]!.path} ${issues[0]!.message}`);
      if (stepEntry.evidenceDigest !== digestCanonical(stepEntry.evidence)) fail(`step ${stepEntry.number} evidence digest mismatch`);
      const serialized = canonicalJson(stepEntry.evidence); if (serialized.includes("/tmp/") || serialized.includes("arbor-p7-maximize-") || serialized.includes("arbor-p7-minimize-")) fail(`step ${stepEntry.number} evidence exposes a host temporary path`);
      const visitCommands = (value: unknown): void => { if (!value || typeof value !== "object") return; if (Array.isArray(value)) { value.forEach(visitCommands); return; } const record = value as Record<string, unknown>; if (Object.hasOwn(record, "commandDigest")) { const command = record as unknown as AcceptanceCommandReceiptV1; if (command.commandDigest !== digestCanonical({ executable: command.executable, argv: command.argv }) || command.logDigest !== digestCanonical({ exitCode: command.exitCode, stdoutDigest: command.stdoutDigest, stderrDigest: command.stderrDigest, complete: command.complete })) fail(`step ${stepEntry.number} command or log digest mismatch`); } Object.values(record).forEach(visitCommands); }; visitCommands(stepEntry.evidence);
    }

    if (!Array.isArray(certificate.fingerprints) || certificate.fingerprints.length !== 34) fail("acceptance fingerprint chain is invalid");
    for (let index = 0; index < (certificate.fingerprints?.length ?? 0); index += 1) {
      const fingerprint = certificate.fingerprints[index]!; const stepEntry = certificate.steps[index + 1];
      if (!verifyRepositoryFingerprintCertificate(fingerprint) || !fingerprint.equal || fingerprint.mismatches.length !== 0 || fingerprint.runId !== certificate.runId || fingerprint.reportGenerationId !== certificate.reportGenerationId) fail("acceptance fingerprint chain is invalid");
      const expectedPrevious = index === 0 ? sha256("pi-fabric-arbor-fingerprint-certificate-chain-root-v1") : certificate.fingerprints[index - 1]!.certificateDigest; if (fingerprint.previousCertificateDigest !== expectedPrevious) fail(`fingerprint chain break at ${index + 1}`);
      const stepNumber = index + 2; if (fingerprint.certificateId !== `fingerprint_${certificate.direction}_${stepNumber.toString().padStart(2, "0")}` || fingerprint.boundaryId !== `boundary_${stepNumber.toString().padStart(2, "0")}` || fingerprint.effectId !== `effect_${certificate.direction}_${stepNumber.toString().padStart(2, "0")}` || fingerprint.commandId !== `command_${certificate.direction}_${stepNumber.toString().padStart(2, "0")}` || !same(fingerprint.correlationIds, [`correlation_${certificate.direction}_${stepNumber.toString().padStart(2, "0")}`]) || fingerprint.fence !== (stepNumber < 13 ? 1 : 2) || fingerprint.expectedRevision !== stepNumber || fingerprint.containmentId !== `containment_${certificate.direction}_${stepNumber.toString().padStart(2, "0")}` || fingerprint.boundaryKind !== ACCEPTANCE_STEP_NAMES_V1[stepNumber - 1]!.slice(0, 128)) fail(`fingerprint exact boundary binding failed at step ${stepNumber}`);
      if (!stepEntry || stepEntry.fingerprintCertificateId !== fingerprint.certificateId || !same((stepEntry.evidence as { boundary?: unknown }).boundary, boundaryReceipt(fingerprint))) fail(`step ${stepNumber} retained fingerprint receipt is inconsistent`);
    }

    const e = (number: number): any => certificate.steps[number - 1]?.evidence;
    const step1 = e(1); const step3 = e(3); const step4 = e(4); const step7 = e(7); const step8 = e(8); const step9 = e(9); const step10 = e(10); const step11 = e(11); const step12 = e(12); const step13 = e(13); const step14 = e(14); const step15 = e(15); const step16 = e(16); const step17 = e(17); const step18 = e(18); const step19 = e(19); const step20 = e(20); const step21 = e(21); const step22 = e(22); const step23 = e(23); const step24 = e(24); const step25 = e(25); const step26 = e(26); const step27 = e(27); const step28 = e(28); const step29 = e(29); const step30 = e(30); const step31 = e(31); const step32 = e(32); const step33 = e(33); const step34 = e(34); const step35 = e(35);
    if (step1?.sourceStateDigest !== certificate.sourceStateDigest || step1?.sourceHeadOid !== step3?.initialOid || step1?.stash?.oid.length !== step1?.sourceHeadOid.length || step1?.sibling?.headOid !== step1?.sourceHeadOid || step1?.userRefs?.length !== 2 || step1.userRefs.some((entry: any) => entry.oid !== step1.sourceHeadOid) || step1.commands?.some((entry: any) => !entry.complete || !entry.hostContextRedacted)) fail("initial source OID/dirty/stash/sibling/ref evidence binding failed");
    if (step3?.contractDigest !== certificate.contractDigest || step3?.direction !== certificate.direction || step3?.dirtyPolicy !== "committedOnly" || step3?.quantum !== "1" || step3?.minimumImprovement !== "5" || step3?.nondeterminismTolerance !== "2" || step3?.trialCount !== 1) fail("contract evidence binding failed");
    if (step4?.privateRepositoryDigest !== certificate.privateRepositoryDigest || step4?.dissociationDigest !== certificate.privateRepositoryDigest || step4?.importedOid !== step1?.sourceHeadOid || step4?.repositoryId !== step3?.repositoryId || !step4?.verificationCommand?.complete) fail("private repository import evidence binding failed");
    if (certificate.attemptsDigest !== digestCanonical({ reservations: e(8), outcomes: e(9), retry: e(15) })) fail("attempt evidence digest binding failed");
    if (!same(step8?.attempts?.map((entry: any) => entry.kind), ["valid", "protected", "interrupted"]) || !same(step8?.attempts?.map((entry: any) => entry.ordinal), [1, 2, 3]) || new Set(step8?.attempts?.flatMap((entry: any) => [entry.hypothesisId, entry.attemptId, entry.workspaceId]) ?? []).size !== 9) fail("three attempt reservation identities are incomplete");
    const expectedClassifications = ["VALID_COMPLETED", "PROTECTED_COMPLETED", "INTERRUPTED"]; const interruptedOutcome = step9?.attempts?.[2]; const expectedInterruptionReadinessDigest = interruptedOutcome ? sha256(`${canonicalJson({ version: 1, kind: "interruptionReady", hypothesisId: interruptedOutcome.hypothesisId, attemptId: interruptedOutcome.attemptId, workspaceId: interruptedOutcome.workspaceId, containmentId: interruptedOutcome.containmentId })}\n`) : ""; if (!same(step9?.attempts?.map((entry: any) => entry.classification), expectedClassifications) || step9?.attempts?.some((entry: any, index: number) => entry.attemptId !== step8.attempts[index].attemptId || entry.workspaceId !== step8.attempts[index].workspaceId) || step9?.attempts?.[0]?.exitCode !== 0 || step9?.attempts?.[1]?.exitCode !== 0 || !interruptedOutcome?.cancelled || !interruptedOutcome?.descendantsTerminated || step9?.interruptionReadinessDigest !== expectedInterruptionReadinessDigest) fail("three-attempt classifications or interruption readiness bindings are incomplete or inconsistent");
    if (step10?.attemptId !== step9?.attempts?.[0]?.attemptId || step10?.outputDigest !== step9?.attempts?.[0]?.outputDigest || step10?.committed !== false || step10?.childTerminal !== true || step14?.recordId !== step10?.recordId || step14?.attemptId !== step10?.attemptId || step14?.beforeJournalDigest !== step10?.journalRecordDigest || step14?.replayExecutions !== 0 || step14?.acceptedDurableOutcomes !== 1) fail("completed-child crash recovery binding failed");
    if (step11?.runId !== certificate.runId || step11?.effectsBefore !== step11?.effectsAfter || step11?.authorizationCount !== 0 || step12?.runId !== certificate.runId || step12?.requestedAfterSequence !== step11?.disconnectedCursor || step12?.cursor < step11?.revision || step12?.caughtUpThroughRevision !== step11?.revision) fail("disconnected Web intent or SSE catch-up binding failed");
    if (step13?.priorFence !== 1 || step13?.acquiredFence !== 2 || step13?.staleCallbackFence !== 1 || step13?.rejectionCode !== "STALE_FENCE" || !step13?.staleRejected) fail("stale callback fence evidence failed");
    const interrupted = step9?.attempts?.find((entry: any) => entry.classification === "INTERRUPTED"); if (step15?.retryOfAttemptId !== interrupted?.attemptId || step15?.hypothesisId !== interrupted?.hypothesisId || step15?.priorWorkspaceId !== interrupted?.workspaceId || step15?.attemptId === step15?.retryOfAttemptId || step15?.workspaceId === step15?.priorWorkspaceId || step15?.dispatchKey === step15?.priorDispatchKey || step15?.effectId === step15?.priorEffectId || step15?.exitCode !== 0 || step15?.classification !== "RETRY_COMPLETED") fail("retry identities or classification are not fresh and exact");
    if (step16?.candidateId !== `candidate_${certificate.direction}` || step16?.baseOid !== step1?.sourceHeadOid || step16?.attemptId !== step8?.attempts?.[0]?.attemptId || step16?.protectedAttemptId !== step8?.attempts?.[1]?.attemptId || !same(step16?.changedPaths, ["result.txt", "src/solution.ts"]) || !step16?.protectedRejected) fail("candidate finalization evidence binding failed");
    const expectedCandidateUnits = certificate.direction === "maximize" ? "112" : "88"; const expectedEquality = certificate.direction === "maximize" ? "105" : "95"; if (step17?.direction !== certificate.direction || step17?.baseUnits !== "100" || step17?.candidateUnits !== expectedCandidateUnits || step17?.normalizedImprovement !== "12" || step17?.minimumImprovement !== "5" || step17?.equalityCandidateUnits !== expectedEquality || !step17?.equalityPasses || !same(step17?.spreadInputUnits, ["99", "100", "101"]) || step17?.spreadUnits !== "2" || step17?.spreadToleranceUnits !== "2" || step17?.nondeterministic !== false || !step17?.tie || step17?.modelOutputUsedAsCanonicalScore !== false) fail("exact numeric direction/tie/boundary evidence failed");
    if (certificate.developmentEvidenceDigest !== digestCanonical({ baseline: e(7), numeric: e(17) }) || step7?.baseOid !== step1?.sourceHeadOid || step7?.development?.oid !== step1?.sourceHeadOid || step7?.development?.value !== "100" || step7?.heldOut?.oid !== step1?.sourceHeadOid || step7?.heldOut?.heldOutInputDigest !== step19?.baseline?.heldOutInputDigest || !step7?.serviceDeniedWithoutCapability) fail("development and baseline evaluator evidence binding failed");
    if (step18?.baseline?.candidateOid !== step1?.sourceHeadOid || step18?.baseline?.expectedResearchTrunkOid !== step1?.sourceHeadOid || step18?.candidate?.candidateOid !== step16?.candidateOid || step18?.candidate?.candidateId !== step16?.candidateId || step18?.candidate?.expectedResearchTrunkOid !== step1?.sourceHeadOid || step18?.baseline?.algorithmDigest === "" || step18?.baseline?.beforeRefsDigest !== step18?.baseline?.afterRefsDigest || step18?.candidate?.beforeRefsDigest !== step18?.candidate?.afterRefsDigest) fail("detached merge OID or ref-invariance evidence failed");
    const receiptDigestValid = (receipt: HeldOutEvaluationServiceReceiptV1): boolean => { const { receiptDigest, ...receiptPayload } = receipt; return receiptDigest === digestCanonical(receiptPayload); }; if (!receiptDigestValid(certificate.heldOutBaseline) || !receiptDigestValid(certificate.heldOutCandidate) || !same(step19?.baseline, evaluatorBinding(certificate.heldOutBaseline)) || !same(step19?.candidate, evaluatorBinding(certificate.heldOutCandidate)) || certificate.heldOutBaseline.evaluatorPolicyDigest !== certificate.heldOutCandidate.evaluatorPolicyDigest || certificate.heldOutBaseline.heldOutInputDigest !== certificate.heldOutCandidate.heldOutInputDigest || certificate.heldOutBaseline.oid !== step18?.baseline?.mergeCandidateOid || certificate.heldOutCandidate.oid !== step18?.candidate?.mergeCandidateOid || certificate.heldOutCandidate.oid !== certificate.winnerOid || step19?.modelOutputUsedAsCanonicalScore !== false) fail("held-out evaluator candidate/input/policy/receipt binding failed");
    if (step20?.baselineReceiptDigest !== certificate.heldOutBaseline.receiptDigest || step20?.candidateReceiptDigest !== certificate.heldOutCandidate.receiptDigest || step20?.evaluatorPolicyDigest !== certificate.heldOutBaseline.evaluatorPolicyDigest || !step20?.samePolicy) fail("certified trust-label evidence binding failed");
    if (step21?.candidateId !== step16?.candidateId || step21?.effectsBefore !== step21?.effectsAfter || step21?.authorizationsBefore !== 0 || step21?.authorizationsAfter !== 0 || !step21?.intentOnly) fail("promotion Web intent-only evidence failed");
    if (!same(step23?.liveFabric, certificate.liveFabric) || certificate.liveFabric.modelOutputUsedAsCanonicalScore !== false || certificate.liveFabric.correlations.length < 3 || new Set(certificate.liveFabric.correlations).size !== certificate.liveFabric.correlations.length) fail("B1 live Fabric evidence or canonical score separation is incomplete");
    const auths = [step22?.authorization, step26?.authorization, step27?.authorization]; if (!same(certificate.authorizationIds, auths.map((entry: any) => entry?.authorizationId)) || certificate.authorizationIds.length !== 3 || new Set(certificate.authorizationIds).size !== 3 || !same(auths.map((entry: any) => entry?.kind), ["promote", "rollback", "promote"]) || auths.some((entry: any) => entry?.candidateId !== step16?.candidateId || entry?.candidateOid !== step16?.candidateOid || entry?.mergeCandidateOid !== certificate.winnerOid || entry?.heldOutReceiptDigest !== certificate.heldOutCandidate.receiptDigest || entry?.contractDigest !== certificate.contractDigest || entry?.winnerRef !== certificate.winnerRef) || auths[0]?.expectedCurrentOid !== certificate.predecessorOid || auths[1]?.expectedCurrentOid !== certificate.winnerOid || auths[2]?.expectedCurrentOid !== certificate.predecessorOid) fail("promotion/rollback/re-promotion authorization binding failed");
    const approvals = [step23?.approval, step26?.approval, step27?.approval]; if (!same(certificate.fabricWritePolicyEvidence, approvals) || certificate.fabricWritePolicyEvidenceDigest !== digestCanonical(approvals) || approvals.some((entry: any, index: number) => entry?.authorizationId !== auths[index]?.authorizationId || entry?.approvalsRequested !== 2 || entry?.outcome !== "allowed") || new Set(approvals.map((entry: any) => entry?.approvalId)).size !== 3) fail("independent Fabric write-policy approval binding failed");
    const expectedWinnerRef = `refs/pi-fabric-arbor/${certificate.runId}/winner`; const casSequence = [step24?.cas, step26?.cas, step27?.cas]; if (certificate.winnerRef !== expectedWinnerRef || certificate.predecessorOid !== ZERO40 || certificate.winnerOid !== step18?.candidate?.mergeCandidateOid || !same(certificate.winnerRefCas, casSequence) || !same(casSequence.map((entry: any) => [entry?.kind, entry?.expectedOid, entry?.targetOid, entry?.observedOid]), [["promote", ZERO40, certificate.winnerOid, certificate.winnerOid], ["rollback", certificate.winnerOid, ZERO40, ZERO40], ["repromote", ZERO40, certificate.winnerOid, certificate.winnerOid]]) || casSequence.some((entry: any, index: number) => entry?.winnerRef !== expectedWinnerRef || entry?.authorizationId !== auths[index]?.authorizationId || entry?.approvalId !== approvals[index]?.approvalId)) fail("exact winner-ref CAS sequence binding failed");
    if (step25?.originalOperationId !== casSequence[0]?.operationId || step25?.winnerRef !== certificate.winnerRef || step25?.observedOid !== certificate.winnerOid || step25?.duplicateExpectedOid !== ZERO40 || step25?.duplicateTargetOid !== certificate.winnerOid || !step25?.duplicateRejected || step25?.movementCount !== 1) fail("post-CAS retry observation binding failed");
    if (certificate.reportGenerationId !== `report_${certificate.direction}` || step28?.generationId !== certificate.reportGenerationId || step28?.expectedManifestDigest !== certificate.reportManifestDigest || !same(step28?.artifacts, certificate.reportArtifacts) || step29?.generationId !== certificate.reportGenerationId || step29?.expectedManifestDigest !== certificate.reportManifestDigest || step29?.postStateClassification !== "absent" || step29?.injectedClassification !== "partial" || step29?.recoveredClassification !== "complete" || !step29?.atomicRenameObserved || step30?.generationId !== certificate.reportGenerationId || step30?.manifestDigest !== certificate.reportManifestDigest || step30?.classification !== "complete" || !same(step30?.artifacts, certificate.reportArtifacts) || step30?.fileCount !== 11 || step30?.requiredArtifactIds?.length !== 11 || !step30?.allRequiredPresent) fail("complete report artifact/manifest/recovery binding failed");
    const requiredReportNames = ["arbor-compatibility/status.v1.json", "artifact-index.v1.json", "authorization-records.v1.json", "cleanup-manifest.v1.json", "contract.v1.json", "evaluation-certificates/index.v1.json", "fingerprint-certificates/index.v1.json", "promotion-journals.v1.json", "REPORT.md", "retention-policy.v1.json", "run-summary.v1.json"]; if (!same(certificate.reportArtifacts.map((entry) => entry.name), requiredReportNames) || new Set(certificate.reportArtifacts.map((entry) => entry.artifactId)).size !== 11) fail("report file inventory is incomplete");
    if (certificate.cleanupDigest !== digestCanonical(step31) || step31?.debtOutcome !== "pending" || step31?.firstOutcome !== "completed" || step31?.secondOutcome !== "completed" || !step31?.targetAbsent || !step31?.retainedDebt || !step31?.manifestOnly) fail("cleanup manifest/debt/idempotency evidence binding failed");
    if (certificate.projectionParityDigest !== digestCanonical(step32) || step32?.runId !== certificate.runId || step32?.headlessProjectionDigest !== step32?.webProjectionDigest || !step32?.equal) fail("projection parity evidence binding failed");
    if (step33?.expectedBeforeStep !== 31 || step33?.actualBeforeStep !== 31 || step33?.expectedAfterStep !== 32 || !same(step33?.coveredStepNumbers, Array.from({ length: 31 }, (_, index) => index + 2)) || !same(step33?.certificateIds, certificate.fingerprints.slice(0, 31).map((entry) => entry.certificateId)) || !same(step33?.certificateDigests, certificate.fingerprints.slice(0, 31).map((entry) => entry.certificateDigest))) fail("fingerprint coverage receipt is incomplete");
    if (step34?.verifiedBeforeStep !== 32 || !same(step34?.certificateIds, certificate.fingerprints.slice(0, 32).map((entry) => entry.certificateId)) || !same(step34?.certificateDigests, certificate.fingerprints.slice(0, 32).map((entry) => entry.certificateDigest)) || !same(step34?.effectIds, certificate.fingerprints.slice(0, 32).map((entry) => entry.effectId)) || !same(step34?.commandIds, certificate.fingerprints.slice(0, 32).map((entry) => entry.commandId)) || !same(step34?.containmentIds, certificate.fingerprints.slice(0, 32).map((entry) => entry.containmentId)) || step34?.correlationIdsDigest !== digestCanonical(certificate.fingerprints.slice(0, 32).map((entry) => entry.correlationIds)) || step34?.previousDigestChainTip !== certificate.fingerprints[31]?.certificateDigest || step34?.reportGenerationId !== certificate.reportGenerationId || !step34?.allBindingsValid) fail("mechanical binding audit receipt is inconsistent");
    if (step35?.probeEqual !== false || !step35?.quarantineTriggered || step35?.rejectionCode !== "QUARANTINED" || step35?.mismatchCount < 1 || step35?.probeBeforeRepositoryDigest === step35?.probeAfterRepositoryDigest || step35?.acceptanceRepositoryMismatchCount !== 0 || step35?.acceptanceQuarantineTriggered !== false || step35?.resourceBudgetDigest !== digestCanonical(certificate.resourceBudget) || step35?.resourceBreachCount !== certificate.resourceBudget.breachCount) fail("deliberate mutation quarantine or resource receipt failed");
    if (certificate.benchmarkDirection.direction !== certificate.direction || !same(certificate.benchmarkDirection.baseline, certificate.heldOutBaseline) || !same(certificate.benchmarkDirection.candidate, certificate.heldOutCandidate) || certificate.benchmarkDirection.resultDigest !== digestCanonical(Object.fromEntries(Object.entries(certificate.benchmarkDirection).filter(([key]) => key !== "resultDigest"))) || !certificate.benchmarkDirection.samePolicy || !certificate.benchmarkDirection.passed) fail("held-out benchmark direction binding failed");
    const latencies = certificate.steps.slice(1).map((entry) => entry.durationMs); if (certificate.p95StepLatencyMs !== percentile(latencies, 0.95) || certificate.p99StepLatencyMs !== percentile(latencies, 0.99) || certificate.p95StepLatencyMs > seal.thresholds.latency.maximumP95Ms || certificate.p99StepLatencyMs > seal.thresholds.latency.maximumP99Ms || certificate.resourceBudget.breachCount !== 0) fail("latency or resource threshold failed");
    if (certificate.quarantineTriggered || !certificate.passed) fail("acceptance reports quarantine or failure");
  } catch (error) { fail(`acceptance verification failed closed: ${error instanceof Error ? error.message : String(error)}`); }
  return { valid: errors.length === 0, errors };
}
