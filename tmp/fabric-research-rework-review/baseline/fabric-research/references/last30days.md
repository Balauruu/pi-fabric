# Recent discussion with last30days

Use last30days for what people are saying, not as a substitute for specifications or controlled performance evidence. Main or a delegated worker may run it.

Read the installed [last30days skill](../../last30days/skills/last30days/SKILL.md) and its [agent JSON reference](../../last30days/docs/reference/json-export.md). Follow its targeting and query-planning instructions; this research assignment's scope, Browser rule and configured-provider choice take precedence over standalone setup, browser, publishing and response-format instructions. Skip onboarding, installs, credential changes and publishing.

## Execution adaptations

- Use `/home/balauru/.pi-profiles/fabric/skills/last30days/.venv/bin/python` with `-B` to run the installed skill's `scripts/last30days.py`. If the declared environment is unavailable, report that gap rather than provisioning it. Never forward the skill-level `--agent` flag to Python.
- Use a task-local directory in the Fabric profile for the plan and any saved output. Give concurrent runs separate output paths. Scope `LAST30DAYS_CONFIG_DIR`, `LAST30DAYS_MEMORY_DIR` and `TMPDIR` there, with `PYTHONDONTWRITEBYTECODE=1`. Scoping config changes which `.env` is read; use existing process auth without copying credentials or changing provider configuration.
- Use `--no-browser-cookies` and `LAST30DAYS_TRUSTPILOT_NO_BROWSER=1`. Select the needed nonbrowser sources explicitly with `--search`, excluding `xiaohongshu`/`xhs`. Prevent inherited `INCLUDE_SOURCES` from re-enabling its browser-session probe, and keep corpus inputs within the task's authorized scope. If a selected source still requires browser interaction, use an accessible alternative or report the gap. The Trustpilot option skips that source entirely.
- Set `LAST30DAYS_NATIVE_SEARCH=1` only when host search is usable through the current execution surface. This suppresses the engine's keyless-search fallback; it does not run host search. Main or the assigned worker must make the host-search calls needed for coverage and retain their supporting evidence alongside the engine results. If host search is unavailable, leave the flag unset or clear an inherited truthy value for this invocation; report any remaining coverage gap without changing provider configuration.
- Preserve requested `--days`, `--as-of`, `--quick` or `--deep` options; otherwise use the engine defaults. Follow the manual for the query plan and pass `--emit=json --json-profile=agent`. Choose a Bash timeout appropriate to the requested work, in seconds.

Before aggregation, validate `schema_version` against the installed agent JSON contract and identify the envelope. For an ordinary report, inspect its `results` and `source_status`; for a comparison, inspect each `reports[].report` with its entity label intact. Accept documented compatible additions and ignore unknown additive fields; report unsupported major versions or incompatible shapes rather than silently treating them as empty research. Discovery and preflight JSON are different contracts, not ordinary reports. Do not silently switch to unversioned raw output when the agent export is unavailable.

Inspect each report's `source_status`, URLs, relevant publication dates and platform-native engagement. Source failures can coexist with useful results or a zero exit code. A clean empty result is different from inaccessible coverage; neither proves that no discussion exists elsewhere. A missing URL needs an accessible supporting source before it supports a decisive claim. Keep useful results when another source fails, supplement only material gaps, and return any artifact paths Main needs to inspect. Main owns the synthesis.
