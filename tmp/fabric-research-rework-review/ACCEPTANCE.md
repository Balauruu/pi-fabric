# TEMPORARY REVIEW ONLY: delete before merge

Baseline: d88d720 installed skill, copied fresh after main sync. Do not ship this directory or install evaluation hooks. No fixtures, mocks, or injected failures. Maximum SIX worker launch attempts across baseline/candidate; no extra evaluator agents. Main performs evaluation and synthesis. No implementation edits before this ledger.

## Acceptance criteria
1. Main sizes effort before retrieval; direct narrow path, bounded independent distinct-uncertainty fan-out only when justified; no fixed worker number or required user count.
2. Stages frame -> preflight -> self-contained assignments -> retrieval -> evidence validation/material-gap recovery -> Main cited synthesis, with observable completion gates.
3. Preserve browser prohibition, configured providers, explicit pi/terra/high/extensions policy, captured tool grants and child discovery, profile isolation, exact agent versus stream counts, budgets, provenance, methodology gates and conditional last30days integration.
4. One canonical evidence field/rule definition; supporting files directly linked; remove primitive catalog duplication.
5. Composing fabric_exec TypeScript templates: named task payloads, defined helpers/schema, durable state between invocations, bounded outputs, real captured/native result envelopes, per-item catch preserving siblings, count-aware Main verification, final coverage and citation preparation.
6. Read agents.md fully and every directly linked local file, resolve containing-file-relative references; no directory bulk read. Verify effective APIs and report source/live conflicts or missing references.
7. Check syntax, input contracts, tool access and evidence handoffs with actual retrieval; source-backed worked example, no invented citations.
8. Compare unchanged/revised narrow lookup and substantive two-stream comparison using same prompts/worker options, inspect naturally occurring partial failure if any; otherwise mark live failure recovery unexercised. Static walkthrough is not a live pass.
9. Run standalone validator, Pi load check and smallest behavioral probes. Keep static/live results distinct. Evaluation artifacts only until pre-merge cleanup, outside shipped skill.
10. Preserve concurrent/unrelated changes; provide revised worktree files and draft PR with validation, blocked checks, reference audit and pre-merge cleanup gate.

## Initial assessment
Strengths: strong source standards and methodological gates, Main-owned synthesis, explicit worker and provider policy, useful last30days boundary, correct conceptual sibling retention/count semantics.
Gaps: no executable blocks or state handoff; no operational effort decision; no implemented dispatch/coverage ledger; prose return validation does not show envelope decoding or retrieval witness handling.
Duplication: primitive choice tour and repeated canonical evidence requirements can be replaced by a staged procedure plus one evidence contract.

## Design decisions (user confirmed)
Main assesses effort and chooses worker count; default direct-first plus bounded fan-out. Predetermined worker config and prescribed tools, no configuration selection or fallback. Live prototype validates composition with real retrieval, not a synthetic state-machine harness. Temporary evaluations are discarded before merge.

## Source audit so far
Read docs/agents.md lines 1-601 including continuation; direct local files: TS agents/mesh references, Python agents reference, configuration.md, kernels.md, components.md, fabric-workflow/council/rlm/swarm SKILL.md. Two directory links are navigation only, no bulk read. Additional API reference: skillsets/typescript/fabric-exec/SKILL.md. Inaccessible ambient path: npm/node_modules/pi-fabric/skills/fabric-exec/SKILL.md (relocated to skillsets/typescript/fabric-exec/SKILL.md). Effective agents.run currently excludes documented kernel field and Claude runner; templates will not use those. Pi skill authoring docs and creator design/evaluation guidance read.

## Publication checkpoint
User requested all main changes committed and pushed first: d88d720. research-skill-rework fast-forwarded cleanly. Existing tmp/fabric-research-eval-1788708568601 artifacts now tracked on main are unrelated earlier evaluations; include them in the pre-merge cleanup review, not the shipped skill. This new evaluation directory is not to be committed.
