{"content":[{"type":"text","text":"1. All processes using a database must be on the same host computer; WAL does not work over a network filesystem. This is because WAL requires all processes to share a small amount of memory and processes on separate host machines obviously cannot share memory with each other. 2. Transactions that involve changes against multiple ATTACHed databases are atomic for each individual database, but are not atomic across all databases as a set. 3. It is not possible to change the page_size after enteri\nSource: Write-Ahead Logging - SQLite (https://www.sqlite.org/wal.html)\n\nGenerally, if your data is separated from the application by a network, you want to use a client/server database. This is due to the fact that the database engine acts as a bandwidth-reducing filter on the database traffic. ... If your data is separated from the application by a network, you want the low-traffic link to be across the network, not the high-traffic link. This means that the database engine needs to be on the same machine as the database itself. Such is the case with a client/serve\nSource: SQLite Over a Network, Caveats and Considerations (https://www.sqlite.org/useovernet.html)\n\nI was looking at ... documentation and was wondering if ... -ahead logging is supported on SAN disks. The documentation states that network ... systems are not supported (because of exclusive locks? reliability? latency?), but the SAN disk is exclusive, low latency due to FC, not a network file system (as it is block storage ... ### (2) By Warren Young (wyoung) on 2021-09-09 15:00:00 in reply to 1 [link] [source] WAL requires shared memory between any processes that use the DB to coordinate acce\nSource: SQLite User Forum: Write-ahead logging on SAN (https://sqlite.org/forum/info/c7054d0041ee730d)\n\n---\n\n**Sources:**\n1. Write-Ahead Logging - SQLite\n   https://www.sqlite.org/wal.html\n\n2. SQLite Over a Network, Caveats and Considerations\n   https://www.sqlite.org/useovernet.html\n\n3. SQLite User Forum: Write-ahead logging on SAN\n   https://sqlite.org/forum/info/c7054d0041ee730d\n\n\n---\nResults stored as responseId \"mtqtng7erasq09\". Use get_search_content({ responseId: \"mtqtng7erasq09\", queryIndex: 0 }) to retrieve them."}],"text":"1. All processes using a database must be on the same host computer; WAL does not work over a network filesystem. This is because WAL requires all processes to share a small amount of memory and processes on separate host machines obviously cannot share memory with each other. 2. Transactions that involve changes against multiple ATTACHed databases are atomic for each individual database, but are not atomic across all databases as a set. 3. It is not possible to change the page_size after enteri\nSource: Write-Ahead Logging - SQLite (https://www.sqlite.org/wal.html)\n\nGenerally, if your data is separated from the application by a network, you want to use a client/server database. This is due to the fact that the database engine acts as a bandwidth-reducing filter on the database traffic. ... If your data is separated from the application by a network, you want the low-traffic link to be across the network, not the high-traffic link. This means that the database engine needs to be on the same machine as the database itself. Such is the case with a client/serve\nSource: SQLite Over a Network, Caveats and Considerations (https://www.sqlite.org/useovernet.html)\n\nI was looking at ... documentation and was wondering if ... -ahead logging is supported on SAN disks. The documentation states that network ... systems are not supported (because of exclusive locks? reliability? latency?), but the SAN disk is exclusive, low latency due to FC, not a network file system (as it is block storage ... ### (2) By Warren Young (wyoung) on 2021-09-09 15:00:00 in reply to 1 [link] [source] WAL requires shared memory between any processes that use the DB to coordinate acce\nSource: SQLite User Forum: Write-ahead logging on SAN (https://sqlite.org/forum/info/c7054d0041ee730d)\n\n---\n\n**Sources:**\n1. Write-Ahead Logging - SQLite\n   https://www.sqlite.org/wal.html\n\n2. SQLite Over a Network, Caveats and Considerations\n   https://www.sqlite.org/useovernet.html\n\n3. SQLite User Forum: Write-ahead logging on SAN\n   https://sqlite.org/forum/info/c7054d0041ee730d\n\n\n---\nResults stored as responseId \"mtqtng7erasq09\". Use get_search_content({ responseId: \"mtqtng7erasq09\", queryIndex: 0 }) to retrieve them.","details":{"queries":["site:sqlite.org WAL network filesystem same host computers"],"queryCount":1,"successfulQueries":1,"totalResults":3,"includeContent":false,"fetchId":null,"searchId":"mtqtng7erasq09"},"isError":false,"source":{"path":"/home/balauru/.pi-profiles/fabric/npm/node_modules/pi-web-access/index.ts","source":"npm:pi-web-access","scope":"user","origin":"package","baseDir":"/home/balauru/.pi-profiles/fabric/npm/node_modules/pi-web-access"}}