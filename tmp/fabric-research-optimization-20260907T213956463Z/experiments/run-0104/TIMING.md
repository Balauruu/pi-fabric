# Timing note — run-0104

This repair did not rerun the prior research phase, dispatch four streams, or produce a fresh end-to-end timing comparison. It reused unchanged Terra-medium evidence notes and performed a new manual reconciliation plus primary-source inspection.

The original full run’s timing is **excluded from matched timing** because its required model configuration drifted. Therefore no elapsed-time, throughput, cost, or quality comparison with that full run is claimed here.

This artifact records method scope rather than invented timing: source-note review, primary-source retrieval/checking, synthesis, and new artifact writes occurred in run-0104. No old timing artifact was overwritten.
