# Retrieval recovery

## Observed cause and fix

The trial executors were spawned without an explicit tools list. Fabric default optional-tool grants excluded web retrieval and descendants cannot widen inherited authority. Every initial root-level trial returned source-free reports. Status partial did not establish usable empirical evidence.

Explicit direct leaf tools [read, web_search, fetch_content, get_search_content] successfully searched SQLite official documentation. The same explicit grants through a recursive depth-1 coordinator into a depth-2 leaf also succeeded, observing https://www.sqlite.org/backup.html. See artifacts/explicit-web-grant-probe.json and artifacts/explicit-two-level-web-grant-probe.json.

Corrected trial coordinators receive explicit core and web-tool grants, and actual research leaves retain the narrow researcher grant. This evaluation-harness correction applies identically to legacy, baseline and candidate. No configuration or installed-skill change.

## Evidence disposition

All six prior root-level runs are EXCLUDED from quality and speed comparisons because none inspected original sources. No qualifying revision or no-improvement streak increment. Preserve artifacts and failure timing as diagnostics only. New live trial receipts must report actual inspected source count and core-evidence availability, independently reviewed against artifacts.
