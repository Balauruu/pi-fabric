# Iteration 005 evidence summary

## Tested hypothesis

The candidate removes the mandatory pre-synthesis verifier/disposition bottleneck. It uses three parallel independent research streams per explicit topic, one source-checking reconciliation/composer with sole `RESEARCH.md` ownership, independent final acceptance, and only targeted correction/recheck. No planner was launched because all three questions had explicit R1/R2/R3 decomposition.

## Live evidence

- T1, T2 and T3 each ran three fresh research leaves, one composer, one independent validator, then one targeted correction composer and independent recheck when the validator required it. All saved native receipts report completed `openai-codex/gpt-5.6-terra`, `medium`, extensions enabled and nonrecursive execution in `evidence/native-receipts.json`.
- T1 final recheck accepted 18 source-unique retained sources.
- T2 final recheck remained partial because two operational-stream sources lack an explicit report disposition.
- T3 final recheck remained rejected because source/context defects remain in AgentDojo, InjecAgent and WebArena claims. These residual defects are preserved, not silently edited.
- Two independent blind comparisons cover the literal candidate, incumbent rev04 and legacy same-topic mappings. They disagree on overall incumbent improvement. Reviewer A finds material T2 regression and no demonstrated T3 net improvement. Reviewer B finds all three improve over incumbent but both reviewers identify unmet legacy-specific evidence. Neither supports a speed claim.

## Mechanical evidence

`evidence/mechanical-checks.json` passes frontmatter, six-file inventory, topology, narrow routing, no-write, blocked-zero-source, gap-only repair, model/grant policy, canonical equality, and unchanged dense-note handling. The 9,001-line multiline probe round-tripped byte-identically by SHA-256. The 60KB single-line probe was rejected by the actual read surface.

## Controller disposition

This is a genuinely tested quality revision with relevant live evidence. It is not a promotion recommendation: unresolved T2/T3 final-acceptance defects and conflicting comparisons prevent a reliable no-regression improvement finding. Root independently owns acceptance/rejection and streak handling.
