# Fictional fixture: Archive policy v2

This is a test fixture, not a real service policy.
Effective 2025-06-01, this document supersedes v1 for new snapshots. Each project may retain 200 snapshots for 24 hours. Snapshots created before this effective date retain the v1 retention period. Snapshots include application data, not access credentials. Recovery-time guarantees remain unspecified. The unit of both quotas is snapshots per project, not per user.
