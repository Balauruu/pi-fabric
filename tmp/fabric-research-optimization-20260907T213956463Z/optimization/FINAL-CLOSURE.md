# Final verification closure

This addendum resolves the specific gaps in the earlier [read-only verification](FINAL-VERIFICATION.md). It does not upgrade the unmet research-quality objective.

## Stopping evidence — verified

Five actual tested research candidates were rejected. Incumbent-relative adjudication now verifies the first three rather than relying only on legacy comparisons:

| Iteration | Concrete incumbent-relative rejection | Evidence |
| --- | --- | --- |
| 001 | False source identity and loss of incumbent ToT per-case cost detail | [Adjudication](iteration-001/incumbent-adjudication.md) |
| 002 | Recovery gains accompanied by loss of empirical evaluator-validity evidence and calibrated human-review artifact | [Adjudication](iteration-002/incumbent-adjudication.md) |
| 003 | New unsupported Reflexion comparator and T3 coverage/operating-detail losses | [Adjudication](iteration-003/incumbent-adjudication.md) |
| 004 | T2/T3 loss of incumbent-retained decision evidence | [Recovered independent acceptance](iteration-004/acceptance-review.md) |
| 005 | Loss of independent-restart control and verified T3 source/context defects | [Source-adjudicated acceptance](iteration-005/acceptance-review.md) |

All five failed no-regression acceptance despite some local gains. Infrastructure failures were excluded. No subsequent research candidate was retained, so the streak is five. This is not a global ceiling or statistical reliability estimate.

## No-write and exact-count checks — executed

The [probe receipt](final-probes/receipt.json) records an actual no-write substantive comparison over clearly fictional local fixtures. It used exactly two research streams and five total leaves, independent verification and final validation, and returned the report inline with null saved report/state paths. Root independently checked the [five native model/effort records](final-probes/native-config-verification.json), unchanged fixture inventory and unchanged fixture contents.

The same request under a total two-agent allowance took the early blocked branch with zero launches. It did not silently remove required acceptance phases or exceed the limit.

## Exact researcher request — typechecked

Final inspection found unescaped Markdown backticks inside the TypeScript task literal. The original pattern failed Fabric’s real typechecker. The two pairs were escaped without changing the intended decoded prompt. The full corrected request then passed `satisfies Parameters<typeof agents.run>[0]`, and its decoded prompt was mechanically equal to the installed file. [Typecheck receipt](final-probes/request-typecheck.json). No external `tsc` installation was needed.

The retained production snapshot is now `../candidates/rev-04-validated/`: rev-04's research behavior plus this executable-template syntax correction, not another research-method optimization. Historical candidate snapshots remain unchanged.

## Other verified checks and limits

The earlier installed audit and direct tests establish frontmatter/manual invocation semantics, local reference targets, one-report ownership, medium model policy, full canonical-field equality and real line/byte-limited pagination, including dense lines, Unicode, trailing blanks and marker-shaped source content. The known >50KB single-physical-line read boundary remains an explicit blocker.

Exact-topic legacy-quality parity remains false. No matched speed improvement or additional-practices result is claimed. Those are unmet objectives, not runtime blockers hidden as success.
