# Iteration 002 result

## Outcome

**Tested, not retained. Matched parity remains false.**

The new coverage-portfolio mechanism was exercised by fresh Terra-medium, extension-enabled, nonrecursive T1 and T3 workflows, plus a T2 changed-contract nonregression review. All 15 native leaves completed. Exact IDs, model, effort, grants, elapsed phase times, and text sizes are in execution.json. Research streams were written by controller code and read back byte-for-byte inside that program.

## Quality evidence

- T1 validators and both blind same-topic comparisons find a tradeoff, not parity. The fresh report improves self-consistency and Self-Refine evidence, but omits legacy long-context intervention, stage-wise tool-call validity, ReAct token-burden, and verifier-conditioned repair evidence. Reviews also identify a comparison/artifact regression.
- T3 has a concise complementary recovery source, but both blind reviews find material loss of legacy evaluator-validity evidence and release-evaluation specificity. One review identifies a ToolSandbox label error in the legacy report, not the new candidate. The fresh candidate therefore does not establish at-least-legacy coverage.
- T2 nonregression is recorded at live/T2-nonregression.md. It does not supply a fresh full candidate workflow replay and cannot close parity.
- Validators did not produce an accepted candidate publication. The failed reports are retained unchanged.

## Mechanical/direct evidence

checks/mechanical.json records six-file/frontmatter/link/Terra and byte-identical runtime checks. checks/direct-boundary.txt records one-report boundaries, no-write/partial/continuation-safe contract reuse, and the local test correction. The initial overly broad validation-word assertion and a shell quoting error are source-free local test failures, explicitly excluded from quality evidence.

## Limitations

There is no matched full-execution legacy timing. No speed claim is made. The previous best remains read-only and unchanged.
